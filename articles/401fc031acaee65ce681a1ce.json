{
  "id": "401fc031acaee65ce681a1ce",
  "title": "Getting Started With Radare2",
  "summary": "Radare2 (and, to a lesser extent, it's fork Rizin) are two very popular binary analysis (and more) tools. Knowing how to use one is essential for CTFs and potentially real-world applications. Industry standard tools like IDA can cost you anywhere from hundreds to thousands of dollars! For students (like me), that isn't acceptable. However, with Radare2 (I w…",
  "content": "<p>Radare2 (and, to a lesser extent, it&#x27;s fork Rizin) are two very popular binary analysis (and more) tools. Knowing how to use one is essential for CTFs and potentially real-world applications. Industry standard tools like IDA can cost you anywhere from hundreds to thousands of dollars! For students (like me), that isn&#x27;t acceptable. However, with Radare2 (I will be calling both of these programs Radare2 as they are mostly interchangable in terms of syntax / usage), it&#x27;s extremely difficult to get started.</p>\n<figure><img src=\"https://www.megabeets.net/uploads/r2_learning_curve.png\" alt=\"Radare2 Learning Curve\" title=\"Radare2 Learning Curve, by MegaBeets\"><figcaption>Radare2 Learning Curve, by MegaBeets</figcaption></figure>\n<p>I&#x27;ve been stumbling around with Radare2 for a while now, and I want to caution you all right now: please have some basic proficiency in the assembly language of the platform you are targeting. If you will be working with x86 binaries, learn x86 assembly. If you&#x27;re working with ARM binaries, learn ARM assembly. If you&#x27;re working with x86-64 binaries, learn that too. Also, an understanding of the operating system you are using is expected.</p>\n<p>Now that the warnings are out of the way, let&#x27;s dive right into learning!</p>\n<p>I&#x27;ve assumed that you&#x27;ve already installed Radare2 / Rizin (whatever floats your boat). Installing Rizin / Radare2 should be pretty simple, based on your Linux distribution. Many distros have it in their official repositories, so installing it should be a matter of just <code>sudo apt install radare2</code> or <code>sudo pacman -Sy radare2</code>. If not, get a copy from <a href=\"https://github.com/radareorg/radare2/releases\" target=\"_blank\" rel=\"noopener noreferrer\">the official GitHub releases</a>. I won&#x27;t get into distro-specific instructions here.</p>\n<table>\n<thead>\n<tr>\n<th>Radare2 Command</th>\n<th>Rizin Command</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>radare2</code></td>\n<td><code>rizin</code></td>\n</tr>\n<tr>\n<td><code>r2pm</code></td>\n<td><code>rz-pm</code></td>\n</tr>\n<tr>\n<td><code>rabin2</code></td>\n<td><code>rz-bin</code></td>\n</tr>\n<tr>\n<td><code>r2agent</code></td>\n<td><code>rz-agent</code></td>\n</tr>\n<tr>\n<td><code>rasm2</code></td>\n<td><code>rz-asm</code></td>\n</tr>\n<tr>\n<td><code>rax2</code></td>\n<td><code>rz-ax</code></td>\n</tr>\n<tr>\n<td><code>radiff2</code></td>\n<td><code>rz-diff</code></td>\n</tr>\n<tr>\n<td><code>rafind2</code></td>\n<td><code>rz-find</code></td>\n</tr>\n<tr>\n<td><code>ragg2</code></td>\n<td><code>rz-gg</code></td>\n</tr>\n<tr>\n<td><code>rahash2</code></td>\n<td><code>rz-hash</code></td>\n</tr>\n<tr>\n<td><code>rarun2</code></td>\n<td><code>rz-run</code></td>\n</tr>\n<tr>\n<td><code>rasign2</code></td>\n<td><code>rz-sign</code></td>\n</tr>\n<tr>\n<td>No equivalent</td>\n<td><code>rz-test</code></td>\n</tr>\n</tbody>\n</table>\n<p>Radare2 also offers a shorthand version of the <code>radare2</code> command called <code>r2</code>. Rizin at this point does not have an alternative.</p>\n<p>All of the source code and binaries for this article can be found in <a href=\"https://git.ersei.net/getting-started-radare2\" target=\"_blank\" rel=\"noopener noreferrer\">this git repo</a>.</p>\n<h1>Your First Binary<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h1>\n<h2>First steps<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h2>\n<p>First, let&#x27;s figure out the basics of Radare2. <a href=\"https://git.ersei.net/getting-started-radare2.git/plain/00-first-binary/first-binary\" target=\"_blank\" rel=\"noopener noreferrer\">Download the <code>first-binary</code></a> binary attached to this post or compile it yourself from the sourcehut repo above. First, let&#x27;s see what the file you just downloaded (or compiled) is.</p>\n<p><strong>You should not run any binary that you don&#x27;t trust!</strong></p>\n<pre><code><span>$</span><span> ./first-binary</span>\nHello World!</code></pre>\n<p>That was simple. Let&#x27;s take a deeper look at the binary itself.</p>\n<pre><code><span>$</span><span> file first-binary</span>\nfirst-binary: ELF 32-bit LSB pie executable, Intel 80386, version 1 (SYSV), dynamically linked, interpreter /lib/ld-linux.so.2, BuildID[sha1]=977c5cd4724f1e78c23e1edbaa01f8ae149890fd, for GNU/Linux 4.4.0, not stripped</code></pre>\n<p>Let&#x27;s dissect this. We know that the file is a 32 bit file for Linux and it is <a href=\"https://en.wikipedia.org/wiki/Stripped_binary\" target=\"_blank\" rel=\"noopener noreferrer\">not stripped</a>.</p>\n<blockquote>\n<p>Compiled binary files can contain debug information which is not necessary for program execution, rather it is useful for debugging and finding problems or bugs in the program. Stripped binary is a binary file without these debugging symbols and thus lesser in size and gives potentially better performance (when done at compile time) than a non-stripped binary.\nA stripped binary makes it hard to disassemble or reverse engineer which also in turn makes it difficult to find problems or bugs in the program.</p>\n</blockquote>\n<p>Because this binary is not stripped, it&#x27;ll be easier for us to reverse it. We can also get some information about the binary from Radare2&#x27;s tools with <code>r2bin -I first-binary</code>.</p>\n<pre><code>arch     x86\nbaddr    0x0\nbinsz    13983\nbintype  elf\nbits     32\ncanary   false\nclass    ELF32\ncompiler GCC: (GNU) 10.2.0\ncrypto   false\nendian   little\nhavecode true\nintrp    /lib/ld-linux.so.2\nladdr    0x0\nlang     c\nlinenum  true\nlsyms    true\nmachine  Intel 80386\nmaxopsz  16\nminopsz  1\nnx       true\nos       linux\npcalign  0\npic      true\nrelocs   true\nrelro    partial\nrpath    NONE\nsanitiz  false\nstatic   false\nstripped false\nsubsys   linux\nva       true</code></pre>\n<p>This gives us more binary-specific information about the file itself. We got architecture from the <code>file</code> command, but we also now know that the binary is little-endian, was compiled with GCC 10.2.0, and some more information that would be useful with more complex tasks.</p>\n<p>Now, let&#x27;s open the actual binary with <code>radare2 first-binary</code>.</p>\n<p>The first thing you should do when opening a binary in Radare2 is to analyze it with the <code>aaa</code> command.</p>\n<p>The next thing we see when we open Radare2 is the prompt itself. The prompt contains the current &quot;address&quot;. In our case, it is <code>0x00001060</code>. Radare2 started at this point because it is the &quot;entrypoint&quot; for the program, or where the binary starts execution. We can get entrypoints with the <code>ie</code> command.</p>\n<p>All Radare2 commands have this format: <code>ie</code> means <code>info</code> -&gt; <code>entrypoints</code>. There is a &quot;parent&quot; command, and a &quot;child&quot; command. To get all &quot;child&quot; commands, type in <code>?</code>. For example, use <code>i?</code> to get all information subcommands. Type in just <code>?</code> to get all commands. The <code>?</code> operator can keep on going too, with <code>ie?</code> showing help on the <code>ie</code> and <code>iee</code> commands.</p>\n<p>After running <code>ie</code>, we get the address of the entrypoint of this program (the same as our prompt).</p>\n<pre><code>[Entrypoints]\nvaddr=0x00001060 paddr=0x00001060 haddr=0x00000018 hvaddr=0x00000018 type=program\n\n1 entrypoints</code></pre>\n<p>Now, for a little bit of C trivia: what function does every C program have? If you said <code>main</code>, you&#x27;re right! That brings us to a very important command in Radare2, the &quot;seek&quot; or <code>s</code> command. We can use the seek command to go to a position in the program. Because this program doesn&#x27;t have debug symbols removed, we can simply seek to the main function with <code>s main</code>. Notice how the prompt changes.</p>\n<h2>Disassembly<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h2>\n<p>Now we are at a position where we can actually begin to look at the binary itself. Running the <code>pdf</code> (print disassembled function) command gives us the disassembly view of the main function.</p>\n<pre><code>[0x0000119d]&gt; pdf\n┌ int main (char **argv);\n│           ; var int32_t var_8h @ ebp-0x8\n│           ; arg char **argv @ esp+0x24\n│           0x0000119d      lea   ecx, [argv]\n│           0x000011a1      and   esp, 0xfffffff0\n│           0x000011a4      push  dword [ecx - 4]\n│           0x000011a7      push  ebp\n│           0x000011a8      mov   ebp, esp\n│           0x000011aa      push  ebx\n│           0x000011ab      push  ecx\n│           0x000011ac      call  sym.__x86.get_pc_thunk.ax\n│           0x000011b1      add   eax, 0x2e4f\n│           0x000011b6      sub   esp, 0xc\n│           0x000011b9      lea   edx, [eax - 0x1ff8]\n│           0x000011bf      push  edx                                  ; const char *s\n│           0x000011c0      mov   ebx, eax\n│           0x000011c2      call  sym.imp.puts                         ; int puts(const char *s)\n│           0x000011c7      add   esp, 0x10\n│           0x000011ca      mov   eax, 0\n│           0x000011cf      lea   esp, [var_8h]\n│           0x000011d2      pop   ecx\n│           0x000011d3      pop   ebx\n│           0x000011d4      pop   ebp\n│           0x000011d5      lea   esp, [ecx - 4]\n└           0x000011d8      ret</code></pre>\n<p>If you don&#x27;t understand assembly, perhaps you can use a decompiler like <a href=\"https://github.com/radareorg/r2ghidra\" target=\"_blank\" rel=\"noopener noreferrer\">Ghidra</a> instead. If you do know assembly, it is pretty clear what is going on here: we&#x27;re passing an object at a position to the <code>sym.imp.puts</code> (printf) function. All this binary does is print something!</p>\n<h2>Flags and Strings<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h2>\n<p>Now that we know what the binary is doing, we can take a look around the other parts of the binary and extract the string that it is returning. That brings us to the next big thing with Radare2: flags. When we ran the analysis command earlier, the symbols in the binary were all found and mapped as flags to positions in the file itself. That was how we could do <code>s main</code>. There are different types of flags (called flag spaces) that we can see with <code>fs</code>.</p>\n<pre><code>  0 * classes\n  1 * functions\n  6 * imports\n  0 * registers\n  6 * relocs\n 30 * sections\n 13 * segments\n  1 * strings\n 37 * symbols</code></pre>\n<p>We can list all of the flags in the current namespace (denoted with a <code>*</code>) with <code>f</code>. To switch flag spaces, run <code>fs strings</code> to move to the strings flagspace, and run <code>f</code> again.</p>\n<pre><code>0x00002008 13 str.Hello_World</code></pre>\n<p>We have a flag called <code>str.Hello_World</code> that points to the location <code>0x00002008</code>, which happens to have the string if we seek there and print out the line. <code>s 0x00002008</code> brings us to that location and <code>pd 1</code> prints one disassembled line (we actually get two because one is a comment).</p>\n<pre><code>;-- str.Hello_World:\n0x00002008     .string &quot;Hello World!&quot; ; len=13</code></pre>\n<p>That&#x27;s it. That&#x27;s the binary. We found the string.</p>\n<p>To switch back to all flag spaces, do <code>fs *</code>.</p>\n<h1>Patching<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h1>\n<p>Now it gets a little more difficult. What happens when we want to modify a program to do what we want? Using the <a href=\"https://git.ersei.net/getting-started-radare2.git/tree/01-patching/patching\" target=\"_blank\" rel=\"noopener noreferrer\">patching binary</a> (or compile it from <a href=\"https://git.ersei.net/getting-started-radare2.git/tree/01-patching\" target=\"_blank\" rel=\"noopener noreferrer\">git</a>), we&#x27;re gonna find out.</p>\n<p>Let&#x27;s run the binary and see what happens.</p>\n<pre><code>$ ./patching\nCan&#x27;t touch this.</code></pre>\n<p>Hmm. What&#x27;s going on behind the scenes? Let&#x27;s open the binary in Radare2 in write mode with <code>radare2 -w patching</code>. This will allow us to modify the binary.</p>\n<p>Seeking to the main function and printing the disassembled code, we see something interesting.</p>\n<pre><code>┌ int main (char **argv);\n│           ; var int32_t var_4h @ ebp-0x4\n│           ; arg char **argv @ esp+0x24\n│           0x000011e4      lea   ecx, [argv]\n│           0x000011e8      and   esp, 0xfffffff0\n│           0x000011eb      push  dword [ecx - 4]\n│           0x000011ee      push  ebp\n│           0x000011ef      mov   ebp, esp\n│           0x000011f1      push  ecx\n│           0x000011f2      sub   esp, 4\n│           0x000011f5      call  sym.__x86.get_pc_thunk.ax\n│           0x000011fa      add   eax, 0x2e06\n│           0x000011ff      sub   esp, 0xc\n│           0x00001202      push  5                                    ; uint32_t arg_8h\n│           0x00001204      call  sym.canttouchthis\n│           0x00001209      add   esp, 0x10\n│           0x0000120c      mov   eax, 0\n│           0x00001211      mov   ecx, dword [var_4h]\n│           0x00001214      leave\n│           0x00001215      lea   esp, [ecx - 4]\n└           0x00001218      ret</code></pre>\n<p>It appears that we are calling the function <code>sym.canttouchthis</code> with the argument of <code>5</code>. Let&#x27;s see what goes on in that function. Seeking to the function and printing it gives us the following:</p>\n<pre><code>            ; CALL XREF from main @ 0x1204\n┌ sym.canttouchthis (uint32_t arg_8h);\n│           ; var int32_t var_4h @ ebp-0x4\n│           ; arg uint32_t arg_8h @ ebp+0x8\n│           0x0000119d      push  ebp\n│           0x0000119e      mov   ebp, esp\n│           0x000011a0      push  ebx\n│           0x000011a1      sub   esp, 4\n│           0x000011a4      call  sym.__x86.get_pc_thunk.ax\n│           0x000011a9      add   eax, 0x2e57\n│           0x000011ae      cmp   dword [arg_8h], 0xa\n│       ┌─ 0x000011ca      sub   esp, 0xc\n│      │    0x000011cd      lea   edx, [eax - 0x1ff3]\n│      │    0x000011d3      push  edx                                  ; const char *s\n│      │    0x000011d4      mov   ebx, eax\n│      │    0x000011d6      call  sym.imp.puts                         ; int puts(const char *s)\n│      │    0x000011db      add   esp, 0x10\n│      │    ; CODE XREF from sym.canttouchthis @ 0x11c8\n│      └──&gt; 0x000011de      nop\n│           0x000011df      mov   ebx, dword [var_4h]\n│           0x000011e2      leave\n└           0x000011e3      ret</code></pre>\n<p>This is where knowing your assembly really helps. The code does the following: checks if the variable <code>arg_8h</code> is equal to <code>0xa</code>. If not, print something. If so, print something else. Assuming that you can&#x27;t figure out how to get the strings (this is just a sample binary, the actual &quot;win&quot; function may be obfuscated), the best thing to do would be to either set the argument or change the control flow. Let&#x27;s go with the first option. Seeking back to the main function, we can edit the line at position <code>0x00001202</code> to be <code>0xa</code> (or 10). That will send us to the proper print function. But how do we edit it?</p>\n<p>First, we have to open up visual mode with <code>V</code> (enter), and switch to the disassembly view with <code>p</code>. &quot;Scroll&quot; down to the line we want to patch at <code>0x1202</code> and press <code>A</code>. This will put us into edit mode, and we can directly type in the Assembly code. We want to change this to <code>push 0xa</code>, so let&#x27;s do that. After saving the changes, we can quit Radare2 with <code>q</code> to exit the visual mode and <code>^D</code> to exit the app. Running our modified program gives us another result:</p>\n<pre><code><span>$</span><span> ./patching</span>\nNice</code></pre>\n<h1>Closing Thoughts<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h1>\n<p>Radare2 is difficult. I&#x27;m still learning and I could not have learned how to do any of this without hands on experiments from CTFs. It&#x27;s gonna be trial and error for you all, and if you find out how to do something, maybe you will write about it too.</p>\n<h1>Credits<a target=\"_blank\" rel=\"noopener noreferrer\"></a></h1>\n<p>I&#x27;ve learned Radare2 from the <a href=\"https://www.megabeets.net/a-journey-into-radare-2-part-1/\" target=\"_blank\" rel=\"noopener noreferrer\">MegaBeets article</a> on Radare2. Most of this post is inspired from the examples given in that article. I definitely recommend giving it a read.</p>\n<p>If anything is inaccurate, feel free to <a href=\"https://ersei.net/en/contact-me\" target=\"_blank\" rel=\"noopener noreferrer\">contact me</a>.</p>",
  "image": "https://www.megabeets.net/uploads/r2_learning_curve.png",
  "publishedAt": "2025-03-24T17:59:49Z",
  "sourceId": "3bdb78ed4280c0bc",
  "sourceTitle": "Ersei 'n Stuff",
  "link": "https://ersei.net/en/blog/getting-started-with-rizin-radare2"
}
