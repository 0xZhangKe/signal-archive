<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>The Cloudflare Blog</title>
    <description>Technical deep dives, product updates, and insights from the teams that are helping to build a better Internet.</description>
    <link>https://blog.cloudflare.com/</link>
    <language>en-us</language>
    <atom:link href="https://blog.cloudflare.com/rss/" rel="self" type="application/rss+xml"/>
    <image>
      <url>https://blog.cloudflare.com/favicon.ico</url>
      <title>The Cloudflare Blog</title>
      <link>https://blog.cloudflare.com</link>
    </image>
    <lastBuildDate>Mon, 03 Aug 2026 18:33:40 GMT</lastBuildDate>
    <item>
      <title>Your agent needs a computer, not a container — introducing @cloudflare/computer</title>
      <link>https://blog.cloudflare.com/cloudflare-computer/</link>
      <pubDate>Mon, 03 Aug 2026 13:15:24 GMT</pubDate>
      <description>Agents need more than just a container to scale. We're introducing @cloudflare/computer, an agent runtime that dynamically orchestrates between fast, efficient isolates and full Linux containers to give every agent a computer of its own.</description>
      <dc:creator>Matt Carey</dc:creator>
      <dc:creator>Aron Carroll</dc:creator>
      <category>Agents</category>
      <category>Agents Week</category>
      <category>AI</category>
      <category>Cloudflare Workers</category>
      <category>Containers</category>
      <content:encoded><![CDATA[<p>The most capable agents have something simple in common: they are given their own computer to work with.</p><p>Coding agents work this way. You give them a filesystem, a shell, tools, packages, and the ability to run code. They inspect the environment, make changes, test their work, and keep going. The computer gives the model a familiar way to act on the world. At Cloudflare, we’re working hard to provide the right primitives on which to build the most capable agents.</p><p><strong>Today we’re introducing an early preview of <a href="https://github.com/cloudflare/workspace"><span style="text-decoration:underline">@cloudflare/computer</span></a>.</strong> The @cloudflare/computer package provides an agent runtime where the details and mechanics of what code runs in an isolate, a container sandbox, or a web browser are handled by the platform. Each agent gets a computer, the runtime optimizes for efficiency, and scalability.</p><p>We believe that in order to meet the growing demand for compute required by agentic systems we need to look to solutions beyond traditional containerization. </p><h2>Changing how agents are built</h2><p>We’ve seen a subtle evolution of this story over the past six months. At the start of the year, spinning up a container and running an agent inside of it was the norm. In recent months, we’ve seen a rapid move for agent harnesses to provide sandboxed code execution via tools. This separates the hands (the sandbox where work is done) from the brain (the agent loop).</p><!-- unsupported block: image --><p>No matter where the harness runs, giving every agent a container presents a challenge — across all the clouds, all the hyperscalers, there’s nowhere near enough compute in the world for every company to give each of their users’ agents their own containerized compute environment. This will not scale to hundreds of millions, then billions, of concurrent agents. This is why there is desperate, panicked industry demand for CPU compute, not just GPU compute.</p><p>We’ve been working on this problem for a long time at Cloudflare, creating a more efficient compute primitive: isolates. We made that out-of-consensus bet almost 10 years ago when we <a href="https://blog.cloudflare.com/introducing-cloudflare-workers/"><span style="text-decoration:underline">introduced Cloudflare Workers</span></a>. We made it again when we <a href="https://blog.cloudflare.com/introducing-workers-durable-objects/"><span style="text-decoration:underline">introduced Durable Objects</span></a> almost six years ago. We made this bet because isolates are infinitely horizontally scalable. They spin up and tear down incredibly quickly. They can <a href="https://developers.cloudflare.com/durable-objects/examples/websocket-hibernation-server/"><span style="text-decoration:underline">hibernate</span></a> when the agent is idle, <a href="https://blog.cloudflare.com/sqlite-in-durable-objects/"><span style="text-decoration:underline">store the agent’s own state</span></a>, and even <a href="https://blog.cloudflare.com/dynamic-workers/"><span style="text-decoration:underline">spin up their own isolates</span></a> to run untrusted code. Isolates are the best way to scale horizontally, and horizontal scale is what agents demand.</p><!-- unsupported block: image --><p>Last year, we <a href="https://blog.cloudflare.com/containers-are-available-in-public-beta-for-simple-global-and-programmable/"><span style="text-decoration:underline">gave isolates the ability to spin up their own container sandboxes</span></a>. From day one, Cloudflare’s architecture has been designed to run the agent harness in the isolate (in a Durable Object) and call an attached container on-demand as a tool. This allows you to utilize heavier compute primitives only when required, optimizing performance and cost. Durable Objects scale infinitely horizontally, and the attached container lets it scale vertically to perform any task. This is how we build agents ourselves, and we’re seeing customers build incredible things this way too.</p><!-- unsupported block: image --><p>But when we look at this need to have multiple underlying compute primitives to build agents (isolates and containers) and the need for our customers and developers to combine them themselves in userspace, we think we can do better. We think that we can provide a simpler abstraction.</p><p>That’s why we’re starting this experiment by shipping @cloudflare/computer as an open-source library, to learn with our customers who are pushing the bounds of running agents at scale.</p><h2>A shared filesystem across isolates and containers</h2><p>The @cloudflare/computer package starts with a simple premise: what if we give an agent a primed filesystem, declaratively defined, containing everything required for the task at hand and a selection of execution environments to operate on those files, each with their own pros and cons regarding speed, capability and cost?</p><p>It turns out that agents today are surprisingly capable of selecting the right environment for the task at hand. A job that only needs to manipulate files, process data, or manage a git repository can run inside an isolate. A command that needs Linux, <code>npm</code>, or a native binary can run inside a container. Both work against the same files that are kept in sync with the source filesystem.</p><!-- unsupported block: image --><p>The @cloudflare/computer package provides a durable filesystem that you can use with git repositories, storage buckets or any files you choose. It provides tools that let you read, write and edit files using <a href="https://blog.cloudflare.com/code-mode/"><span style="text-decoration:underline">Code Mode</span></a> or bash commands. All operations are gated, audited and observed, giving you fine-grained control over changes the agent is allowed to perform as well as a clear paper trail showing what the agent did.</p><h2>How you use it</h2><p>An instance of a @cloudflare/computer workspace can be instantiated on any Durable Object to provide a virtual filesystem and execution runtime.</p><p>It is installed via npm:</p><!-- unsupported block: code --><p>The primary use case is to provide that filesystem and tooling to an agent. For example, here’s how to instantiate the workspace on an agent powered by @cloudflare/think intended to triage bug reports.</p><!-- unsupported block: code --><p>Several execution backends are provided as part of the @cloudflare/computer package, or you can write your own. Here we wire up a Cloudflare Container.</p><!-- unsupported block: code --><p>Expose the file, git, and shell tools alongside product specific tools to reply to reported issues.</p><!-- unsupported block: code --><p>The model can use tools during the agent loop, but you can also use the workspace API directly, for example, to prepare the environment before prompting the agent.</p><!-- unsupported block: code --><p>Check out the <a href="https://github.com/cloudflare/computer"><span style="text-decoration:underline">workspace repository</span></a> for more examples of how to use the different backends and tools including a <a href="https://github.com/cloudflare/computer/tree/main/examples/tutorial"><span style="text-decoration:underline">step-by-step tutorial </span></a>walking through building an agent from scratch.</p><h2>How it works</h2><p>The central piece of @cloudflare/computer is the workspace. A virtual filesystem backed by SQLite that can be populated from various sources including cloud storage and source control.</p><p></p><!-- unsupported block: image --><p>The workspace supports optional execution runtimes that allow code to be run against the file system. All runtimes support the same interface <code>exec(string, options)</code> and currently two are provided out of the box (but you can write your own):</p><ul><li>An isolate-based runtime environment that uses <a href="https://justbash.dev/"><span style="text-decoration:underline">just-bash</span></a> to translate shell code into JavaScript runs in a <a href="https://developers.cloudflare.com/dynamic-workers/"><span style="text-decoration:underline">dynamic worker</span></a>. Here, the filesystem is available directly via worker bindings.</li><li>A container runtime that uses <a href="https://developers.cloudflare.com/containers/"><span style="text-decoration:underline">Cloudflare Containers</span></a> to provide a full Linux environment. Here, the filesystem is provided via a Filesystem in Userspace (FUSE) mount, which ensures files are available to the container and changes are synced back.</li></ul><p>The <code>Workspace</code> class provides an API interface for manipulating the filesystem directly as well as a <code>node:fs</code> compatible wrapper so that it can be used easily with third-party JavaScript libraries.</p><!-- unsupported block: image --><p>For use with agents, we provide an AI SDK compatible toolkit that provides the most common tools: read, write, edit, ls and exec. The exec tool is a little special as it works across the runtimes taking a <code>backend</code> argument. The tool description guides the agent into choosing the correct runtime for the task at hand: either a fast, cheap worker backend or the fully featured container. In our testing, the frontier models are very good at making the correct decision and falling back to using containers only when needed.</p><h2>What’s next</h2><p>Here at Cloudflare we’re already seeing agents exclusively using isolates to build, test, and deploy JavaScript applications with modern tooling, generate tailored documentation for each of our customers, and use web browsers to perform complex tasks.</p><p>Our goal with @cloudflare/computer is to provide an agent with a runtime where a container is required for less than 10% of its work, and coding tasks, audio/video manipulation, and document creation can all be handled by isolates.</p><p>Try out the <a href="https://github.com/cloudflare/computer"><span style="text-decoration:underline">early preview today</span></a> - we can’t wait to hear your thoughts.</p>]]></content:encoded>
      <guid isPermaLink="false">01KZ0Q4HGGZF4CDMZVFZRGTTCF</guid>
    </item>
    <item>
      <title>Cloudflare Workers and Containers now support inbound TCP connections and gRPC</title>
      <link>https://blog.cloudflare.com/grpc-workers/</link>
      <pubDate>Mon, 03 Aug 2026 13:00:00 GMT</pubDate>
      <description>Cloudflare Workers now support inbound TCP connections via Spectrum, allowing direct socket forwarding to Durable Objects and Containers. Developers can run full-duplex gRPC applications or leverage automatic gRPC-to-gRPC-web translation directly within Workers.</description>
      <dc:creator>Mar Witek</dc:creator>
      <dc:creator>Thomas Rubini</dc:creator>
      <category>Agents Week</category>
      <category>Cloudflare Workers</category>
      <category>Containers</category>
      <category>Developers</category>
      <category>gRPC</category>
      <category>Product News</category>
      <content:encoded><![CDATA[<p>AI is changing how people interact with computers, and voice is becoming an increasingly important part of that shift. Real-time assistants, AI-powered dictation, and other voice interfaces need low-latency communication between clients, models, and supporting services. Many developers use <a href="https://grpc.io/"><span style="text-decoration:underline">gRPC</span></a>, a Remote Procedure Call (RPC) framework built on HTTP/2 and TCP, for this infrastructure.</p><p>Ever since Workers <a href="https://blog.cloudflare.com/introducing-cloudflare-workers/"><span style="text-decoration:underline">launched in 2017</span></a>, we’ve been expanding their capabilities, including adding the ability to <a href="https://blog.cloudflare.com/workers-tcp-socket-api-connect-databases/"><span style="text-decoration:underline">open outbound TCP connections</span></a> and a <a href="https://blog.cloudflare.com/workers-javascript-modules/"><span style="text-decoration:underline">JavaScript-native RPC system</span></a> built on <a href="https://capnproto.org/"><span style="text-decoration:underline">Cap’n Proto</span></a>. And so as part of Agents Week, we’re extending Workers in the other direction, supporting inbound TCP connections and adding new ways to run gRPC applications on Cloudflare.</p><p>Today, we’re announcing:</p><ul><li><strong>connect(socket)</strong> — a new <a href="https://developers.cloudflare.com/workers/runtime-apis/handlers/"><span style="text-decoration:underline">handler</span></a> in the Workers runtime that lets your Worker directly accept an inbound TCP socket provided by <a href="https://developers.cloudflare.com/spectrum/"><span style="text-decoration:underline">Spectrum</span></a> (Cloudflare’s ingress proxy for non-HTTP traffic)</li><li><strong>Full-duplex, bi-directional gRPC from Cloudflare Containers</strong> — forward the socket from your Worker to your gRPC server running in a container</li><li><strong>Workers can serve unary and server-streaming gRPC APIs and call gRPC servers </strong>— you write your code using <a href="https://grpc.io/docs/platforms/web/"><span style="text-decoration:underline">gRPC-web</span></a>, and Cloudflare automatically converts incoming and outgoing requests to gRPC</li></ul><p>We’re introducing this in private beta — you can sign up <a href="https://forms.gle/Q2SoJLUKjBFGxBgW6"><span style="text-decoration:underline">here</span></a>.</p><p>Let’s dig into each of these below.</p><h2>connect(socket) from your Worker to Durable Objects and Containers</h2><p>The Workers runtime now provides a <a href="https://developers.cloudflare.com/workers/runtime-apis/handlers/"><span style="text-decoration:underline">connect() handler</span></a> that accepts a socket that you can read from and write to:</p><!-- unsupported block: code --><p>You can pass this socket from one Worker to another Worker, or from a Worker to a Durable Object. This lets your Worker control where an incoming TCP connection is routed:</p><!-- unsupported block: code --><p>You can pass a socket from a Durable Object to <a href="https://developers.cloudflare.com/durable-objects/api/container/"><span style="text-decoration:underline">its Container</span></a>:</p><!-- unsupported block: code --><p>And then handle the socket in the container:</p><!-- unsupported block: code --><p>This gives you full control over the entire path from client to your server running in a container on Cloudflare, opening the door to full-duplex communication between client and server running any program, in any language, for any TCP-based protocol.</p><p>To expose the raw TCP socket to the client, we’re introducing a new type of <a href="https://developers.cloudflare.com/spectrum/"><span style="text-decoration:underline">Spectrum</span></a> application, where you specify a Worker that you want incoming TCP connections to be routed to. <a href="https://developers.cloudflare.com/spectrum/"><span style="text-decoration:underline">Spectrum</span></a> is Cloudflare’s ingress proxy for non-HTTP traffic, and allows Cloudflare to sit in front of any TCP or UDP application.</p><h2>Bidirectional gRPC from Cloudflare Containers</h2><p><a href="https://grpc.io/"><span style="text-decoration:underline">gRPC</span></a> is a well-established and popular Remote Procedure Call (RPC) framework that was initially released by Google almost 10 years ago, and is now used across mobile apps, distributed systems, and most recently — voice AI applications.</p><p>Real-time voice AI applications demand low-latency, and both client and server to be able to send messages to each other over a single, persistent connection. WebSockets and Durable Objects are excellent fits for this, and the Cloudflare Agents SDK provides <a href="https://developers.cloudflare.com/agents/communication-channels/voice/"><span style="text-decoration:underline">@cloudflare/voice</span></a> to make this easy. But there is a ton of software out there that uses gRPC for real-time client-server communication.</p><p>Using the APIs described above, you can now deploy gRPC servers to Cloudflare, written in any language, with full support for bidirectional streaming between client and server. This lets you take advantage of Cloudflare’s network of <a href="https://www.cloudflare.com/network/"><span style="text-decoration:underline">330+ locations</span></a> and handle requests much closer to clients than is possible elsewhere. We’re excited about the doors this opens up for low-latency voice and colocated inference.</p><p>For example, here’s a minimal gRPC server that echoes messages it receives back to the client:</p><!-- unsupported block: code --><p>With this, there’s pretty much no gRPC-based application that you can’t deploy to Cloudflare, no matter what language it’s in or dependencies it relies on. But what if you need to do something simpler, and just serve a basic gRPC server or connect from a Worker to a gRPC server running somewhere else?</p><h2>Workers as gRPC servers and clients with gRPC to gRPC-web conversion — no container needed</h2><p><a href="https://grpc.io/docs/platforms/web/basics/"><span style="text-decoration:underline">gRPC-web</span></a> is a browser-compatible version of gRPC. Web browsers don’t expose the lower-level HTTP/2 features that gRPC requires, and there is no raw TCP Socket API built into web browsers — this is why the WebSocket API exists, and why Workers have supported WebSockets <a href="https://blog.cloudflare.com/introducing-websockets-in-workers/"><span style="text-decoration:underline">since 2021</span></a>.</p><p>HTTP/2 splits each request and response into small binary messages called <a href="https://httpwg.org/specs/rfc7540.html#FramingLayer"><span style="text-decoration:underline">frames</span></a>. This is core to how a single HTTP/2 or HTTP/3 connection is able to multiplex — many requests can be interleaved over one connection. Each frame has a stream ID, allowing the receiver to reassemble it into the correct request or response. gRPC depends on this stream-level control for efficient streaming, cancellation, flow control, and <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Trailer"><span style="text-decoration:underline">trailers</span></a>.</p><p>Web platform APIs like <a href="https://developers.cloudflare.com/workers/runtime-apis/fetch/"><span style="text-decoration:underline">fetch()</span></a> don’t provide this control. So how can we make it simple and easy to use gRPC from Cloudflare Workers — without clients needing to make any changes? We translate incoming gRPC to gRPC-web, and translate outgoing gRPC-web to gRPC.</p><p>We’ve actually used gRPC-web within Cloudflare’s reverse proxy since 2020, when we wrote about the <a href="https://blog.cloudflare.com/road-to-grpc/#converting-to-http11"><span style="text-decoration:underline">Road to gRPC</span></a> on the Cloudflare blog. We convert requests to HTTP/1.1 so that messages can be inspected and gRPC apps can benefit from <a href="https://www.cloudflare.com/solutions/security/"><span style="text-decoration:underline">Cloudflare’s security features</span></a>, like WAF rules and Bot Management.</p><p>Now, in private beta and then rolling out to everyone, we’re extending this so that given a Protocol Buffer (protobuf) definition file like this:</p><!-- unsupported block: code --><p>You can write a unary gRPC server in a Worker in just a few lines of code, using the <a href="https://connectrpc.com/"><span style="text-decoration:underline">@connectrpc/connect</span></a> open-source package:</p><!-- unsupported block: code --><p>You can make outbound requests to external gRPC servers this way too, by using the client built into <a href="http://@connectrpc/connect"><span style="text-decoration:underline">@connectrpc/connect</span></a>:</p><!-- unsupported block: code --><p>Your code uses gRPC-web, but when it speaks to the outside world, it is automatically translated into gRPC. This means that clients and servers that you already depend on don’t need to change. For example, you can:</p><ul><li><strong>Provide gRPC backends to mobile apps that speak gRPC</strong> — Many mobile apps already use gRPC to reduce network payloads, serialize data more efficiently, and generate strongly-typed client libraries. You can now build the backend server for mobile apps on Workers, while still using established gRPC native libraries like <a href="https://www.swift.org/blog/grpc-swift-2/"><span style="text-decoration:underline">grpc-swift-2</span></a> and <a href="https://grpc.io/docs/languages/kotlin/quickstart/"><span style="text-decoration:underline">grpc-kotlin</span></a>.</li><li><strong>Put a Worker in front of an existing gRPC backend</strong> — So many developers already put Workers in front of existing REST APIs to move performance critical work closer to the user, or to incrementally move state into <a href="https://developers.cloudflare.com/durable-objects/"><span style="text-decoration:underline">Durable Objects</span></a>. Now you can do this with existing gRPC backends as well, or build new APIs and services that fetch data from your existing gRPC backend.</li></ul><h2>What’s next for Socket Workers and gRPC on Cloudflare</h2><p>We’re introducing everything from this post in private beta — you can sign up <a href="https://forms.gle/Q2SoJLUKjBFGxBgW6"><span style="text-decoration:underline">here</span></a>.</p><p>At Cloudflare, we use <a href="https://capnproto.org/"><span style="text-decoration:underline">Cap’n Proto</span></a> and <a href="https://blog.cloudflare.com/capnweb-javascript-rpc-library/"><span style="text-decoration:underline">Cap’n Web</span></a> and the <a href="https://blog.cloudflare.com/javascript-native-rpc/"><span style="text-decoration:underline">JavaScript-native RPC system that is built into Cloudflare Workers</span></a> instead of gRPC. And when we ship things, we always aim to be using them ourselves. So in this case, we want to first work closely with a smaller set of developers using gRPC, and make sure we’ve nailed it before turning this on for everyone.</p><p>More broadly, we’re excited to continue to push the bounds of what types of traffic the Workers platform can serve, going beyond TCP and into UDP-based protocols. Keep <a href="https://x.com/CloudflareDev"><span style="text-decoration:underline">telling us what you want to build</span></a> on Workers, and we’ll keep pushing the bounds of what is possible.</p>]]></content:encoded>
      <guid isPermaLink="false">01KZ21C4W4K7E7W8E61PNSYFQ1</guid>
    </item>
    <item>
      <title>Introducing the Billable Usage API: programmatic cost visibility for Cloudflare</title>
      <link>https://blog.cloudflare.com/billable-usage-api/</link>
      <pubDate>Mon, 03 Aug 2026 13:00:00 GMT</pubDate>
      <description>Cloudflare has launched a new Billable Usage API for accounts, giving developers and FinOps teams single-endpoint programmatic visibility into cost and usage across all self-serve products. Built around the FOCUS specification, track spend seamlessly alongside the rest of your cloud stack.</description>
      <dc:creator>Ryan Noel</dc:creator>
      <dc:creator>Zunayed Ali</dc:creator>
      <dc:creator>Filipa Nóbrega</dc:creator>
      <category>Agents Week</category>
      <category>API</category>
      <category>Billing</category>
      <category>Developers</category>
      <category>Engineering</category>
      <category>Product News</category>
      <content:encoded><![CDATA[<p>Agents Week is about the shift already underway: agents write code, deploy Workers, and provision infrastructure on your behalf. That shift changes what you need to see. If a program is spending money in your Cloudflare account, you need to know what it&#x27;s spending; throughout the day, per product, in a shape another program can consume. The dashboard is the right answer for humans. It&#x27;s not the right answer for automation.</p><p>So we&#x27;re launching a new <strong>Billable Usage API</strong> for self-serve accounts: a single endpoint that returns your account&#x27;s usage and cost, broken down by product and by service period. It covers every usage-based Cloudflare product on the account, including Workers, R2, D1, Workers AI, Vectorize, Images, and Stream, all with one call. And if you already work in a FinOps toolchain, the column names should look familiar.</p><!-- unsupported block: code --><!-- unsupported block: code --><p>You&#x27;ll get back an <code>HTTP 200 OK</code> with <code>Content-Type: application</code>/<code>json</code> and the usage rows in the response body. Today, usage and cost data are updated daily while we work towards providing more real time data. </p><h2>What comes back</h2><p>Each row in the response is one charge period for one product on your account.</p><ul><li><code>ServiceName</code> and <code>ServiceFamilyName</code> — which product (&quot;Workers Standard&quot; under the &quot;Workers&quot; family, &quot;R2 Storage&quot; under &quot;R2&quot;, etc.).</li><li><code>ChargePeriodStart</code> / <code>ChargePeriodEnd</code> — the window this row covers.</li><li><code>PricingQuantity</code> and <code>ConsumedUnit</code> — how much you used, in the unit of measure we bill on (GB-months, GB-seconds, requests, etc.).</li><li><code>ContractedCost</code> — what that period cost, in <code>BillingCurrency</code>.</li><li><code>CumulatedPricingQuantity</code> and <code>CumulatedContractedCost</code> — running totals for the billing period.</li><li><code>ZoneId</code> / <code>ZoneName</code> — when the usage is attributed to a specific zone.</li></ul><p>Most of these map directly to columns in the <a href="https://focus.finops.org/"><span style="text-decoration:underline">FinOps Open Cost and Usage Specification (FOCUS)</span></a>, so if your team is already ingesting FOCUS data from another provider, the names and semantics should be familiar:</p><!-- unsupported block: table --><p>Responses use the standard Cloudflare API envelope — <code>result</code> is an array of rows, one per product per charge period, alongside <code>success</code>, <code>errors</code>, and <code>messages</code>.</p><p></p><!-- unsupported block: code --><h2>Where we are on FOCUS</h2><p>Matching FOCUS naming was a deliberate choice. AWS, Azure, Google Cloud, Oracle, and a growing list of SaaS providers already publish FOCUS formatted exports, and every serious cost-management tool speaks to it. That said, we&#x27;re not yet claiming full conformance: a handful of columns the spec requires aren&#x27;t in the payload today. Getting there is on our roadmap. Consider this the first step: familiar shape now, full conformance next.</p><h2>Cloudflare spend, next to the rest of your cloud spend: our partnership with Vantage</h2><p>We&#x27;ve partnered with <a href="https://www.vantage.sh/"><span style="text-decoration:underline">Vantage</span></a> on a native Cloudflare integration. Vantage is an infrastructure cost management platform that ingests cost and usage data from more than 30 providers, across AI, Cloud and SaaS providers, and brings it into a single view for reporting, allocation, and optimization. With this integration, your usage flows into the same Cost Reports, Budgets, and Cost Alerts you already use for the rest of your infrastructure.</p><p>Vantage connects to Cloudflare using a read-only API token with Billing Read access. Once connected, Vantage pulls your Billable Usage data daily and breaks it down by product (such as Workers and R2), zone, and account, so you can see which products drive your spend and attribute it to the teams and services behind it.</p><p>A few of the workflows this integration supports:</p><ul><li><strong>Cross-provider allocation.</strong> Group Cloudflare spend by product, zone, and account, then use Virtual Tags to allocate by team or product line alongside your AWS, Azure, and other provider costs, all in a single report.</li><li><strong>Anomaly detection.</strong> Vantage Cost Alerts monitor every connected provider and notify you via Slack or E-Mail when spend deviates from its baseline, so a change in Workers or R2 spend surfaces the same way it does for any other provider.</li><li><strong>FinOps agents and MCP.</strong> Ask the in-console Vantage FinOps agent a question such as &quot;What was our biggest cost driver last week across every provider?&quot;, or query the same data from Claude or ChatGPT through Vantage&#x27;s hosted MCP server. Cloudflare spend is included alongside your other connected providers.</li></ul><p>Connect your Cloudflare account in the <a href="https://console.vantage.sh/"><span style="text-decoration:underline">Vantage console</span></a>, and your costs appear next to everything else you run. There are no manual exports, no invoice uploads, and no separate dashboard to maintain. </p><p>This FOCUS standardized API also works with other Fintech tooling.</p><h2>Why we built this</h2><p>Agents do more than write code. They deploy Workers, provision R2 buckets, and manage D1 databases. When you grant programmatic access to your Cloudflare account, you need programmatic visibility into what it&#x27;s costing you. Not at the end of the month, but throughout the day, by product, in a shape a program can actually consume.</p><p>The Billable Usage API is that shape. And customers have been asking us for programmatic usage for years. Finance teams want to pull spend into their own systems and attribute cost back to internal projects, teams, and even their end customers. Developers want a curl they can drop in a script. Every one of those workflows used to involve a screenshot or a manual export. Now it&#x27;s an HTTP call, or a configuration in Vantage.</p><h2><strong>What&#x27;s next</strong></h2><ul><li><strong>Finer-grained time windows.</strong> Today the API returns charge-period rows, which for most products is daily. We&#x27;re looking at more real time breakdowns for the products where it makes sense.</li><li><strong>Forecasting.</strong> <code>CumulatedContractedCost</code> tells you where your spend is in the current billing cycle. We want to help you predict where you&#x27;re going to end up. And not just at the account level, but the product level.</li><li><strong>Enterprise coverage.</strong> This first release is self-serve only. An equivalent experience for Enterprise contracts is in the works.</li></ul><h2>Try it</h2><p>The endpoint is live today for all self-serve accounts. Grab an API token with the <em>Billing Read</em> permission, point your curl at it, and you&#x27;ll get back your current billing period broken down by product. Full reference is available on the Cloudflare API docs. To see it alongside the rest of your cloud spend, connect your Cloudflare account in the <a href="https://console.vantage.sh/"><span style="text-decoration:underline">Vantage console</span></a>.</p><p>Cloudflare has spent years making it easy to run more of your stack on our network. It&#x27;s time we made it just as easy to see what that&#x27;s costing you — on Cloudflare, and everywhere else.</p>]]></content:encoded>
      <guid isPermaLink="false">01KZ1X08HVW808ZTAF030PTT5X</guid>
    </item>
    <item>
      <title>Smaller, faster, safer: running Kimi and GLM at scale</title>
      <link>https://blog.cloudflare.com/smaller-faster-safer-models/</link>
      <pubDate>Mon, 03 Aug 2026 13:00:00 GMT</pubDate>
      <description>Serving frontier models like Kimi and GLM means fighting for GPU memory. Here's how we quantize KV caches, compress model weights, and add integrity checks to serve them faster, cheaper, and safely.</description>
      <dc:creator>Alex Reneau</dc:creator>
      <dc:creator>Kevin Flansburg</dc:creator>
      <dc:creator>Chi McIsaac</dc:creator>
      <category>Agents Week</category>
      <category>AI</category>
      <category>Developers</category>
      <category>Inference</category>
      <category>Quantization</category>
      <category>Workers AI</category>
      <content:encoded><![CDATA[<p>Workers AI runs inference for some of the best open models in the world on GPUs in Cloudflare data centers close to your users. Two of the most capable, and most demanding, are Moonshot&#x27;s Kimi K-series and <a href="http://Z.ai">Z.ai</a>&#x27;s GLM. They are large, long-context, mixture-of-experts models, and they are wonderful to use. They are also very hard to serve efficiently because of memory constraints.</p><p>We&#x27;ve written before about how we <a href="https://blog.cloudflare.com/workers-ai-large-models/"><span style="text-decoration:underline">serve large models on Workers AI</span></a> and about separating <a href="https://blog.cloudflare.com/high-performance-llms/"><span style="text-decoration:underline">the prefill and decode phases</span></a> of inference to get more out of each GPU. This post looks at three techniques we layer on top of that to fit these models into memory and keep them fast: quantizing the KV cache, compressing the model weights, and, because both of those pack more requests onto shared hardware, protecting the cache those requests share. These optimizations enable us to support more customers at lower costs, with no change in model accuracy.</p><p>All our experiments and production traffic are running and benchmarked with <a href="https://github.com/sgl-project/sglang"><span style="text-decoration:underline">SGLang</span></a>, an open-source inference serving framework. We found that SGLang offers the best performance in the market, and we work closely with the SGLang team to upstream patches and new features to make our work available to the open-source community.</p><h2>Quantizing the KV cache</h2><p>As a model generates text, it stores the attention keys (K) and values (V) for every token it has already processed in a structure called the KV cache. The cache is what lets the model extend a long conversation without re-reading the entire context on every new token. For a long-context model, it grows quickly, and it is usually the KV cache, not the model&#x27;s weights, that fills up GPU memory first.</p><p>By default, the cache is stored in 16-bit precision (BF16). We store it in 8-bit floating point instead (FP8, e4m3), which halves its size. On Kimi K2.6, that raises the amount of context we can hold in memory from roughly 686,000 tokens to about 1.37 million, twice as much.</p><p>It&#x27;s worth being precise about where the benefit comes from, because it isn&#x27;t raw speed. Quantizing the cache adds a small amount of work per token, since the FP8 attention kernel has to convert values as it reads them. What it changes is how many requests we can keep resident at once. The following measurements are for Kimi K2.6 decoding on a disaggregated H200 deployment, comparing the attention kernels directly:</p><!-- unsupported block: table --><p>At any single concurrency level, BF16 is a few percent faster per token. But BF16 runs out of cache at 32 concurrent requests and can&#x27;t admit a 33rd, while FP8 keeps going to 64 and reaches 2,192 tokens per second, about 41% higher than BF16&#x27;s peak, for roughly 30% less cost per token. Because we run prefill and decode as separate pools, we can apply this where it helps most: prefill is compute-bound rather than memory-bound, so there we leave the cache in BF16 and keep its slightly higher throughput.</p><p>None of this would matter if it changed the model&#x27;s answers, so we checked. Across our evaluation suite, FP8 and BF16 caches are indistinguishable:</p><!-- unsupported block: table --><h2>Compressing the model weights</h2><p>The KV cache is one demand on GPU memory; the model&#x27;s weights are the other. For GLM 5.2, we compress the weights from 8-bit floating point down to 4-bit integers (INT4) with no loss in accuracy. The checkpoint shrinks from 705 GB to 421 GB, about 40%, and per-GPU memory across an 8-way tensor-parallel deployment drops from roughly 88 GB to 52 GB, which leaves room for around 1.18 million tokens of KV cache on the same hardware.</p><p>Across our evaluation suite, INT4 and FP8 weights are indistinguishable:</p><!-- unsupported block: table --><p>Smaller weights make the decode phase faster, and for a clear reason: generating each token means streaming the model&#x27;s weights out of GPU memory, so decode speed is limited by memory bandwidth. Move less data and every token arrives sooner. The effect is largest at low concurrency, where per-request latency matters most:</p><!-- unsupported block: table --><p>Prefill behaves differently. It is compute-bound, and INT4 weights have to be expanded back out before the model can multiply with them, so that extra step makes prefill slower rather than faster, GLM sustains about 10,160 tokens per second of prefill in FP8 versus 8,660 in INT4. As with the KV cache, the disaggregated design turns this into a choice rather than a compromise: we run INT4 for decode, where it wins, and FP8 for prefill, where it wins. Model accuracy stays within 0.8 points of the FP8 model across every benchmark we run, making its quality indistinguishable.</p><h2>Protecting a shared KV cache</h2><p>Both techniques above have the same effect: they let many more requests share one GPU&#x27;s memory at the same time. That efficiency is the whole point, but it also means hundreds of requests are reading and writing pages of the same physical KV cache. The mechanisms that make this fast, paged attention, continuous batching, cache reuse, all rely on getting the bookkeeping exactly right, and at our request volumes, even a one-in-a-billion mistake would show up regularly.</p><p>So we built KV cache integrity checking as a layer of defense. The idea is straightforward: every physical cache page gets a tag that changes whenever the page is reallocated, and the server records which pages and tags each request expects to use. Before supported decode operations read from the cache, those mappings are checked. If anything doesn&#x27;t match, the affected request is aborted rather than allowed to return data from the wrong page.</p><p>The question that decides whether a safety check ships is what it costs. We measured it on a mid-sized production model in a two-prefill, two-decode configuration, with 8,192-token inputs and 1,000-token outputs:</p><!-- unsupported block: table --><p>The cost is under 1% on both throughput and tail latency, and even the upper bound of the 95% confidence interval stays near 1%. We kept it computationally cheap by running the validation as a separate batch check rather than fusing it into the attention kernel, which would have introduced a race between GPU thread groups. It&#x27;s enabled per deployment, and the default path uses a no-op tracker with no measurable overhead, so deployments that don&#x27;t need it pay nothing.</p><h2>What&#x27;s next</h2><p>Serving frontier models efficiently is a moving target, and this is the ongoing work behind it. We&#x27;re expanding FP8 KV caches across more of the fleet, validating NVFP4 weights on Blackwell (NVIDIA’s GPU architecture), and working toward making integrity checks something we can leave on everywhere at negligible cost. These optimizations will allow us to continue to support more customers at a lower cost and at the same accuracy.</p><p>If squeezing the best open models onto GPUs and serving them to millions of developers sounds like your kind of problem, <a href="https://www.cloudflare.com/careers/"><span style="text-decoration:underline">come work with us</span></a>.</p>]]></content:encoded>
      <guid isPermaLink="false">01KZ1PQYERR7GDZ61F4H35QVT1</guid>
    </item>
    <item>
      <title>Workers RPC now works across Python and JavaScript</title>
      <link>https://blog.cloudflare.com/python-workers-rpc/</link>
      <pubDate>Mon, 03 Aug 2026 13:00:00 GMT</pubDate>
      <description>One coding agent can write a Python Worker and another can write a JavaScript Worker. At runtime, those Workers can exchange references to live objects and call their methods without defining APIs, schemas, or serialization code</description>
      <dc:creator>Dominik Picheta</dc:creator>
      <dc:creator>Gyeongjae Choi</dc:creator>
      <category>Agents Week</category>
      <category>Cloudflare Workers</category>
      <category>Developers</category>
      <category>Python Workers</category>
      <category>RPC</category>
      <content:encoded><![CDATA[<p>Two years ago, we introduced <a href="https://blog.cloudflare.com/javascript-native-rpc/"><span style="text-decoration:underline">Workers RPC</span></a>, built on <a href="https://capnproto.org/"><span style="text-decoration:underline">Cap’n Proto RPC</span></a>. This made it possible for Workers to call other Workers and Durable Objects’ methods, return live objects and call their methods, return functions, streams and get all the benefits of a Remote Procedure Call (RPC) system, without defining schemas or adding any dependencies. We called it “JavaScript-native RPC” because it made using RPC feel native to the language.</p><p>Last year, we made this work between web browsers and servers, and <a href="https://blog.cloudflare.com/capnweb-javascript-rpc-library/"><span style="text-decoration:underline">introduced Cap’n Web</span></a>.</p><p>Now we’re taking it cross-language.</p><p>Normally, getting programs written in different languages to talk to each other is complicated: developers usually have to build custom APIs or adopt language-agnostic serialization formats like <a href="https://protobuf.dev/"><span style="text-decoration:underline">protobuf</span></a>, so the two systems can understand each other. The RPC system built into Workers is able to translate across JavaScript and Python without any additional work.</p><p>You can now call methods defined in a Python Worker from a JavaScript Worker and vice versa. You can share objects across Python and JavaScript, and call methods on a Python object from TypeScript. It all just works.</p><p>If you define a method <code>add()</code> in a Worker written in TypeScript:</p><!-- unsupported block: code --><p>…you can simply call it from Python:</p><!-- unsupported block: code --><p>There are no dependencies needed. All you need to configure is a <a href="https://developers.cloudflare.com/workers/runtime-apis/bindings/service-bindings/"><span style="text-decoration:underline">Service binding</span>:</a></p><!-- unsupported block: code --><h2>So, what can you do with it?</h2><p>This RPC system allows you to build a complex multi-language system as if you are using a library. Here are some features of cross-language RPC.</p><ul><li>Cross-language RPC calls behave like ordinary function calls that return promises in JavaScript/TypeScript and futures in Python. Exceptions are propagated and are thrown at the call site of the RPC method.</li><li>You can pass any <a href="https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Structured_clone_algorithm#supported_types"><span style="text-decoration:underline">Structured Cloneable types</span></a> as the parameters or a return value of an RPC call. These get converted to the appropriate types in Python: for example, a JS Date is converted to a Python datetime</li><li>You can pass JavaScript functions to a Python Worker and return them, and vice versa. When the other side calls the function passed to it, they make a new RPC back for you.</li><li>Typically, RPC to another Worker does not cross a network. The other Worker usually runs in the same thread as the caller. There is near-zero performance overhead compared to running code in the same Worker.</li><li>The implementation is fully open source as part of <a href="https://github.com/cloudflare/workerd/"><span style="text-decoration:underline">workerd</span></a> and <a href="https://github.com/cloudflare/workers-py/blob/4bb01fe72a9af71918746e3851c526335302a3bc/packages/runtime-sdk/src/workers/rpc.py"><span style="text-decoration:underline">workers-runtime-sdk</span></a>.</li></ul><h3>But wait, how do you convert types across languages? </h3><p>The main hurdle for making RPC seamless across the JavaScript and Python Workers is bridging their distinct type systems. JavaScript developers expect to work with native JavaScript types, and Python developers expect the same for Python. Bridging two distinct languages with their own type systems required a careful, deliberate type conversion strategy.</p><p>Consider how each language handles function arguments. A typical way to define a complex function in JavaScript is passing an <code>Object</code> as an argument:</p><!-- unsupported block: code --><p>In contrast, a Python developer would typically define the same function using keyword arguments:</p><!-- unsupported block: code --><p>Our goal was to make cross-language RPC completely transparent. Developers should feel like they are writing code for a single-language application without needing to worry about the underlying translation layer. We achieved this by combining Pyodide’s Foreign Function Interface (FFI) with a custom type-conversion layer for Python Workers.</p><h3>Pyodide FFI already translates between Python and JavaScript types</h3><p>Pyodide is the CPython interpreter compiled to WebAssembly, and it has powered <a href="https://blog.cloudflare.com/python-workers/#pyodide-and-the-magic-of-foreign-function-interfaces-ffi"><span style="text-decoration:underline">Python Workers from the start</span></a>. It includes a robust FFI that automatically translates types between JavaScript and Python.</p><!-- unsupported block: image --><p>When a Python Worker communicates with a JavaScript Worker via Service bindings, Pyodide’s FFI transparently converts objects during the RPC call. Developers on either side don’t need to know which language the other Worker is written in, and everything is handled under the hood.</p><p>Pyodide maps native types between both environments out of the box:</p><!-- unsupported block: table --><p>When direct translation isn’t possible (such as with custom classes or functions), Pyodide creates a <code>Proxy</code> object. This proxy forwards attribute accesses and method calls across the boundary, enabling patterns like passing a Python function directly as a callback to JavaScript handlers.</p><p>Pyodide FFI also maps Python’s keyword arguments directly to JavaScript’s object-style parameters. For example, imagine a JavaScript Worker with a method that takes an optional options object:</p><!-- unsupported block: code --><p>When calling this JavaScript Worker from Python, you <em>could </em>pass a Python dictionary to represent the JavaScript object:</p><!-- unsupported block: code --><p>However, you can also use native Python keyword arguments:</p><!-- unsupported block: code --><p>Pyodide FFI translates both calls into the exact structure the JavaScript Worker expects, giving Python developers a clean, natural API experience.</p><p>To explore type translation in more detail, check out the <a href="https://pyodide.org/en/stable/usage/type-conversions.html"><span style="text-decoration:underline">Pyodide documentation</span></a>.</p><h3>Handling Cloudflare Workers objects</h3><p>While Pyodide FFI seamlessly converts standard built-in types, it doesn’t automatically understand Web API objects such as <code>Request</code>, <code>Response</code>, <code>Blob</code>, or <code>File</code>. They are commonly used in Cloudflare Workers, but there is no direct built-in equivalent in Python.</p><p>As explained in the previous section, Pyodide, by default, treats these non-standard objects as JavaScript Proxies. Rather than converting them into Python objects, it creates a passthrough proxy for attribute lookups and method calls. While functional, this approach leaks underlying JavaScript implementation details into Python. Python developers would have to constantly remember they are interacting with JavaScript proxies, adding unnecessary mental overhead.</p><p>To fix this, we introduced the <a href="https://github.com/cloudflare/workers-py/blob/4bb01fe72a9af71918746e3851c526335302a3bc/packages/runtime-sdk/src/workers/rpc.py"><span style="text-decoration:underline">workers-runtime-sdk Python package</span></a>. This acts as a thin conversion layer built specifically to handle custom Workers types over RPC. When you deploy a Python Worker using <a href="https://developers.cloudflare.com/workers/languages/python/#the-pywrangler-cli-tool"><span style="text-decoration:underline">uv run pywrangler deploy</span></a>, this package is included by default. In fact, if you import from the <code>workers</code> namespace, you’re already using it:</p><!-- unsupported block: code --><p>Behind the scenes, this SDK wraps the RPC stubs provided by the bindings. It intercepts objects crossing the language boundary and translates them into native forms that both JavaScript and Python Workers can work with naturally.</p><p>As a result, Python developers can work with familiar, idiomatic Python objects, making cross-language execution feel completely invisible.</p><h2>Use Python packages from your JavaScript Worker</h2><p>Have you ever wanted to use a great Python package, but your app is written in JavaScript? You can do this with Python Workers. Let’s look at an example.</p><p>Pygments is a popular syntax highlighting package, written in Python. To use it from JavaScript, you just need to expose a method from a Python Worker that calls the Pygments package.</p><p></p><p>We can call this method in our JavaScript by accessing the request’s <code>env</code>:</p><!-- unsupported block: code --><p>Now on the Python side, we define a Python Worker with this method like so:</p><!-- unsupported block: code --><p>Now all that’s left is to write the necessary code to do the highlighting in Python. A simplified version of this looks like so:</p><!-- unsupported block: code --><p>The JavaScript lives in its own Worker that is separate from the Python Worker. So you also need to define the Service bindings to ensure they can communicate. You can do so by putting this in the JavaScript Worker’s wrangler.jsonc file:</p><!-- unsupported block: code --><p>The name of the service needs to match the name of your Python Worker here.</p><p>To test these, you can run <code>npx wrangler dev</code> in the JavaScript Worker’s directory and <code>uv run pywrangler dev</code> in the Python Worker’s directory in two separate terminals.</p><p>A full example is <a href="https://github.com/cloudflare/python-workers-examples/tree/main/13-js-api-pygments"><span style="text-decoration:underline">available on GitHub</span></a>. You can run it directly by using the following commands:</p><!-- unsupported block: code --><h2>Try it now</h2><p>In addition to those above, there are far more examples and information about RPC in <a href="https://developers.cloudflare.com/workers/runtime-apis/rpc/"><span style="text-decoration:underline">our documentation</span></a>.</p>]]></content:encoded>
      <guid isPermaLink="false">01KYT15YJPSYBNRFS8G400P5P5</guid>
    </item>
    <item>
      <title>Welcome to Agents Week</title>
      <link>https://blog.cloudflare.com/agents-week-welcome/</link>
      <pubDate>Sun, 02 Aug 2026 16:00:00 GMT</pubDate>
      <description>Agents Week explores how cloud infrastructure must evolve to serve autonomous agents rather than human browsers. Join us as we unpack the storage, execution, and security primitives needed for an agent-native web.</description>
      <dc:creator>Rita Kozlov</dc:creator>
      <category>Agents</category>
      <category>Agents Week</category>
      <category>AI</category>
      <category>Cloudflare Workers</category>
      <category>Developer Platform</category>
      <content:encoded><![CDATA[<p>This week is Agents Week.</p><p>As we started thinking about and planning the week, we wrestled with a broader question of what it means to support this new era of agents and what a purpose-built foundation for agents actually looks like.  Which brought us to a simpler framing: what is an Agent Cloud? </p><p>We quickly realized however, that our framing was wrong. Not because it’s the wrong question to ask, but because of who we were asking — ourselves, instead of our agents. It’s no longer about us and what we think, but about what agents need. </p><p>That, in a nutshell, is what Agents Week is about. </p><p>The cloud we have today, and the web it sits on, were built for people. Every layer assumes a human is watching: pages designed to hold your attention, dashboards to click through, interfaces tuned for how we read and decide. But agents don&#x27;t work that way. They don&#x27;t get distracted, tired or fatigued…  and they have their own needs around speed, structure, and access.</p><p></p><!-- unsupported block: image --><p>An Agent Cloud has to do two things at once. It has to set us up for an agent-native future, where the primitives are built for agents from the ground up rather than retrofitted from human tools. And realistically, it has to meet us where we are today, acting as a translation layer between the human-shaped web that exists now and the agent-shaped one we&#x27;re moving toward.</p><p>That&#x27;s the throughline for the next five days: the shape of a cloud built for agents and humans and how they interact. The week will explore the theme through what that means for the primitives and execution layer you need, the updated agentic software development lifecycle, how organizations can securely enable employees and agents to interact with safe controls, how this shapes the agentic web, and finally, grounding all of it in the reality of agents and humans today.</p><p>Going back to the question: what does your agent need from an Agent Cloud? Well, rather than copy and pasting responses we got from our agents, we encourage you to ask your own agent that, and share any interesting insights and responses you get. Here’s an example prompt for you to use, but we encourage you to explore answers of your own:</p><p><em>What do you, as an agent, need from an agent cloud? Imagine things across the categories of a storage &amp; compute cloud and the execution and storage primitives you need, your dev lifecycle (adlc - like sdlc but with humans taken out of the loop), secure access to systems of record within an organization to get deep work done, and the web (discovery, access, payments...).</em></p><p>Let us know what your agent says by replying here, we’d love to see the responses! </p><p><a href="https://blog.cloudflare.com/"><span style="text-decoration:underline">Follow along on the blog this week</span></a> for the latest innovations around Agents, and <a href="https://x.com/CloudflareDev"><span style="text-decoration:underline">reach out on X</span></a> to join in the conversation.</p><!-- unsupported block: image --><p></p>]]></content:encoded>
      <guid isPermaLink="false">01KYZDDR9B6VG028YHK8T709TD</guid>
    </item>
    <item>
      <title>An API for MoQ: provision your own isolated relays</title>
      <link>https://blog.cloudflare.com/moq-relays/</link>
      <pubDate>Fri, 31 Jul 2026 13:00:00 GMT</pubDate>
      <description>Last year we made every Cloudflare server a Media over QUIC (MoQ) relay. Now the new provisioning API lets you create your own isolated relay and control who can publish and who can only watch.</description>
      <dc:creator>Jacob Curtis</dc:creator>
      <dc:creator>Manish Pandit</dc:creator>
      <dc:creator>Mike English</dc:creator>
      <category>API</category>
      <category>Developers</category>
      <category>IETF Standards</category>
      <category>Live Streaming</category>
      <category>Product News</category>
      <category>QUIC</category>
      <category>Video</category>
      <category>WebRTC</category>
      <content:encoded><![CDATA[<p>Last year, <a href="https://blog.cloudflare.com/moq/"><span style="text-decoration:underline">we enabled Media over QUIC</span></a> (MoQ) on every Cloudflare server and opened the network for anyone to test. It provided a global MoQ endpoint, but not the isolation and access controls needed to run an application.</p><p>Today, we’re adding those isolation and access controls. The new MoQ provisioning API lets you create an isolated relay for your application and issue separate credentials for publishers and subscribers. The relays you create are available across Cloudflare’s network within seconds, with no servers to deploy, size, or load balance.<br/>Cloudflare now supports the <a href="https://datatracker.ietf.org/doc/draft-ietf-moq-transport/14/"><span style="text-decoration:underline">draft-14</span></a> and <a href="https://www.ietf.org/archive/id/draft-ietf-moq-transport-16.html"><span style="text-decoration:underline">draft-16</span></a> versions of the MoQ Transport protocol with authentication support.</p><p>You can create relays through <a href="https://developers.cloudflare.com/api/resources/moq"><span style="text-decoration:underline">the API</span></a> and the <a href="https://dash.cloudflare.com/"><span style="text-decoration:underline">Cloudflare dashboard</span></a>. They are completely free to use during beta.</p><h2>A QUIC recap on MoQ</h2><p>MoQ (originally short for Media over QUIC) is a new open protocol under development at the Internet Engineering Task Force (IETF), the standards body that also standardized HTTP, TLS, and QUIC. It is being developed in the open and will become a free public standard (an RFC) that anyone can implement. No single company owns it.</p><p>MoQ is a publish/subscribe system. A publisher sends out streams of data that have names, and subscribers ask for those streams by name. Between them sit relays, which are just CDN servers that copy each stream to everyone who wants it. A relay never has to look inside the data it forwards, so one publisher can reach a large audience without handling the fan-out itself.</p><p>Because relays don&#x27;t care what&#x27;s in the data, the same protocol can carry many things that each used to need a separate system: live video, video calls, low-latency messaging, and more. It runs on QUIC, the transport under HTTP/3, which is what keeps latency low.</p><p>The practical result is that you don&#x27;t have to build and run your own fleet of specialized servers. You publish to a CDN through one simple API and get both low latency and large scale for much less cost.</p><h2>How we got here: the MoQ open preview</h2><p>Last year, we launched <a href="https://blog.cloudflare.com/moq/"><span style="text-decoration:underline">the first global MoQ relay network</span></a>: every Cloudflare server in over 330 cities became a MoQ relay, free and open to anyone. Because these endpoints required no authentication, they were ideal for protocol testing and client development. More than 1,000 unique clients still connect each day to test against them.</p><p>But an unauthenticated relay isn&#x27;t suitable for production, because you can&#x27;t control who publishes and who subscribes. That rules out any application that needs confidentiality, access control, or a clear split between publisher and subscriber roles. Take a live auction site, where bids have to reach bidders in milliseconds. MoQ is a good fit, but publishers and subscribers need different permissions, so that a viewer&#x27;s credentials can&#x27;t be used to hijack the publisher&#x27;s tracks.</p><h3>What is a relay on the Cloudflare MoQ Network?</h3><p>In most MoQ deployments today, a relay is a dedicated server or a dedicated process on a shared server. Scaling this architecture means running more instances, assigning clients to them, and adding load balancers as demand changes. This is not how any Cloudflare service works, including our <a href="https://blog.cloudflare.com/cloudflare-calls-anycast-webrtc/#eliminating-sfu-region-selection"><span style="text-decoration:underline">Realtime SFU WebRTC service</span></a>.</p><p>Provisioning a relay doesn’t start a virtual machine, container, or dedicated process. Instead, it creates an isolated scope across the existing global network.</p><p>That scope separates your namespaces, tracks, and objects from those belonging to other relays. It also defines who can enter the scope and whether they can publish or subscribe. Clients connect to the <a href="https://www.cloudflare.com/learning/cdn/glossary/anycast-network/"><span style="text-decoration:underline">Anycast</span></a> endpoint, and Cloudflare handles routing them across the network.</p><p>If you’re familiar with web hosting, creating a Cloudflare relay is more like adding a virtual host than starting a new web server. Since the infrastructure is already running, the provisioning API adds your application’s configuration and credentials. This makes the relay available immediately without choosing regions, estimating capacity, or setting up a load balancer.</p><!-- unsupported block: image --><h2>The control plane API for MoQ at Cloudflare</h2><p>The provisioning API is a control plane: it manages relays and the tokens used to reach them, and it never touches the media that flows through them.</p><p>There are two kinds of resources.</p><ul><li>A <strong>relay </strong>is the isolated scope from the previous section, so one application&#x27;s streams never mix with another&#x27;s. </li><li>A<strong> token</strong> is a credential that grants a set of operations (publish, subscribe, or both) on a single relay. Handing publishers and subscribers different tokens is what stops a viewer from taking over a broadcaster&#x27;s tracks.</li></ul><p>Each token is scoped to the operations a client needs, can be given an expiration, and can be revoked on its own. That lets you grant exactly the access a client should have, and take it back later without disrupting anyone else.</p><p>For now, each token applies to an entire relay and permits publishing, subscribing, or both. We&#x27;re working in the IETF and the wider MoQ community on a richer scheme that works for everyone. If you have opinions, tell us at <a href="mailto:moq@cloudflare.com"><span style="text-decoration:underline">moq@cloudflare.com</span></a>.</p><h3>Provision a relay </h3><p>You can provision a relay two ways: with the <a href="https://developers.cloudflare.com/api/resources/moq"><span style="text-decoration:underline">HTTP API</span></a> and in the <a href="https://dash.cloudflare.com/"><span style="text-decoration:underline">Cloudflare dashboard</span></a>. </p><h3>With the API</h3><p>Creating a relay takes a single API call and only needs a name:</p><!-- unsupported block: code --><p>Cloudflare returns a relay ID and the two default tokens:<br/>The first token can publish and subscribe, and the second can only subscribe.</p><!-- unsupported block: code --><p>To give a client narrower access, add more tokens. This one is a subscribe-only token for viewers that expires at the start of 2027:<br/></p><h3>In the Cloudflare dashboard</h3><!-- unsupported block: code --><p>You can also create a relay in the dashboard:</p><!-- unsupported block: image --><p>Go to <strong>Media </strong> &gt; <strong>Realtime </strong>&gt; <strong>MoQ Relay. </strong>Select <strong>Create relay</strong>, give it a name, and then confirm. </p><h3>Connect a publisher and a subscriber</h3><p>You can create and manage tokens through the API or dashboard, just as you can the relay itself. Give your broadcaster the publish-and-subscribe token and your viewers the subscribe-only token. Each client sends its token when it opens a MoQ session, and the relay enforces what that token is allowed to do.</p><p>The token travels in the URL path. For example, with the open-source <a href="https://github.com/cloudflare/moq-rs"><span style="text-decoration:underline">moq-rs</span></a> tools, a broadcaster can publish a fragmented MP4 stream from ffmpeg:</p><!-- unsupported block: code --><p>A viewer connects with moq-sub:</p><!-- unsupported block: code --><p>The relay reads the token when the session opens and checks whether the requested operation is allowed.</p><h2>What we changed to support draft-16 </h2><p>The provisioning API is only one part of what’s new. The MoQ transport itself is advancing fast, and Cloudflare now supports <a href="https://datatracker.ietf.org/doc/html/draft-ietf-moq-transport-16"><span style="text-decoration:underline">draft-16 of the IETF MoQ spec</span></a> in its relays. This draft adds two features relevant to publishing and subscribing.</p><p><code>PUBLISH</code> now lets a publisher send a track to a relay before a viewer requests it. Without <code>PUBLISH</code>, the first subscription must travel through the relay chain to the publisher before the publisher starts sending. With <code>PUBLISH</code>, the relay can already be receiving the track when the first viewer connects.</p><p><code>SUBSCRIBE_NAMESPACE</code> lets a subscriber request every track announced under a namespace instead of requesting tracks individually. The subscription also covers tracks added later, such as a new video rendition or audio track introduced during a live stream.</p><p>You can now connect a draft-16 client to use both features. </p><h2>Built in the open</h2><p>MoQ is an open standard, developed at the IETF by engineers across the industry. This lets clients and relays implement a common protocol. That interoperability is less useful if every relay provider requires a different control plane for creating scopes and issuing credentials.</p><p>In that vein, we’re documenting the design behind this API in the <a href="https://datatracker.ietf.org/doc/draft-englishm-moq-cdn-provisioning/"><span style="text-decoration:underline">MoQ CDN Provisioning Internet-Draft</span></a>. The draft calls the provisioned resource a scope rather than a relay, but both terms refer to the same logical delivery context: a boundary that applications create and then enter with a credential. </p><p>The goal is for multiple CDN and relay implementations to support a common provisioning model. The document is still an Internet-Draft, not an RFC, and its API model may change as the working group develops it. </p><h2>Available today, still free in beta</h2><p>The MoQ relay provisioning API is available now, as part of the MoQ beta. It&#x27;s free to use at any scale during this preview period. </p><p>The API will change as we develop it, so we recommend checking the <a href="https://developers.cloudflare.com/moq"><span style="text-decoration:underline">developer docs </span></a>for updates and breaking changes. </p><p>We’d also love to hear what you want next. Finer-grained permissions? Bring-your-own signing keys? Let us know at <a href="mailto:moq@cloudflare.com"><span style="text-decoration:underline">moq@cloudflare.com</span></a>.</p><h2>Get started today</h2><ul><li>Read the <a href="https://developers.cloudflare.com/moq"><span style="text-decoration:underline">developer documentation</span> </a></li><li>Read the <a href="https://developers.cloudflare.com/api/resources/moq"><span style="text-decoration:underline">API reference docs</span></a>. </li><li>Read the <a href="https://datatracker.ietf.org/doc/draft-englishm-moq-cdn-provisioning/"><span style="text-decoration:underline">provisioning draft</span></a>. </li><li>Email us: <a href="mailto:moq@cloudflare.com"><span style="text-decoration:underline">moq@cloudflare.com</span></a> </li><li>Building with MoQ? Join the <a href="https://discord.cloudflare.com/"><span style="text-decoration:underline">Cloudflare Developers Discord</span></a> to share your projects and tell us what the tech preview needs.</li></ul><p></p>]]></content:encoded>
      <guid isPermaLink="false">01KYVQDQC4RNEJ8Q0PMDQV2RGB</guid>
    </item>
    <item>
      <title>Dogfooding at scale: migrating cdnjs to Cloudflare’s Developer Platform</title>
      <link>https://blog.cloudflare.com/cdnjs-dev-platform-migration/</link>
      <pubDate>Thu, 30 Jul 2026 13:00:00 GMT</pubDate>
      <description>We moved cdnjs, serving 9 billion requests a day, entirely onto Cloudflare's Developer Platform. That means we’re running one of the Internet's busiest open-source CDNs on our own building blocks, and we pushed Workflows and Workers limits higher for everyone.</description>
      <dc:creator>Simona Badoiu</dc:creator>
      <category>Cache</category>
      <category>CDNJS</category>
      <category>Cloudflare Queues</category>
      <category>Cloudflare Workers</category>
      <category>Containers</category>
      <category>Developer Platform</category>
      <category>Developers</category>
      <category>Durable Objects</category>
      <category>Open Source</category>
      <category>R2</category>
      <content:encoded><![CDATA[<p>As of June 23, 2026, cdnjs, one of the Internet&#x27;s busiest open-source CDNs, is running exclusively on <a href="https://www.cloudflare.com/en-gb/developer-platform/"><span style="text-decoration:underline">Cloudflare’s Developer Platform</span></a>. Along the way, cdnjs surfaced limits in the platform, and the platform grew to meet them.</p><p><a href="https://cdnjs.com/"><span style="text-decoration:underline">cdnjs</span></a> is a free, open-source content delivery network for JavaScript and CSS libraries. Instead of using a bundler or self-hosting jQuery, Bootstrap, or Lodash, you drop a &lt;script&gt; tag pointing to <a href="http://cdnjs.cloudflare.com">cdnjs.cloudflare.com</a> and the library loads from Cloudflare&#x27;s edge, instantly, anywhere in the world, with no signup, no API keys, and no rate limits. It&#x27;s the infrastructure behind a significant portion of “intro to JavaScript” tutorials, CodePen demos, and Stack Overflow answers.</p><p>Community-driven, cdnjs is used on <a href="https://w3techs.com/technologies/details/cd-cdnjs"><span style="text-decoration:underline">roughly 12% of all websites</span></a>, a 48.3% share of the JavaScript CDN market. It serves an average of 108,000 requests per second, 9 billion per day, across more than <a href="https://www.cloudflare.com/en-gb/network/"><span style="text-decoration:underline">330 Cloudflare data centers</span></a>, with a 98.6% cache hit rate. Pretty cool, Internet!</p><p>In 2011, when bundlers were exotic, npm was barely a year old, and &quot;just drop a <code>&lt;script&gt;</code> tag&quot; was how the web shipped JavaScript, Ryan Kirkman and Thomas Davis built cdnjs as a free, community-run mirror of every popular open-source library. </p><p>Cloudflare stepped in to host it free of charge <a href="https://blog.cloudflare.com/cdnjs-community-moderated-javascript-librarie/"><span style="text-decoration:underline">months later</span></a>, and took over project maintenance <a href="https://blog.cloudflare.com/an-update-on-cdnjs"><span style="text-decoration:underline">in 2019</span></a>. Back then, Cloudflare didn&#x27;t have a mature Developer Platform that could fully sustain the entire cdnjs ecosystem. Fifteen years and a lot of building blocks later, the platform is mature enough to run cdnjs end to end, on Workers, Workflows, D1, Queues, Workers Cache, R2, KV, and Containers.</p><h3>Why cdnjs has 9 billion requests a day </h3><p>The web has changed beyond recognition from those days. We have ES Modules (ESM), the standardized import / export syntax browsers understand natively. We have import maps, Vite, Bun, Turbopack. We have AI assistants that scaffold entire apps in seconds. Bundlers are everywhere. So why does a CDN for <code>&lt;script&gt;</code> tags still serve 9 billion requests a day?</p><p>One reason: LLMs love cdnjs. When ChatGPT, Claude, or Cursor scaffold a quick HTML demo, they reach for cdnjs because their training data is full of it. There have been 15 years of blog posts, GitHub READMEs, tutorial sites, and Q&amp;A threads pointing to <a href="http://cdnjs.cloudflare.com">cdnjs.cloudflare.com</a>. The URL pattern is consistent and versions are immutable — exactly the kind of dependency a model can produce reliably without hallucinating.</p><p>Every file on cdnjs has an SRI hash (<em>we&#x27;re still working on ensuring all the existing stored hashes match reality due to bugs in the old system</em>), mirrors are auditable, and the whole project is open source. In a world increasingly worried about supply-chain attacks, an immutable, hash-verified mirror of well-known libraries is indispensable.</p><p><em>And it&#x27;s free, forever, for everyone. No API keys. No rate limits. No &quot;sign up to continue.&quot; That&#x27;s a rare thing on today&#x27;s Internet, and it&#x27;s worth protecting.</em></p><h3>Why we migrated</h3><p>We didn&#x27;t migrate because cdnjs was slow. We migrated because we want to keep improving it.</p><p>The previous architecture served users well: 98% cache hit, billions of requests, no outages. But internally, shipping anything new or fixing existing issues in how packages were processed was getting harder. Making a change meant coordinating deployments across GCP Functions, a VM, and Cloudflare. Observability was painful too.</p><h3>The pain points  </h3><p>In 2020, <a href="https://blog.cloudflare.com/migrating-cdnjs-to-serverless-with-workers-kv/"><span style="text-decoration:underline">we migrated cdnjs to serverless</span></a>, moving file serving onto Cloudflare Workers and KV, with a bare-metal origin as fallback. That change dramatically improved resilience and scalability, and let us pre-compress every asset with Brotli and gzip for smaller, faster responses, but only on the serving side.</p><p>The publishing side — the pipeline that watches npm and GitHub for new library versions, downloads them, processes them, and writes the results so cdnjs can serve them — stayed on Google Cloud Platform (GCP). At the time, Cloudflare Workers were designed for fast, short-lived HTTP requests; they didn&#x27;t yet have the building blocks for a long-running, multi-step pipeline that fetches large tarballs, runs CPU-heavy compression, and orchestrates work over hours. Workflows, Queues, Durable Objects, R2, and Containers didn&#x27;t exist yet.</p><p>So we built the publishing bot on what was available: a chain of GCP Functions, a VM running git-sync, and a <a href="https://github.com/cdnjs/cdnjs"><span style="text-decoration:underline">GitHub repository as the source of truth</span></a>. It worked, but six years later, that architecture was showing its age. Here&#x27;s a diagram of the previous architecture:</p><!-- unsupported block: image --><p> </p><p>The architecture had five pain points. The one that hurt most was observability: debugging meant stitching logs together by hand. We&#x27;ll start there.</p><ol><li><strong>No shared trace<br/></strong>A single package update could pass through Cloud Functions, Google Cloud Storage (GCS) object events, Pub/Sub topics, a git-sync VM, and Workers KV before a file reached a user. None of those systems shared a correlation ID. GCP Logging held one half of the story, Cloudflare Logpush held the other, and the two had no common key to join on.<br/><br/>The problem wasn&#x27;t outright failure, it was partial success. A version that processed cleanly, wrote to KV, and then silently failed to land in the GitHub repo would serve fine for weeks until someone noticed the two stores had diverged. There was no alert for that. There couldn&#x27;t be, because nothing in the system knew the full pipeline state.<br/></li><li><strong>Split-brain storage<br/></strong>Files lived in two places at once: Workers KV at the edge (with a bare-metal origin as fallback) and a GitHub repository as the source of truth. The ingestion pipeline wrote to both at the end of every run. Neither was authoritative, and when they drifted, there was no clean way to reconcile them.<br/></li><li><strong>Pipeline glued together with object events<br/></strong>The ingestion pipeline was a chain of small GCP Cloud Functions, each doing one step and handing off to the next through shared storage. One function fetched the package&#x27;s release archive from npm and dropped it in a bucket. The bucket firing a &quot;new file&quot; event triggered the next function, which unpacked it and wrote the results somewhere else, triggering the next, and so on. Storage was doing double duty as a message queue, with no dead-letter queue, no backlog visibility, and no clean replay when a step failed.<br/></li><li><strong>26 functions for 26 letters<br/></strong>Just checking npm for updates required 26 Cloud Functions, one per letter of the alphabet. Each shard had its own deployment and its own logs, and the only way to know if the fleet was healthy was to check all 26.</li><li><strong>The GitHub repo GitHub couldn&#x27;t serve<br/></strong>A separate VM ran git-sync, mirroring every processed file into cdnjs/cdnjs. Years of releases pushed it past 1.1TB of packed storage, large enough that GitHub&#x27;s own archive service refused to generate tarballs or zip downloads for it. Forking became impractical, clones were slow, and the .gitignore had grown to 274 hand-curated entries blocking broken or weirdly-versioned releases. It was a documented graveyard of everything the pipeline couldn&#x27;t reasonably reject upstream.<br/><br/>A genuine thank you to the GitHub team for hosting this giant for over a decade. They bore with us through years of storage growth, and the project wouldn&#x27;t have survived without them.<br/><br/>A quieter benefit that came with the migration is having fewer moving parts to secure. Cloud Functions, a git-sync VM, container images, GCS buckets, service-account keys — every one of those was a thing to secure, patch, and audit. Retiring the pipeline closed all of the recently opened cdnjs vulnerabilities.</li></ol><h3>How we re-built it</h3><p>The new cdnjs architecture runs entirely on Cloudflare’s Developer Platform.</p><p><a href="https://developers.cloudflare.com/r2/"><span style="text-decoration:underline">R2</span></a> is the single source of truth for file content. It has no practical size limit, so the files that couldn&#x27;t fit in KV before, like source maps, big bundles, and font packs, now live alongside everything else. As a bonus, the S3 API makes the entire cdnjs catalog accessible to any S3 client. <em>Maintain a mirror? Open an issue on the <a href="https://github.com/cdnjs/cdnjs"><span style="text-decoration:underline">cdnjs repository</span></a> and we&#x27;ll set you up with read-only credentials.</em></p><p><a href="https://developers.cloudflare.com/kv"><span style="text-decoration:underline">KV</span></a> stores only metadata now: package info, version lists, SRI hashes. KV is built for high read volume with infrequent writes, which is exactly the shape of metadata access.</p><p>In front of the Worker sits <a href="https://blog.cloudflare.com/workers-cache/"><span style="text-decoration:underline">Workers Cache</span></a>, a tiered cache Cloudflare launched this year. Before, we relied on a separate internal caching layer between the edge and the Worker. That layer is gone now, replaced by one owned by the Developer Platform, the same platform that runs the rest of cdnjs. One less moving part!</p><p>The new architecture also extends a long-standing partnership. <a href="https://www.digitalocean.com/"><span style="text-decoration:underline">DigitalOcean</span></a> has hosted the cdnjs website for years as a sponsor; now it hosts the storage too. Every file published to R2 is mirrored to DigitalOcean Spaces: architecturally a disaster-recovery copy, operationally also a live fallback. The serving worker reads through to it whenever R2 can’t return a file. The chain is cache → R2 → DigitalOcean, so R2 having a bad day doesn&#x27;t take cdnjs down. <em>A Cloudflare-hosted origin still sits in the chain during the transition, but it will retire once the GitHub backfill lands in R2.</em></p><p>The ingestion pipeline is built on <a href="https://developers.cloudflare.com/workflows/"><span style="text-decoration:underline">Cloudflare Workflows</span></a>. Every ten minutes, a cron job triggers PackageUpdatesWorkflow, which checks npm and GitHub for new versions. For each new version found, it spawns a DownloadPackageWorkflow that fetches the tarball into R2, then a ProcessingWorkflow per file that extracts, minifies, and compresses. Finally, PublishingWorkflow writes the results to R2 and KV and updates the <a href="https://www.algolia.com/"><span style="text-decoration:underline">Algolia</span></a> search index.</p><p>Because Workflows provides durable execution, the state of each step is preserved. If anything fails — a network timeout, a compression error — the workflow resumes from the last successful step.</p><p>The trickier piece is how we glue Workflows to the <a href="https://developers.cloudflare.com/containers/"><span style="text-decoration:underline">external compression container</span></a>. We pre-compress text-based files to streamline the delivery process. Because our library processing algorithms require us to buffer the entire library (which might be very large) in memory we opted to use a container for the initial migration. We are now looking into making these algorithms support streaming to move this logic to the worker.</p><p>The pipeline has two kinds of waiting:</p><p><em>Per file: </em>Each ProcessingWorkflow writes the uncompressed file to an R2 bucket, sends a job to a Queue, and hibernates. A Rust compression service running in the container picks it up, compresses it, and writes the result to another bucket. An R2 event notification wakes the workflow up so it can continue.</p><p><em>Per package: </em>The parent workflow needs to wait for all its file children before moving on to publish. A package with thousands of files means thousands of children running in parallel. We use a small Durable Object as a counter: a parent increments on each child it spawns, children decrement when they finish. The parent wakes up when the counter reaches zero.</p><p>An overview of the new architecture, with R2 as the source of truth and Workflows running the pipeline:</p><!-- unsupported block: image --><h3>Pushing the limits</h3><p>Designing the new architecture was one challenge. Migrating the existing catalog into it, without disturbing a single file already in the wild, was another.</p><p>We&#x27;d actually tried this once before and had to roll back. The plan, back then, was to re-process old packages and write the results directly to R2, but the regenerated files didn&#x27;t byte-match what KV had been serving. Minifiers and compressors aren&#x27;t fully deterministic across versions, so the new outputs were correct but had different SRI hashes. For a CDN where users pin those hashes in their HTML, that&#x27;s a serving break. So we rolled back, and now, we migrated the existing content from KV to R2 as-is instead of regenerating it.</p><p>That decision shifted the problem from &quot;re-process millions of files&quot; to &quot;copy millions of files between accounts, without missing any.&quot; And that&#x27;s where we ran into the Workers subrequest limit, capped at 1,000 per invocation on paid plans. A package with thousands of files would burn through it in one go. Parallelizing didn&#x27;t help, since every Worker hits the same ceiling. So we sharded the migration by package name and fanned the work out across many invocations via <a href="https://developers.cloudflare.com/queues"><span style="text-decoration:underline">Queues</span></a>, whose at-least-once delivery guarantee meant no package could silently fall out of the migration.</p><p>We hit two platform limits during the migration: 1,000 subrequests per Worker invocation and 1,024 steps per Workflow. Instead of just working around them, we asked the Workers and Workflows teams to raise them — which they did. Subrequests now go up to 10 million on paid plans; Workflows now default to <a href="https://developers.cloudflare.com/changelog/post/2026-03-03-step-limits-to-25k/"><span style="text-decoration:underline">10,000 steps, configurable to 25,000</span></a>.</p><p>The cdnjs pipeline runs on the same building blocks anyone can use: Workers, Workflows, R2, KV, Queues, Containers, and Durable Objects. The limits we hit are limits we lifted for everyone. If the Cloudflare Developer Platform can serve 9 billion requests a day and publish packages with hundreds of thousands of compressed, minified files, it can probably run whatever you&#x27;re building.</p><h3>What’s next</h3><p>There&#x27;s an obvious next question hiding in all of this: could cdnjs also serve modern, browser-native ES modules? The same packages, transformed on publish, ready to import without a bundler. The architecture doesn&#x27;t rule it out. The Workflows-plus-Containers pattern that pre-compresses files today would work just as well for transforming them. We&#x27;re not committing to it, but it&#x27;s the kind of thing that&#x27;s now possible to consider, which wasn&#x27;t true a year ago.</p><p></p><p><code>git commit -m &quot;with love&quot; --author=&quot;cdnjs team&quot;</code></p><p></p><p><em>We follow every open issue on <a href="https://github.com/cdnjs"><span style="text-decoration:underline">GitHub</span></a> and we want your feedback. Don&#x27;t hesitate to contribute and help make the Internet better for everyone.</em></p>]]></content:encoded>
      <guid isPermaLink="false">01KYQNRTVJ54SHCVBMMXFYGJ5P</guid>
    </item>
    <item>
      <title>Post-quantum authentication to origins is now supported</title>
      <link>https://blog.cloudflare.com/post-quantum-authentication-to-origins/</link>
      <pubDate>Wed, 29 Jul 2026 13:00:00 GMT</pubDate>
      <description>Cloudflare now supports post-quantum (PQ) authentication when connecting to customer origin servers via Authenticated Origin Pulls and Custom Origin Trust Store. This is the first step towards providing PQ authentication for all Cloudflare products.</description>
      <dc:creator>Luke Valenta</dc:creator>
      <dc:creator>Kevin Guthrie</dc:creator>
      <category>Cryptography</category>
      <category>Cybersecurity</category>
      <category>Post-Quantum</category>
      <category>Research</category>
      <category>SSL</category>
      <category>TLS</category>
      <content:encoded><![CDATA[<p>Cloudflare&#x27;s Authenticated Origin Pulls and Custom Origin Trust Store now support post-quantum authentication.</p><p>Here we’ll explain how you can configure fully post-quantum secure mutually authenticated TLS connections to your origin server, dive into the engineering details of how we built it, make a shameful confession, and finally explain how this work fits into our overall post-quantum migration roadmap.</p><h2>Reaching a major milestone</h2><p>Our focus for the past several years has been in deploying post-quantum <a href="https://radar.cloudflare.com/post-quantum"><span style="text-decoration:underline">encryption</span></a> to protect against <a href="https://en.wikipedia.org/wiki/Harvest_now,_decrypt_later"><span style="text-decoration:underline">harvest-now/decrypt-later</span></a> attacks, where an attacker quietly stockpiles your encrypted data with the hope of decrypting it in the future with a quantum computer.</p><p>However, recent breakthroughs in quantum computing and cryptanalysis pulled the timelines for upgrading to post-quantum cryptography forward <a href="https://www.microsoft.com/en-us/security/blog/2026/06/30/microsoft-advances-quantum-safe-security-as-the-risk-timeline-shifts/"><span style="text-decoration:underline">across</span></a> <a href="https://blog.google/innovation-and-ai/technology/safety-security/cryptography-migration-timeline/"><span style="text-decoration:underline">industry</span></a> and <a href="https://blog.cloudflare.com/post-quantum-eo-2026/"><span style="text-decoration:underline">government</span></a> and have caused us to shift our attention to deploying post-quantum <em>authentication</em>, to protect against attackers who will soon be able to use quantum computers to break classical credentials and carry out impersonation attacks.</p><p>In a previous post, we announced that Cloudflare is <a href="https://blog.cloudflare.com/post-quantum-roadmap/#cloudflares-roadmap-to-full-post-quantum-security"><span style="text-decoration:underline">targeting 2029</span></a> for full post-quantum security, and laid out several milestones to hit along the way. We have reached the first of those milestones: our <a href="https://developers.cloudflare.com/ssl/origin-configuration/authenticated-origin-pull/"><span style="text-decoration:underline">Authenticated Origin Pulls</span></a> and <a href="https://developers.cloudflare.com/ssl/origin-configuration/custom-origin-trust-store/"><span style="text-decoration:underline">Custom Origin Trust Store</span></a> products <a href="https://developers.cloudflare.com/changelog/post/2026-06-17-pqc-mldsa-aop-cots/"><span style="text-decoration:underline">now support post-quantum (PQ) authentication</span></a> via Module-Lattice-Based Digital Signature Algorithm (ML-DSA) signatures to protect connections between Cloudflare and customer origin servers. </p><h2>The origin connection is different</h2><p>When a client visits a website proxied by Cloudflare, there are typically two connections involved. The first connection is from the visitor (e.g., a browser) to Cloudflare. If the request can be served from Cloudflare’s cache or triggers any blocking rules, Cloudflare might respond directly. Otherwise, Cloudflare establishes a second connection to the customer’s origin server to fetch the requested content, so it can respond to the original request.</p><!-- unsupported block: image --><p>Protecting sensitive visitor data requires both of these connections to be secure against quantum attacks. We enabled post-quantum encryption support for both the visitor-to-Cloudflare (Connection 1) and Cloudflare-to-origin (Connection 2) connections in <a href="https://blog.cloudflare.com/post-quantum-for-all/"><span style="text-decoration:underline">2022</span></a> and <a href="https://blog.cloudflare.com/post-quantum-to-origins/"><span style="text-decoration:underline">2023</span></a>, respectively, and already see <a href="https://radar.cloudflare.com/post-quantum"><span style="text-decoration:underline">significant usage</span></a>.</p><p>We are actively working on completing the picture with post-quantum authentication. For the visitor-to-Cloudflare connection, we are collaborating with <a href="https://blog.google/security/cultivating-a-robust-and-efficient-quantum-safe-https/"><span style="text-decoration:underline">Google</span></a> and others at the Internet Engineering Task Force (<a href="https://datatracker.ietf.org/wg/plants/about/"><span style="text-decoration:underline">IETF</span></a>) to develop and <a href="https://blog.cloudflare.com/bootstrap-mtc"><span style="text-decoration:underline">experiment</span></a> with Merkle Tree Certificates <a href="https://datatracker.ietf.org/doc/draft-ietf-plants-merkle-tree-certs/"><span style="text-decoration:underline">(MTC)</span></a>, a design for fast, post-quantum certificates for the web, with initial deployments targeting 2027. The topic of this post, however, is the Cloudflare-to-origin connection, where the requirements for authentication differ from that of the visitor-to-Cloudflare connection in several important ways.</p><p>For this connection, Cloudflare is the client. This gives us the control to employ techniques such as connection pooling to fan in requests from all over our network to a smaller set of connections to origin servers, amortizing the overhead of connection setup over many requests. This makes the cost of “drop-in” post-quantum signatures more palatable, and the performance benefits of MTC less necessary.</p><p>And with a pre-existing trust relationship between Cloudflare and customers (i.e., a Cloudflare account), we need not tie ourselves to the constraints and timelines of the public key infrastructure (PKI) for the public Internet (<a href="https://cabforum.org/working-groups/server/baseline-requirements/requirements/"><span style="text-decoration:underline">WebPKI</span></a>) and can instead use custom PKIs tailored to the use case, without overhead from intermediate certificates and <a href="https://datatracker.ietf.org/doc/html/rfc6962"><span style="text-decoration:underline">Certificate Transparency</span></a> that may not be applicable. Solutions like <a href="https://developers.cloudflare.com/tunnel/"><span style="text-decoration:underline">Cloudflare Tunnel</span></a> can also be used to protect the Cloudflare-to-origin connection without upgrading legacy origin systems, by forwarding traffic over a tunnel secured with post-quantum encryption (and post-quantum authentication in the works).</p><p>All this to say, the unique requirements of the Cloudflare-to-origin connection have allowed us to deploy post-quantum authentication via ML-DSA authentication ahead of support landing in the WebPKI for the public Internet. (For customers who stick with the WebPKI, don’t worry: we’ll add MTC support on the Cloudflare-to-origin connection in the future.)</p><p>So how do you turn this on? Let’s dive into the configuration.</p><h2>Configuring fully PQ-secure origin connections</h2><p>We have added ML-DSA support (for all <a href="https://csrc.nist.gov/pubs/fips/204/final"><span style="text-decoration:underline">FIPS 204</span></a> parameter sets: ML-DSA-44, ML-DSA-65, and ML-DSA-87) to the Custom Origin Trust Store and Authenticated Origin Pulls products. ML-DSA-44 is our recommendation for most applications as it is the most performant option and attains a comfortable NIST <a href="https://nvlpubs.nist.gov/nistpubs/FIPS/NIST.FIPS.204.pdf#page=25"><span style="text-decoration:underline">category 2</span></a> security strength.</p><h3>Custom Origin Trust Store</h3><p>When Cloudflare makes a connection to a customer origin server configured with <a href="https://developers.cloudflare.com/ssl/origin-configuration/ssl-modes/full-strict/"><span style="text-decoration:underline">Full (strict)</span></a> SSL mode, we authenticate the origin certificate against a default trust store consisting of all <a href="https://ccadb.org"><span style="text-decoration:underline">commonly trusted</span></a> Certificate Authorities (CAs) as well as Cloudflare’s <a href="https://developers.cloudflare.com/ssl/origin-configuration/origin-ca/"><span style="text-decoration:underline">origin CA</span></a>. The <a href="https://developers.cloudflare.com/ssl/origin-configuration/custom-origin-trust-store/"><span style="text-decoration:underline">Custom Origin Trust Store (COTS)</span></a> product (which requires <a href="https://developers.cloudflare.com/ssl/edge-certificates/advanced-certificate-manager/"><span style="text-decoration:underline">Advanced Certificate Manager</span></a> to be enabled) allows customers to replace this default trust store with a set of CAs they control. COTS now allows customers to upload ML-DSA CAs, such that Cloudflare will trust any origin server certificate chaining to that CA when connecting to the origin.</p><h3>Authenticated Origin Pulls</h3><p>To limit abuse and resource consumption on their origin servers, customers may want to only serve requests coming from Cloudflare’s servers. <a href="https://developers.cloudflare.com/ssl/origin-configuration/authenticated-origin-pull/"><span style="text-decoration:underline">Authenticated Origin Pulls (AOP)</span></a> can be used to configure Cloudflare to present a client certificate to the origin server in order to establish a <a href="https://www.cloudflare.com/learning/access-management/what-is-mutual-tls/"><span style="text-decoration:underline">mutual TLS (mTLS)</span></a> connection, in which communication between the parties is bidirectionally secure and trusted. AOP is available for free on all Cloudflare plan levels.</p><p>AOP supports three <a href="https://developers.cloudflare.com/ssl/origin-configuration/authenticated-origin-pull/#configuration-levels"><span style="text-decoration:underline">configuration levels</span></a>: global, per-zone, and per-hostname. The per-zone and per-hostname configuration levels now allow customers to upload ML-DSA certificates and private keys (in the FIPS 204 seed format), so that Cloudflare’s TLS client will present this certificate to authenticate itself when connecting to the origin server. (Don’t worry, we haven’t forgotten about the global configuration level — it just happens to be a more involved change that will be prioritized at a later date.)</p><h3>Avoiding downgrades</h3><p>Adding post-quantum encryption and authentication support to both the authenticating and verifying parties is necessary but <em>not</em> sufficient for full post-quantum security. The pesky issue of downgrades remains. If the verifying party supports any quantum-vulnerable authentication mechanisms, they remain open to attack from an <a href="https://www.cloudflare.com/learning/security/threats/on-path-attack/"><span style="text-decoration:underline">on-path attacker</span></a> capable of forging classical credentials.</p><p>The fix: the verifying party must remove trust in quantum-vulnerable authentication mechanisms. (This is more nuanced in complex PKIs. For example, see the Chromium Security team’s <a href="https://www.chromium.org/Home/chromium-security/post-quantum-auth-roadmap/"><span style="text-decoration:underline">four-stage plan</span></a> for transitioning the Web.) See the <a href="https://developers.cloudflare.com/ssl/post-quantum-cryptography/pqc-to-origin/#avoid-downgrades"><span style="text-decoration:underline">configuration guide</span></a> for AOP and COTS for details on how to ensure your origin is secure against downgrade attacks.</p><h3>Quick start</h3><p>The walkthrough below shows how to generate an ML-DSA certificate chain and configure both products via the Cloudflare API. For dashboard instructions and additional context, refer to the <a href="https://developers.cloudflare.com/ssl/post-quantum-cryptography/pqc-to-origin/"><span style="text-decoration:underline">developer docs</span></a>.</p><p>1. Generate certificates</p><p>You will need OpenSSL 3.5.0 or later. The private key must be generated in the FIPS 204 seed-only encoding, which is the only format Cloudflare currently accepts on upload.</p><p><strong>Origin server certificate chain for COTS:</strong></p><!-- unsupported block: code --><p><strong>Cloudflare client certificate chain for AOP:</strong></p><!-- unsupported block: code --><p>2. Upload the origin CA to Custom Origin Trust Store</p><!-- unsupported block: code --><p><em>Uploading a COTS CA replaces the default publicly-trusted CAs for the zone. Make sure you only upload post-quantum CAs if you want to avoid downgrade attacks.</em></p><p>3. Upload the client certificate for Authenticated Origin Pulls</p><p>The example below uses zone-level AOP. If you prefer per-hostname AOP, use the <code>/origin_tls_client_auth/hostnames/certificates</code> endpoint instead.</p><!-- unsupported block: code --><p>4. Set your SSL/TLS mode to Full (strict)</p><p>Custom Origin Trust Store is only active when your zone is using <strong>Full (strict)</strong> mode. If you are using AOP without COTS, <strong>Full</strong> or higher is sufficient.</p><!-- unsupported block: code --><p>5. Configure your origin server (on NGINX)</p><p>If you are using COTS (your origin presents the ML-DSA server certificate):</p><!-- unsupported block: code --><p>If you are using AOP (your origin verifies Cloudflare&#x27;s client certificate):</p><!-- unsupported block: code --><p>If you are using both together (recommended for full post-quantum mutual TLS):</p><!-- unsupported block: code --><p>6. Verify the post-quantum handshake</p><p>The TLS handshake between Cloudflare and your origin happens behind the scenes, so you cannot observe it directly by connecting to your proxied hostname from the outside. Instead, verify each side separately.</p><p><strong>Verify COTS (origin presents an ML-DSA certificate):</strong></p><p>If your origin IP is directly reachable (for example, during testing before enabling the Cloudflare proxy), connect to the origin IP directly and validate the certificate:</p><!-- unsupported block: code --><p>Look for <code>Signature type: mldsa44</code> in the output.</p><p>If your origin is firewalled to only accept Cloudflare IPs, check your origin server&#x27;s TLS logs or use a packet capture tool such as <code>ssldump</code> or <code>tcpdump</code> on the origin to confirm that Cloudflare negotiated TLS 1.3 with the ML-DSA certificate.</p><p><strong>Verify AOP (Cloudflare presents a client certificate):</strong></p><p>Confirm that direct connections to the origin (without a valid client certificate) are rejected:</p><!-- unsupported block: code --><p>With <code>ssl_verify_client</code> on enforced, this should fail with an SSL alert.</p><p><strong>Verify the full Cloudflare-to-origin path:</strong></p><p>Because the mTLS handshake happens server-to-server, the most reliable way to confirm that Cloudflare is presenting the ML-DSA client certificate is to inspect your origin server logs. For example, in NGINX you can log the client certificate serial number or subject:</p><!-- unsupported block: code --><p>After sending a request through Cloudflare, check the log. You should see the serial number of the <code>aop-client.crt</code> certificate you uploaded.</p><p>For the key agreement, ensure that your origin&#x27;s TLS library supports <code>X25519MLKEM768</code> and that it is preferred in your configuration. The post-quantum key agreement will be visible in origin server logs or packet captures as the negotiated group.</p><h2>The boring details</h2><p>Implementing this feature involved two primary systems: our control plane service that allows customers to manage their TLS settings and upload certificates, and the data plane service responsible for establishing TLS connections to origin servers based on customer configurations.</p><h3>Control plane</h3><p>As with many other services that power Cloudflare’s APIs and Dashboard, the service that powers the configuration for Cloudflare’s <a href="https://developers.cloudflare.com/ssl/"><span style="text-decoration:underline">SSL/TLS products</span></a> runs in a <a href="https://blog.cloudflare.com/major-data-center-power-failure-again-cloudflare-code-orange-tested/"><span style="text-decoration:underline">highly available setup</span></a> across a set of critical data centers. The service is responsible for handling SSL/TLS settings updates and pushing them out to our <a href="https://blog.cloudflare.com/quicksilver-v2-evolution-of-a-globally-distributed-key-value-store-part-1/"><span style="text-decoration:underline">globally-distributed key-value store</span></a> so that they are available to data plane services when handling live requests.</p><p>Enabling ML-DSA support for AOP and COTS required updating this service to support parsing and validating ML-DSA certificates. This sounds simple on paper, but there’s a catch: the service is written in Go, but Go’s standard X.509 and TLS libraries did not yet support ML-DSA. We instead implemented the <a href="https://github.com/cloudflare/circl/pull/559"><span style="text-decoration:underline">necessary functionality</span></a> in Cloudflare’s <a href="https://blog.cloudflare.com/introducing-circl/"><span style="text-decoration:underline">CIRCL</span></a> <a href="https://github.com/cloudflare/circl"><span style="text-decoration:underline">library</span></a> to patch in support. This was a relatively simple change, but repeating this for every service that needs post-quantum authentication support would be a major chore.</p><p>Fortunately, <a href="https://go.dev/doc/go1.27"><span style="text-decoration:underline">Go 1.27</span></a> (expected August 2026) will include native ML-DSA support, and will allow us to drop the CIRCL dependency. Other Go-based services will then be able to seamlessly pull in ML-DSA support with a simple version update.</p><h3>Data plane</h3><p>With the control plane changes in place, customers could then upload ML-DSA certificates for the AOP and COTS products. The next step was to update our data plane service responsible for interacting with customer origins to actually <em>use </em>those certificates.</p><p>We have talked in previous blog posts about our open-source proxy framework <a href="https://blog.cloudflare.com/pingora-open-source/"><span style="text-decoration:underline">Pingora</span></a> and specifically how we have a Pingora-based service that handles all the connections to those origins. That service is unimaginatively named <a href="https://blog.cloudflare.com/pingora-saving-compute-1-percent-at-a-time/#motivation"><span style="text-decoration:underline">Pingora Origin</span></a>, and it is responsible for ensuring millions of requests per second worth of origin-bound requests make it safely and securely to their final destination. </p><p>The task of ensuring the request’s security typically falls to the TLS provider, and it may surprise you to know that post-quantum security (or in this case authenticity) is no different. It also might come as a letdown that defending against quantum attacks <a href="https://blog.cloudflare.com/you-dont-need-quantum-hardware"><span style="text-decoration:underline">does not require</span></a> exotic states of matter with lasers and superconducting <a href="https://en.wikipedia.org/wiki/Josephson_junction"><span style="text-decoration:underline">Josephson junctions</span></a>; all you need is an update to <a href="https://github.com/google/boringssl"><span style="text-decoration:underline">BoringSSL</span></a>. Now, BoringSSL lives up to its name: over the past several years, there have been no CVEs or major changes. In fact, we relied on that stability so heavily that we have an admission to make: we snoozed Pingora Origin’s update to BoringSSL for four years, instead maintaining an internal fork to patch in additional functionality as needed. That has worked well, but when post-quantum authentication support <a href="https://github.com/google/boringssl/commit/4a3cda40b965bbda7cebf86e35c1ed6890ebcc34"><span style="text-decoration:underline">landed in BoringSSL</span></a> in April 2026, we decided that this update was worth the inconvenience.</p><p>This is where we wish we could say, “This update went perfectly. No notes!” but naturally there were some hiccups. Within the four years’ worth of code changes was <a href="https://github.com/google/boringssl/commit/cee2dbb08cb3daf56995875207b5e947310697de#diff-fab9b7680cab08ae895d70161c48cb7fde3af78110c4b5641a7c6a3e0b1f441aL706"><span style="text-decoration:underline">this commit</span></a> that enables enforcement of rules related to <a href="https://datatracker.ietf.org/doc/html/rfc5280#section-4.2.1.3"><span style="text-decoration:underline">KeyUsage</span></a> in TLS certificates. This change is in line with the specifications, but as we have seen before, the Internet is not known for being <a href="https://github.com/cloudflare/boring/pull/495"><span style="text-decoration:underline">RFC compliant</span></a>. The result was that even after testing the changes for weeks and a very slow release rollout looking for just this sort of regression, a small number of customers’ certificates were deemed invalid after the change, leading to an <a href="https://www.cloudflarestatus.com/incidents/mwj6mb927p3g"><span style="text-decoration:underline">incident on June 10, 2026</span></a>. We quickly rolled back the change and after a <a href="https://github.com/cloudflare/boring/pull/512"><span style="text-decoration:underline">patch</span></a> to retain support for RSA certificates with technically invalid <a href="https://datatracker.ietf.org/doc/html/rfc5280"><span style="text-decoration:underline">KeyUsage</span></a>, fully post-quantum secure TLS to origins is now live and ready to use.</p><h2>We are only getting started</h2><p>ML-DSA support is increasingly ubiquitous across <a href="https://developers.cloudflare.com/ssl/post-quantum-cryptography/pqc-support/#libraries"><span style="text-decoration:underline">TLS libraries</span></a>, and routine software updates will bring post-quantum authentication support to many applications. (Please keep your libraries updated!) The highly-anticipated <a href="https://go.dev/doc/go1.27"><span style="text-decoration:underline">Go 1.27</span></a> (August 2026) will come with native ML-DSA support, allowing Go-based services to add post-quantum authentication with a simple version update.</p><p>As these changes propagate across the ecosystem, we will be upgrading our systems as well. See <a href="https://developers.cloudflare.com/ssl/post-quantum-cryptography/pqc-cloudflare-products/"><span style="text-decoration:underline">PQC in Cloudflare Products</span></a> for an up-to-date tracker of post-quantum encryption and authentication support in Cloudflare products and services.</p>]]></content:encoded>
      <guid isPermaLink="false">01KYMH71B35NXQ00E0M2D0WMQV</guid>
    </item>
    <item>
      <title>Natural disasters and government interference: examining Q2 2026’s major Internet disruption events</title>
      <link>https://blog.cloudflare.com/q2-2026-internet-disruption-summary/</link>
      <pubDate>Tue, 28 Jul 2026 13:00:00 GMT</pubDate>
      <description>Cloudflare Radar tracked Internet disruptions driven by natural disasters, government-mandated shutdowns, and DNSSEC key rollovers over the last quarter. This post analyzes traffic telemetry to explain how these events impacted global connectivity.</description>
      <dc:creator>Lai Yi Ohlsen</dc:creator>
      <category>AWS</category>
      <category>Internet Shutdown</category>
      <category>Internet Traffic</category>
      <category>Internet Trends</category>
      <category>Outage</category>
      <category>Radar</category>
      <content:encoded><![CDATA[<p>Like most infrastructure, the Internet&#x27;s fragility is easy to overlook — as long as it&#x27;s working. When it fails, its complexity comes into full view. Cloudflare is in a unique position to detect and document the moments when one of the interrelated systems the Internet depends on breaks down and connectivity suffers as a result. Each quarter, we summarize the disruptions we detect and annotate on <a href="https://radar.cloudflare.com/"><span style="text-decoration:underline">Cloudflare Radar</span></a>.</p><p>In Q2 2026, Super Typhoon Sinlaku just north of Guam caused the longest outage, while government-mandated shutdowns during exam periods in Sudan were the most frequent. Iran restored national Internet access, reconnecting its citizens to the global network after an 88-day blackout, even as damage from drone strikes continued to disrupt AWS infrastructure elsewhere in the region. Finally, a cable cut in Saint Lucia and the distribution of faulty DNSSEC signatures in Germany underscored the fragility of Internet infrastructure, but also the remarkable stability these regional and global systems maintain when operating normally.</p><p>Here we will walk through the most significant Internet disruptions we observed in the second quarter of 2026, drawing on traffic data from Cloudflare Radar to show how each unfolded and what it meant for users on the ground. As always, this is a summary of notable, confirmed disruptions rather than an exhaustive list; a fuller view of detected traffic anomalies is available in the <a href="https://radar.cloudflare.com/outage-center?dateStart=2026-04-01&amp;dateEnd=2026-06-30"><span style="text-decoration:underline">Cloudflare Radar Outage Center</span></a>. </p><h3>Natural disasters and electricity cause disruptions in Guam, Venezuela, and Tanzania</h3><p>Super Typhoon Sinlaku, the strongest storm of the 2026 Pacific typhoon season so far, tracked through the Mariana Islands in mid-April, passing just north of Guam. Though the island was spared a direct hit, the storm brought tropical-storm-force winds, knocking out power across Guam and disrupting water systems, which had a direct impact on Internet connectivity. Traffic from the territory fell as much as 80% below expected levels from April 13 to 14. </p><!-- unsupported block: htmlBlock --><p>Two months later, on June 24, two major earthquakes struck northern Venezuela within about a minute of each other, in Yumare and San Felipe, followed by an aftershock near the coast outside of Caracas. The first 7.5 magnitude earthquake took place at roughly 22:04 UTC (18:04 local time). The immediate impact of these events can be seen in Radar, which shows a sharp decrease in both HTTP bytes transferred at the same time as the earthquakes. This decrease can be seen particularly well in Fibex Telecom, which, according to <a href="https://stats.labs.apnic.net/aspop/"><span style="text-decoration:underline">APNIC data</span></a>, has 1.6 million estimated users. The drop is also visible for <a href="https://radar.cloudflare.com/traffic/as8048?dateStart=2026-06-24&amp;dateEnd=2026-06-25#traffic-trends"><span style="text-decoration:underline">CANTV</span></a>, the state-owned incumbent, and <a href="https://radar.cloudflare.com/traffic/as263703?dateStart=2026-06-24&amp;dateEnd=2026-06-25"><span style="text-decoration:underline">VNET</span></a>, a slightly smaller regional ISP. </p><!-- unsupported block: htmlBlock --><p>Across the Atlantic just a few days later, a power outage in Tanzania on June 27 caused a sharp drop in HTTP traffic there that lasted for at least five hours. While distinct in cause from the country&#x27;s election-related blackout in October 2025 (a deliberate government action rather than an infrastructure failure) the resulting telemetry and user impact were nearly identical: a drastic loss of connectivity that left residents unable to communicate with loved ones or access critical news. </p><p>It is striking how such fundamentally different events leave such similar footprints in the data and user experience. Taken together, these weather-related and power-driven disruptions demonstrate the immense impact the physical world can have on the digital, and the importance of Internet resilience and of building networks with enough redundancy in power, routing, and physical paths to withstand inevitable shocks.</p><h3>Governments and geopolitics impact connectivity in Iran, UAE, Iraq and Sudan</h3><p>Starting on May 26, Radar began seeing signs of Iran&#x27;s previously <a href="https://x.com/ir_aref/status/2059261258566877640?s=20"><span style="text-decoration:underline">announced</span></a> Internet restoration, the tentative end of an 88-day shutdown that had left the country almost entirely offline since it began on February 28. On May 27, Radar <a href="https://blog.cloudflare.com/iran-internet-partially-restored-may-2026/"><span style="text-decoration:underline">reported</span></a> that traffic had been restored to 40% of its pre-outage levels, a partial reopening consistent with reports that access was being reintroduced selectively rather than all at once. Since then, we have seen HTTP bytes climb to as high as 90% before settling back to roughly 59% of pre-shutdown levels. This volume is consistent with the traffic we observed in February, a window between this recent shutdown and a previous one in January, suggesting that connectivity has returned to something like its most recent pre-shutdown baseline rather than fully normalizing. In our <a href="https://blog.cloudflare.com/2026-world-cup-internet-traffic/#streaming-makes-some-countries-appear-more-online"><span style="text-decoration:underline">2026 World Cup analysis</span></a>, Iran stood out as a solo outlier: While traffic in most participating countries rose and fell with match schedules, Iran&#x27;s readings were dominated instead by the contrast between its post-restoration levels and the near-complete loss of connectivity that had preceded them.</p><!-- unsupported block: htmlBlock --><p>Meanwhile, HTTP traffic to me-central-1, an AWS cloud region located in the United Arab Emirates, has <a href="https://radar.cloudflare.com/cloud-observatory/amazon/me-central-1?dateRange=24w#http-traffic"><span style="text-decoration:underline">remained low</span></a>, aligning with <a href="https://health.aws.amazon.com/health/status#multipleservices-me-central-1_1777533954"><span style="text-decoration:underline">AWS service reports</span></a> on April 30 that the region &quot;has suffered damage as a result of the conflict in the Middle East and is currently unable to reliably support customer applications.&quot; This update follows the reports on March 3 that facilities in both UAE and Bahrain “have experienced physical impacts to infrastructure as a result of drone strikes.” In the UAE, two facilities were “directly struck” and in Bahrain a drone strike near the facility caused “physical impact” to their infrastructure. The decreased traffic is the downstream signature of physical damage to the underlying data center infrastructure rather than a network fault, and it continues to affect the websites and applications hosted in that region, regardless of their own availability.</p><!-- unsupported block: htmlBlock --><p>The second quarter of 2026 also included three government-mandated shutdowns in Iraq (on <a href="https://radar.cloudflare.com/traffic/iq?dateStart=2026-06-01&amp;dateEnd=2026-06-02"><span style="text-decoration:underline">June 2</span></a>, <a href="https://radar.cloudflare.com/traffic/iq?dateStart=2026-06-10&amp;dateEnd=2026-06-11"><span style="text-decoration:underline">June 11</span></a>, and <a href="https://radar.cloudflare.com/traffic/iq?dateStart=2026-06-27&amp;dateEnd=2026-06-28"><span style="text-decoration:underline">June 28</span></a>) as well as <a href="https://radar.cloudflare.com/traffic/sd?dateStart=2026-04-13&amp;dateEnd=2026-04-23#traffic-trends"><span style="text-decoration:underline">10 in Sudan</span></a> between April 13 and 23, all of which were imposed to prevent cheating on national exams — a seasonal pattern we have documented across multiple prior quarters in both countries. The outages in Sudan followed a consistent rhythm, each lasting approximately 3.5 hours from 11:45 to 15:15 UTC (13:45 to 17:15 local time), timed to the exam window. In Iraq the outages were shorter, lasting about 90 minutes each, and likewise scheduled around the hours in which exams were administered.</p><!-- unsupported block: htmlBlock --><!-- unsupported block: htmlBlock --><p>Each of these examples, whether a restoration or a disruption, illustrates the significant control governments exert over their national connectivity, and the ease with which access can be switched off, throttled, or selectively reintroduced as a matter of policy rather than infrastructure.</p><h3>Infrastructure vulnerabilities affect users in Germany and Saint Lucia </h3><p>On May 5, a DNSSEC key rollover at DENIC, the registry for Germany&#x27;s .de domain, <a href="https://blog.denic.de/technische-storung-bei-de-domains-behoben/"><span style="text-decoration:underline">started producing invalid signatures</span></a>. These key rollovers are the periodic replacement of the cryptographic keys used to sign a zone&#x27;s DNS records; they are a routine but crucial piece of maintenance, as resolvers that validate DNSSEC will only trust answers whose signatures match the current published keys. In other words, if the digital signatures don&#x27;t match expected values, the resolver assumes the site has been tampered with and cuts off access. When invalid signatures started being produced, validating resolvers worldwide rejected every request for a .de website and returned SERVFAIL errors until normal operation was restored at 23:15 UTC (01:15 local time on May 6). </p><p>Cloudflare Radar observed worldwide .de query volume rise during the outage. While perhaps initially counterintuitive, this is because failed answers are effectively uncacheable, so lookups normally served silently from cache instead had to be re-resolved and retried repeatedly, causing a sharp increase in queries.</p><!-- unsupported block: htmlBlock --><p>From a user&#x27;s perspective, the incident was experienced not as a DNS or cryptographic failure but simply as a wave of .de websites and services suddenly becoming unreachable. Though users were still able to access sites that did not use the .de TLD, the experience included pages failing to load, email bouncing, and apps timing out, all of which can mirror the experience of an outage. You can read more about DNSSEC and the impact of the events <a href="https://blog.cloudflare.com/de-tld-outage-dnssec/"><span style="text-decoration:underline">on our blog</span></a>.</p><p>In the Caribbean, an infrastructure failure caused a similar drop in availability. On June 21, HTTP request traffic from Karib Cable’s network fell to essentially zero by around 21:00 UTC (17:00 local time), and remained flat for the better part of a day before recovering to expected levels around 17:00 UTC on June 22 (13:00 local time). The outage was <a href="https://stluciatimes.com/181838/2026/07/flow-reveals-details-of-customer-rebates-after-major-outage/"><span style="text-decoration:underline">reportedly</span></a> caused by a fiber cut near the island, a familiar hazard for Caribbean networks that depend on a few terrestrial and submarine paths to reach the wider Internet, meaning a single break can sever a disproportionate amount of capacity. Because Karib Cable is one of the largest providers, the loss was also visible at the country level, with Saint Lucia&#x27;s overall traffic <a href="https://radar.cloudflare.com/explorer?dataSet=netflows&amp;loc=LC&amp;dt=2026-06-21_2026-06-27&amp;timeCompare=1#result"><span style="text-decoration:underline">dropping approximately 60% against the prior week</span></a> for the duration of the cut.</p><!-- unsupported block: htmlBlock --><h3>Radar continues to monitor disruptions</h3><p>The second quarter of 2026 saw Internet disruptions arise from a wide range of causes, including severe weather, an earthquake, power outages, government-directed shutdowns, damage to cloud infrastructure, cable cuts, and a DNSSEC misconfiguration. As these events demonstrate, the Internet depends on a complex set of interrelated systems, and a failure in any one of them can result in a loss of connectivity.</p><p>The Cloudflare Radar team is constantly monitoring for Internet disruptions, sharing our observations on the <a href="https://radar.cloudflare.com/outage-center">Cloudflare Radar Outage Center</a>, via social media, and in posts on <a href="http://blog.cloudflare.com">blog.cloudflare.com</a>. Follow us on social media at <a href="https://twitter.com/CloudflareRadar">@CloudflareRadar</a> (X), <a href="https://noc.social/@cloudflareradar">noc.social/@cloudflareradar</a> (Mastodon), and <a href="http://radar.cloudflare.com">radar.cloudflare.com</a> (Bluesky).</p>]]></content:encoded>
      <guid isPermaLink="false">01KYJZQZHMFNYN2EGYDF73J99A</guid>
    </item>
    <item>
      <title>We’re open-sourcing our privacy proxy CLI</title>
      <link>https://blog.cloudflare.com/open-sourcing-our-privacy-proxy-cli/</link>
      <pubDate>Mon, 27 Jul 2026 13:00:00 GMT</pubDate>
      <description>pvcli is a curl-like tool designed to simplify the testing of complex privacy protocols like OHTTP.</description>
      <dc:creator>Hannah Wang</dc:creator>
      <dc:creator>Ben Yang</dc:creator>
      <dc:creator>Fisher Darling</dc:creator>
      <category>Better Internet</category>
      <category>HTTP3</category>
      <category>Open Source</category>
      <category>Privacy</category>
      <category>QUIC</category>
      <category>Rust</category>
      <content:encoded><![CDATA[<p>Debugging privacy-preserving protocols is hard. Oblivious HTTP has several different steps across four different parties, not to mention binary HTTP encoding and details spread across many draft RFCs. We&#x27;ve taken what we&#x27;ve learned operating protocols like Oblivious HTTP at the scale of millions of requests per second, and wrapped it up in a nice, clean CLI tool — that we are open-sourcing today.</p><p>We call it our <strong>privacy-client</strong>, or <strong>pvcli</strong>. We’re releasing it under the Apache-2.0 License, and it is<a href="https://github.com/cloudflareresearch/pvcli"> <span style="text-decoration:underline">open for contributions</span></a>.</p><p>Here’s a single line of code that executes a full Oblivious HTTP request with a relay, gateway and origin. Don&#x27;t worry if you don&#x27;t know what that means, we&#x27;ll cover it below.</p><!-- unsupported block: code --><p>We’ll explain why we built this tool, and show just how handy it can be.</p><h3>Why privacy protocols can be hard to debug</h3><p>Let’s take a closer look at our motivation for creating pvcli. Over time, the Privacy team’s product suite and customer base grew. We added products like Privacy Proxy and Privacy Gateway, which power Apple’s <a href="https://blog.cloudflare.com/icloud-private-relay/"><span style="text-decoration:underline">Private Relay</span></a>, Microsoft’s <a href="https://blog.cloudflare.com/cloudflare-now-powering-microsoft-edge-secure-network/"><span style="text-decoration:underline">Edge Secure Network VPN</span></a>, Flo Health’s <a href="https://www.cloudflare.com/case-studies/flo-health/"><span style="text-decoration:underline">Anonymous Mode</span></a>, and more. With it came an increasing amount of special customer requirements, domain knowledge, and complexity. As a result, we saw increased friction in development and incident response.</p><p>To see this in action, let’s look at how one of our products implements <a href="https://developers.cloudflare.com/privacy-gateway/"><span style="text-decoration:underline">Oblivious HTTP</span></a>, also known as OHTTP. First, a quick primer. OHTTP provides users with a privacy guarantee: no one can know both <em>who</em> made a request and <em>what</em> they’re requesting. To achieve this, OHTTP requires two servers, a relay and a gateway, operated by two non-colluding parties. </p><p>Below is a sequence diagram of OHTTP, where our customer owns the relay and Cloudflare owns the gateway. At a high level, OHTTP can be broken down into these steps:</p><ol><li>Client gets public key from the gateway.</li><li>Client encrypts the request and sends it to the relay.</li><li>Relay removes “who” the client is from the encrypted request, and sends it to the gateway.</li><li>Gateway decrypts the request, and sends it to the target.</li><li>Target processes the request, and sends a response to the gateway.</li><li>Gateway encrypts the response, and sends it to the relay.</li><li>Relay sends the encrypted response to the client.</li><li>Client decrypts, and gets the plaintext response.</li></ol><p>It involves quite a bit of back-and-forth, as you can see: </p><!-- unsupported block: image --><p>Each step is a potential point of failure that we have to consider while debugging!</p><p>In particular, we saw certain kinds of problems when debugging OHTTP.</p><ul><li>Customers asked for ways to test the live system from their end, and we often wrote one-off, custom clients for our customers specific deployments.</li><li>Figuring out which step caused an issue was time-consuming. Was the root cause a bug in our system or our customer’s system?</li><li>Examining raw bits was tedious and highly prone to human error. OHTTP builds on binary HTTP, which is a binary encoded HTTP request. Anytime we needed to check the binary encoding, we were painstakingly going through raw bits.</li></ul><p>As a result, we decided to place all of our privacy protocols in one tool. It has a clean interface that’s already familiar, displays every single step of the protocol in order, and is flexible enough to support new protocols and architectures.</p><p>To see the difference this makes, let’s see an OHTTP debugging scenario — before and after pvcli.</p><h3>Debugging without pvcli</h3><p>Say we operate an OHTTP relay that sits in front of a customer&#x27;s gateway. The customer has asked us to do an end-to-end test with a request:</p><!-- unsupported block: code --><p>Recall the OHTTP steps from earlier. The first step is to fetch the public key from the gateway. We use curl to fetch it from the customer gateway and get this back:</p><!-- unsupported block: code --><p>That’s a big binary string in hex. To make sense of it, we look at OHTTP <a href="https://www.rfc-editor.org/rfc/rfc9458#name-key-configuration-encoding"><span style="text-decoration:underline">RFC 9458 §3</span></a> and parse it manually:</p><ul><li><code>0029</code> is 41 in decimal, telling us this public key entry has 41 bytes associated with it.</li><li><code>55</code> is the public key ID.</li><li><code>0020</code> identifies the asymmetric encryption method we can use. In this case, DHKEM(X25519, HKDF-SHA256).</li><li><code>b9bb667e2230dc01c6d6cc047f94a1083beb185c63e50ec09f7692a5a0832540</code> is the public key.</li><li><code>0004</code> tells us there are 4 bytes of symmetric cryptographic IDs that follow.</li><li><code>0001</code> and <code>0001</code> identify the symmetric encryption methods we can use: HKDF-SHA256 and AES-128-GCM.</li></ul><p>We repeat this process for however many public keys are in the binary string.</p><p>Next, we convert our original HTTP request into binary HTTP, referencing <a href="https://www.rfc-editor.org/rfc/rfc9292"><span style="text-decoration:underline">RFC 9292</span></a>. We manually craft the binary with the help of some bespoke scripts:</p><!-- unsupported block: code --><p>We verify each field:</p><ul><li><code>02</code> means it&#x27;s an indeterminate-length request</li><li><code>04504f5354</code> is <code>POST</code></li><li><code>056874747073</code> is https</li><li><code>117461726765742e6f687474702e696e666f</code> is <code>target.ohttp.info</code></li><li>and so on</li></ul><p>Finally, we form a wrapper HTTP request that will hold our OHTTP request. To do so, we spend some more time writing another makeshift script that encrypts the binary HTTP request in the manner OHTTP specifies, using the public key from earlier. We create a header, which is the concatenation of public key ID, asymmetric encryption method ID, and symmetric encryption method IDs. Then, we concatenate header and encrypted binary HTTP request, resulting in:</p><!-- unsupported block: code --><p>We put those bytes into the body of our wrapper HTTP request, and send it to our relay. We get back a response.</p><!-- unsupported block: code --><p>What does that mean? We reach out to the customer to ask if they can share logs from their gateway. In the meantime, we double-check the bits we&#x27;ve crafted. The decoded public keys look fine. The binary HTTP request... Oh! We see:</p><!-- unsupported block: code --><p>BHTTP is length prefixed. That means we specify a length (0x0a is 10 in decimal), and then 10 bytes follow. But here, 11 bytes follow. There is an extra 20 before the 00. 20 represents a space character, so we must have accidentally added that when building the body. We remove the extra character, resend, and it works!</p><h3>Debugging with pvcli</h3><p>With pvcli, all of that is now a single command:</p><!-- unsupported block: code --><p>It handles all the binary parsing and encrypting for us, and prints logs in case we want to dive deeper:</p><!-- unsupported block: code --><p>What used to be a fragile process — involving manipulating bits, gluing together scripts, and referencing long RFCs — is now one command.</p><h3>What pvcli can do</h3><p>To install:</p><!-- unsupported block: code --><p>pvcli takes a lot of inspiration from curl. We designed it with the “principle of least surprise” in mind. As a result, a lot of the arguments are the same as curl’s! Try a quick GET request to our <a href="https://developers.cloudflare.com/fundamentals/reference/cdn-cgi-endpoint/"><span style="text-decoration:underline">cdn-cgi endpoint</span></a>:</p><!-- unsupported block: code --><p>If you’re curious about what is happening under the hood, you can use <code>-v</code> to get detailed logs:</p><!-- unsupported block: code --><p>Now, about that OHTTP command from earlier: you use --ohttp to tell pvcli to construct an OHTTP request. You pass in the relay as the --first-hop and the gateway as the --proxy. The target will be an echo server, so you can see what the target would see. In this command, we filled in the arguments with a relay, gateway, and target from <a href="http://ohttp.info">ohttp.info</a>.</p><!-- unsupported block: code --><p>Try running the command yourself!</p><p>We’ve encountered many cases where we wanted to pass headers to the relay, rather than the target. You are able to do that with <code>--first-hop-header</code>:</p><!-- unsupported block: code --><p>Similarly, we’ve also had cases where we wanted to authenticate to the relay with <a href="https://www.cloudflare.com/learning/access-management/what-is-mutual-tls/"><span style="text-decoration:underline">mTLS</span></a>, to ensure that the correct client is talking with the correct relay. To do that, you can use <code>--first-hop-client</code> and <code>--first-hop-key</code>.</p><!-- unsupported block: code --><p>And it just works. Need to test a full Oblivious HTTP request with a relay, a gateway, arbitrary headers, and mTLS? Or perhaps only request through a gateway? Or maybe you just want to see the OHTTP key configuration? pvcli can do it with a single command, debugging included.</p><h3>Why build our own tool?</h3><p>There are some great tools for OHTTP that already exist. Martin Thomson’s <a href="https://github.com/martinthomson/ohttp"><span style="text-decoration:underline">Rust implementation</span></a> and Chris Wood’s <a href="https://github.com/chris-wood/ohttp-go"><span style="text-decoration:underline">Go implementation</span></a> were incredibly helpful when we built out our original OHTTP implementation a few years ago. But pvcli is not only focused on OHTTP. We’re looking to add as many privacy-preserving protocols as we can to the tool. So while there are other OSS tools out there for debugging OHTTP, nothing combines OHTTP, CONNECT proxying, MASQUE and Privacy Pass (coming soon) all in one place.</p><h3>Contribute to pvcli</h3><p>Oblivious HTTP is an amazing protocol, and we would love to see you use it. We hope that this tool helps people debug OHTTP and write their own OHTTP implementations. </p><p>We are accepting contributions! To get started, clone the repo at <a href="https://github.com/cloudflareresearch/pvcli"><span style="text-decoration:underline">https://github.com/cloudflareresearch/pvcli</span></a>, and submit a pull request. </p><p>If you&#x27;re looking for ways to contribute, here are some things on our to-do list. For MASQUE, we plan to add support for proxying TCP over HTTP/3, and UDP and/or IP over HTTP/2 and HTTP/3. For OHTTP, we plan to support post-quantum cryptography, add timing/latency information, support Chunked OHTTP, and improve logging.</p><p><a href="https://www.cloudflare.com/lp/privacy-edge/"><span style="text-decoration:underline">Contact us</span></a> if you are interested in using Cloudflare’s OHTTP Relays and Gateways.</p>]]></content:encoded>
      <guid isPermaLink="false">01KYGZXPC2FQ4NSXSKXXYY86FZ</guid>
    </item>
    <item>
      <title>BGP ORIGIN attribute manipulation and its impact on the Internet</title>
      <link>https://blog.cloudflare.com/bgp-origin-attribute/</link>
      <pubDate>Fri, 24 Jul 2026 17:25:28 GMT</pubDate>
      <description>By doing in-depth testing, we found nearly 70% of BGP paths experience ORIGIN attribute rewrites by transit providers seeking traffic advantages. We examine the global impact of this practice and argue for deprecating ORIGIN in route selection.</description>
      <dc:creator>Iliana Xygkou</dc:creator>
      <dc:creator>Bryton Herdes</dc:creator>
      <category>Better Internet</category>
      <category>BGP</category>
      <category>Network Services</category>
      <category>Routing</category>
      <category>Routing Security</category>
      <content:encoded><![CDATA[<p>Border Gateway Protocol (<a href="https://www.cloudflare.com/learning/security/glossary/what-is-bgp/"><span style="text-decoration:underline">BGP</span></a>) is the de facto routing protocol of the Internet. It offers built-in mechanisms to allow entities, represented by Autonomous Systems (ASes), to express how they want to send and receive traffic on the Internet. One such mechanism is <a href="https://www.iana.org/assignments/bgp-parameters/bgp-parameters.xhtml#bgp-parameters-2"><span style="text-decoration:underline">path attributes</span></a>, which carry essential routing information and metadata for their associated route. The path selection algorithm processes some of these path attributes in a <a href="https://datatracker.ietf.org/doc/html/rfc4271#section-9.1"><span style="text-decoration:underline">deterministic sequence</span></a> to calculate the best path for this specific prefix.</p><p>Using our unique position on the Internet, we took an investigative look at one of the well-known mandatory attributes in BGP, the <a href="https://datatracker.ietf.org/doc/html/rfc4271#section-5.1.1"><strong><span style="text-decoration:underline">ORIGIN</span></strong></a> attribute. ORIGIN must be present in every BGP prefix <a href="https://datatracker.ietf.org/doc/html/rfc4271#section-4.3"><span style="text-decoration:underline">announcement</span></a> and should not be modified by any router after being set by the originating one. What we found through our own experiments was a dramatic ~70% of observed paths in numerous vantage points have a <strong>different</strong> ORIGIN value compared to what was set by the originating Autonomous System. This ORIGIN attribute manipulation has a significant impact on the way traffic is forwarded on the Internet, as we’ll explore in this post. </p><h2>BGP ORIGIN and its operational history</h2><p>The ORIGIN attribute indicates <strong>how</strong> a route was injected into BGP — not to be confused with the origin AS that indicates <strong>which<em> </em></strong>AS<strong><em> </em></strong>announced a route. It has three possible values:</p><p>(0) IGP: Indicates the route is interior to the originating AS</p><p>(1) EGP: A historical value indicating that the route was learned via the old <a href="https://datatracker.ietf.org/doc/html/rfc904"><span style="text-decoration:underline">Exterior Gateway Protocol (EGP)</span></a>, which is obsolete and not intended to be used in the modern Internet</p><p>(2) INCOMPLETE: Indicates the route was learned via an unknown or external source</p><p>Among total observable routes from all the public BGP collectors of <a href="https://www.ripe.net/analyse/internet-measurements/routing-information-service-ris/"><span style="text-decoration:underline">RIPE RIS</span></a> and <a href="https://www.routeviews.org/routeviews/"><span style="text-decoration:underline">RouteViews</span></a>, 89.8% have ORIGIN set to IGP, 3.5% to EGP, and 6.7% are INCOMPLETE. As mentioned above, EGP is meant to be deprecated entirely and INCOMPLETE carries a minority share of total routes. These figures suggest that while IGP is by far the most popular value for ORIGIN, more than 10% of routes have an EGP or INCOMPLETE value that could make a difference in routing decisions.</p><p>As a part of the BGP <a href="https://datatracker.ietf.org/doc/html/rfc4271#section-9.1"><span style="text-decoration:underline">path selection process</span></a>, a router evaluates the ORIGIN if two routes have equal Local Preference and AS_PATH length, selecting and installing the path with the lower ORIGIN value.</p><p></p><!-- unsupported block: image --><p>Aside from the path selection decision, it is worth noting that <a href="https://datatracker.ietf.org/doc/html/rfc4271#section-5.1.1"><span style="text-decoration:underline">RFC4271 states the following</span></a> about ORIGIN:</p><p><code>The ORIGIN attribute is generated by the speaker that originates the associated routing information. Its value SHOULD NOT be changed by any other speaker.</code></p><p>Though the guidance is to not modify the ORIGIN attribute, due to its early evaluation in the route selection process, this attribute has presented an attractive option for ASes to alter route preferences and divert traffic either through or away from their networks. </p><p>For example, in the diagram below, AS64501 announces a prefix with ORIGIN set to INCOMPLETE and propagates this announcement to both of its customers, AS64502 and AS64503. Normally, both of them should prepend their own AS in the AS_PATH and forward the announcement to their common customer AS64504, preserving the INCOMPLETE value. However, to increase the likelihood of their route being selected over competitors’, AS64503 modifies the ORIGIN to IGP. As a result, AS64504 receives two routes for the prefix with equal AS_PATH lengths, but the one from AS64503 carries the preferred IGP value. Thus, AS64504 will select the route through AS64503 to send traffic to AS64501, driving more traffic and revenue to that provider.</p><p>This simple change, to an inaccurate but more preferable ORIGIN, allows the transit provider to draw traffic and profit.</p><!-- unsupported block: image --><p>The network operators community has silently accepted the reality that transit providers have been rewriting the ORIGIN attribute to IGP in order to attract more traffic to their links. However, James Bensley at the <a href="https://ripe91.ripe.net/programme/meeting-plan/sessions/30/GJ8PFJ/"><span style="text-decoration:underline">RIPE 91 meeting</span></a> was the first to spotlight the widespread adoption of this manipulation technique by major networks. A follow-up presentation by Celsa Sánchez at the <a href="https://www.youtube.com/watch?v=YaY0-4aGCnQ"><span style="text-decoration:underline">LACNIC 45 meeting</span></a> investigated the impact of this behavior in the LACNIC (Latin American and Caribbean) region. While a public disclosure should discourage this practice, it highlights the reality that route selection is a revenue-driven arms race. Rather than waiting for competitors to resume RFC compliance, network operators are more likely to quickly resort to rewriting the ORIGIN simply to level the playing field.</p><p>This increasingly inconsistent handling of ORIGIN across the Internet prompted the creation of a now-expired <a href="https://datatracker.ietf.org/doc/draft-marenamat-idr-scrub-bgp-origin/00/"><span style="text-decoration:underline">Internet-Draft</span></a> that recommended its deprecation. To uncover the extent of this phenomenon and track its actors along with their intentions, we conducted our own experiments and share the results in the next section.</p><h2>ORIGIN attribute manipulation analysis </h2><p>In our experiment, we announced three IPv4 and three IPv6 prefixes, each with a different ORIGIN value (IGP/EGP/INCOMPLETE) from all of our peering locations using BGP <a href="https://www.cloudflare.com/learning/cdn/glossary/anycast-network/"><span style="text-decoration:underline">Anycast</span></a>. After confirming global propagation, we later withdrew the prefixes to trigger the <a href="https://blog.cloudflare.com/going-bgp-zombie-hunting/#path-hunting"><span style="text-decoration:underline">path hunting</span></a> process, revealing more paths to the test prefixes, giving us more opportunities to spot altered ORIGINs. As shown in the figure below, we used the <a href="https://bgpkit.com/"><span style="text-decoration:underline">BGPKIT</span></a> toolkit to parse the Update messages from the Multi-threaded Routing Toolkit (<a href="https://datatracker.ietf.org/doc/html/rfc6396"><span style="text-decoration:underline">MRT</span></a>) dumps of all the public BGP collectors from <a href="https://www.ripe.net/analyse/internet-measurements/routing-information-service-ris/"><span style="text-decoration:underline">RIPE RIS</span></a> and <a href="https://www.routeviews.org/routeviews/"><span style="text-decoration:underline">RouteViews</span></a>, and the local BMP data that we collect from our border routers. Note that we opted to analyze the Updates instead of the Routing Information Base (RIB) dumps, which are snapshots of the routing tables of the peer ASes, to retrieve as many as possible routes both during the announcement and the withdrawal phase. </p><p></p><!-- unsupported block: image --><p>A fundamental difficulty of BGP analysis is the <a href="https://www.routeviews.org/routeviews/2025/01/10/routeviews-peering-policy/#:~:text=The%20changing%20internet%20landscape"><span style="text-decoration:underline">lack of visibility</span></a> into <strong>all</strong> ASes on the Internet for a single routing announcement. This gap is widened by the modern <a href="https://blog.apnic.net/2020/12/04/unpacking-a-flattened-internet/"><span style="text-decoration:underline">flattening of the Internet</span></a>, driven by hyperscalers and CDNs bypassing traditional transit routes in favor of direct, local peering. Consequently, public monitors miss a significant portion of the BGP topology for a given prefix, meaning inferences about AS properties always carry inherent uncertainty. </p><h3>Finding ORIGIN rewriters in two-hop AS_PATHs</h3><p>First, we focused on AS_PATHs with only two ASes: “<em>ASX AS13335”</em>, where we call <em>ASX</em> a direct peer. Since we configured our routers to propagate our announcements with a specific ORIGIN, we are confident that if we observe a different ORIGIN, <em>ASX</em> must have changed it to its new value. The table below shows the manipulation behavior of the 352 direct peers for IPv4 where the “Advertised ORIGIN” column shows what value is being advertised for each of our three prefixes, and the right-hand side columns list the ORIGIN values observed by direct peer ASes:</p><!-- unsupported block: htmlBlock --><p><br/>We would like to point out two interesting observations. First, three ASes change the ORIGIN to EGP, and four ASes change it to INCOMPLETE, regardless of its original value, meaning that they are potentially attempting to deprioritize these routes. We reached out to one of these ASes’ network engineers and confirmed that they are rewriting the ORIGIN to EGP on routes received from peers or providers (to render them less preferred compared to customers’ routes). Second, for the non-IGP prefixes, three ASes propagate routes with both the original value and IGP (the two right-most columns above). Based on other attributes of the routes such as <a href="https://datatracker.ietf.org/doc/html/rfc1997"><span style="text-decoration:underline">communities</span></a> and <a href="https://datatracker.ietf.org/doc/html/rfc4271#section-5.1.7"><span style="text-decoration:underline">AGGREGATOR</span></a>, we infer that these ASes receive our routes at multiple peering locations and update the ORIGIN to IGP, presumably to forward traffic through a preferred point. Combining the latter ASes with the ones consistently rewriting to IGP, we discover that almost 10% of the direct peers change the ORIGIN attribute to IGP. </p><p>In testing IPv6, we expected similar results but wanted to note discrepancies involving the manipulation of the ORIGIN value by the same AS between two address families. In the following table we present the manipulation behavior of the 315 direct peers for IPv6:</p><!-- unsupported block: htmlBlock --><p><br/>Most noteworthy is that two direct peers change the ORIGIN to IGP only for IPv4 prefixes and not IPv6, implying different configurations for the two address families.</p><p>Narrowing our analysis to the <a href="https://bgp.tools/kb/what-is-a-upstream"><span style="text-decoration:underline">Tier-1 ASes</span></a>, six out of the 16 appear to manipulate the ORIGIN value to IGP, matching the results of the <a href="https://ripe91.ripe.net/programme/meeting-plan/sessions/30/GJ8PFJ/"><span style="text-decoration:underline">previous study</span></a>. Note that through manual investigation we found that one Tier-1 network changes the ORIGIN value to IGP for routes learned from peers, while it preserves it for customer routes. This pattern of behavior across several Tier-1 networks reflects the drive to attract the most traffic and cancel out other ORIGIN modifiers’ advantage.</p><h3>Identifying rewriters in longer AS_PATHs</h3><p>Based on these initial results, we extended our methodology to process longer paths and infer the behavior of more ASes. Briefly, our algorithm works as follows: </p><ol><li>We seed our <em>trusted</em> set <em>(T)</em> with AS13335. <em>T </em>holds all the ASes that preserve the ORIGIN.</li><li>For each AS_PATH:<ol><li>Filter out all ASes in <em>T</em>.</li><li>If only one AS remains, we can attribute the ORIGIN to that AS. We record the mapping <em>AS → ORIGIN(s).</em></li></ol></li><li>For each pair <em>AS → ORIGIN(s)</em>:<ol><li>If <em>ORIGIN(s) == original value</em>, we add AS to <em>T.</em></li><li>Else, we add AS to <em>M(odifiers).</em></li></ol></li><li>If <em>T</em> was updated in 3, repeat the procedure from 2.</li></ol><p>Applied to the routes of our IPv4 and IPv6 prefixes originated with EGP and INCOMPLETE, this methodology increases our AS attribution to 606 out of 802 (75.6%) visible ASes in the AS_PATHs, out of which 64 (10.6%) are changing the ORIGIN to IGP. Motivated by the Tier-1s adopting this technique, we studied the significance of the attributed ASes using <a href="https://asrank.caida.org/"><span style="text-decoration:underline">CAIDA’s AS Rank</span></a>. AS Rank provides a ranking of the ASes based on their customer cone (i.e., themselves and all the ASes that can be reached through provider-customer links). </p><p>In the figure below, we show the cumulative distribution (CDF) of AS Ranks of the attributed ASes. An interesting observation is that the IGP-rewriting ASes are highly concentrated at the top of the AS hierarchy, with 20.3% of the rewriting ASes falling in the top-50 of AS Rank.</p><p></p><!-- unsupported block: image --><h3>Impact of large ASes rewriting ORIGIN</h3><p>In total, 26% of the top 50 ASes and 20% of the top 100 ASes are manipulating the ORIGIN attribute, highlighting that despite their overall low percentage, the ASes resetting the ORIGIN are highly central and impactful on the Internet. </p><p>The impact is further reinforced by the finding that a staggering 70% of the unique IPv4 AS_PATHs and 67% of IPv6 AS_PATHs observed in the experiment have ORIGIN reset to IGP. We also studied the changes imposed on best path selection by consolidating BGP Updates to compute the converged active routing-table state of each peer AS. We used the prefix advertisement with ORIGIN set to IGP as a <a href="https://www.britannica.com/science/control-group"><span style="text-decoration:underline">control group</span></a> to compare AS_PATHs observed versus our EGP and INCOMPLETE announcements. When doing so, we found in IPv4 that 110 AS_PATHs out of the total 539 (20%) traversed Tier-1 networks and that resetting the ORIGIN to IGP secured for ORIGIN rewriters 12 additional paths (an increase of 18%) that would otherwise go through the networks that preserve ORIGIN. The effect is even stronger in IPv6, where the rewriters gained 33 more paths (40%). These include 11 paths that for our control group did not traverse any Tier-1 network. This highlights the redirection of traffic toward large Tier-1 ISPs when the ORIGIN was manipulated, diverting traffic away from alternative ISPs.</p><p>As you can see, Internet routing is significantly impacted by the manipulation of the ORIGIN attribute. In our experiments, we can easily observe the effects on BGP path selection and demonstrate how rewriting ORIGIN is a way to siphon traffic and generate revenue. There is no valid <em>technical</em> reason to require a rewrite of the ORIGIN attribute, and we can see it does not make sense to depend on ORIGIN as a driving factor in deciding what routes to take.</p><h2>Deprecating the ORIGIN attribute</h2><p>Given our findings of widespread ORIGIN manipulation, we have to ask ourselves whether this attribute has a meaningful role at all in the modern Internet. We think the answer is <strong>No</strong>. </p><p>The inconsistent treatment of ORIGIN creates unfairness between networks opportunistically changing it and networks complying with the RFC. While immediate deprecation of a <strong>mandatory</strong> attribute in BGP would be infeasible, there has already been work <a href="https://datatracker.ietf.org/doc/html/draft-marenamat-idr-scrub-bgp-origin-00"><span style="text-decoration:underline">proposed in the IETF</span></a> to make the ORIGIN attribute less relevant in BGP path selection. We believe requiring BGP (vendor) implementations to set ORIGIN as IGP on <strong>all</strong> routes received and advertised is a reasonable starting point. IGP is already set as ORIGIN on the great majority of routes.</p><p>We want to revive these conversations in the community and the IETF about the ORIGIN attribute and its future (or lack thereof) on the Internet. This may include renewing the expired draft “Scrubbing BGP ORIGIN Attribute” (<a href="https://datatracker.ietf.org/doc/html/draft-marenamat-idr-scrub-bgp-origin-00"><span style="text-decoration:underline">draft-marenamat-idr-scrub-bgp-origin-00</span></a>), or may include a new approach entirely. Internet routing would be better and fairer without the ORIGIN attribute carrying influence.</p>]]></content:encoded>
      <guid isPermaLink="false">01KYAFZ0EEBE4R099P8HT79D5X</guid>
    </item>
    <item>
      <title>Introducing Cache Response Rules</title>
      <link>https://blog.cloudflare.com/introducing-cache-response-rules/</link>
      <pubDate>Thu, 23 Jul 2026 18:40:38 GMT</pubDate>
      <description>Perhaps you’ve seen something that should sail out of cache get dragged back to the origin by a stray Set-Cookie or Cache-Control, headers that can be difficult to change on the origin itself. Cache Response Rules is the fix, applied at the right time.</description>
      <dc:creator>Alex Krivit</dc:creator>
      <dc:creator>Anthony Turcios</dc:creator>
      <category>Application Services</category>
      <category>Cache</category>
      <category>Cache Rules</category>
      <category>CDN</category>
      <category>Optimization</category>
      <category>Product News</category>
      <content:encoded><![CDATA[<p><strong>Today we’re excited to announce Cache Response Rules. </strong>These are a new rule type that runs after an origin server replies but before Cloudflare caches the content. </p><p>If you&#x27;ve ever been irked watching something that should easily sail out of cache get dragged back to the origin by a stray <code>Set-Cookie</code> or wrong <code>Cache-Control</code>, headers that are sometimes hard or impossible to strip or change on the origin itself, then Cache Response Rules is that fix, applied at exactly the right moment.  </p><h2>When and how caching decisions are made</h2><p>A CDN cache and an origin server work as a pair. Their goal is to answer from the cache whenever possible and only go back to the origin when the edge can’t respond. Every point of cache hit ratio comes from getting that division of labor right. Check the cache when we shouldn&#x27;t, and we waste a lookup that was always going to miss. Check it too rarely, the origin serves traffic the edge should have absorbed, and the performance win evaporates.</p><p>Importantly, the origin guides the cache. When it returns a cacheable asset, its response headers tell Cloudflare how long it’s OK to serve it, when and how to revalidate, and even whether to cache it at all. The cache is only ever as efficient as the origin allows. If the origin gets it wrong, cache becomes decoration while the origin infrastructure costs skyrocket.</p><!-- unsupported block: image --><p>Most cache eligibility problems are not decided at request time. They manifest after the origin replies.</p><p>A visitor asks for <code>/static/app.js</code>. Cloudflare checks cache, it misses, and forwards the request to the origin. The origin returns the file. Somewhere in those response headers, quietly, is a <code>Set-Cookie</code> header. The asset that should have been cached at every Cloudflare data center is now uncacheable. Multiply that by every visitor, on every site, with the same accidental header, and you have a cache hit ratio that is leaking origin bandwidth, ruining performance, and driving infrastructure costs higher.</p><p>There is a long list of variations of this same problem. The origin sends <code>Cache-Control: no-cache</code> on assets that are perfectly safe to cache on the CDN. The origin sends correct directives, but they&#x27;re meant for the browser, not for Cloudflare. Or the origin attaches an <code>ETag</code> (an identifier for a specific version of a resource) that&#x27;s overly aggressive and causes revalidation thrash on every conditional request. Often, especially in large teams, those managing the origin’s responses and the group that manages the CDN are different. This makes changing a one-line header a weeks-long negotiation.</p><p>None of these problems can be solved at request time. By the time Cloudflare sees the <code>Set-Cookie</code> on <code>/app.js</code>, the request phase is over. The response is already in flight.</p><p>So we put a fix in the right place.</p><p><strong>Cache Response Rules run after the origin’s response arrives at Cloudflare, but before it gets written to cache.</strong> With them, you can rewrite <code>Cache-Control</code> directives, manage cache-tags, and strip headers like <code>Set-Cookie</code>, <code>ETag</code>, and <code>Last-Modified</code> from the origin response before Cloudflare&#x27;s cache ever sees them. The fix lives entirely on Cloudflare. No origin code changes required.</p><h2>The missing piece</h2><p>If you&#x27;ve used Cloudflare for any length of time, you&#x27;ve watched the cache control surface area evolve. A few years ago, most of this lived inside <a href="https://developers.cloudflare.com/rules/page-rules/"><span style="text-decoration:underline">Page Rules</span></a>, which operated as a single, albeit overloaded, primitive that mixed caching with redirects, security, and a dozen other behaviors, all evaluated on requests. We later <a href="https://blog.cloudflare.com/future-of-page-rules/"><span style="text-decoration:underline">split these rules apart</span></a> so that only relevant behavior changes would be evaluated against requests, reducing unnecessary latency and allowing for complex rule stacking between behaviors. <a href="https://developers.cloudflare.com/cache/how-to/cache-rules/"><span style="text-decoration:underline">Cache Rules</span></a> became a dedicated, expressive rule type for caching decisions, joined by <a href="https://developers.cloudflare.com/cache/concepts/cdn-cache-control/"><span style="text-decoration:underline">CDN-Cache-Control</span></a>, <a href="https://developers.cloudflare.com/cache/concepts/cache-control/"><span style="text-decoration:underline">Origin Cache Control</span></a>, <a href="https://developers.cloudflare.com/cache/how-to/cache-rules/examples/custom-cache-key/"><span style="text-decoration:underline">custom cache keys</span></a>, and other controls that give you precise ways to tell Cloudflare what&#x27;s safe to cache, for how long, and under what conditions.</p><p>Many of these controls share a common trait: they operate on the <em>request</em>.</p><p>While maybe unintuitive, this makes sense. The most important caching decisions Cloudflare has to make are <em>should we look this up in cache, and under what key?</em> This question has to be answered <em>before</em> Cloudflare talks to the origin. If the answer is &quot;no&quot; when it should have been &quot;yes,&quot; the request has already paid for the origin round trip and there&#x27;s nothing the response can do to give it back that latency. Request-time rules answer that question using the only information available at that moment: the URL, the requested file’s extension, the request headers, geography, device type, and so on. Using these request parameters and the rules set to change these request parameters, we determine if something is likely in cache and look there before talking with the origin. </p><!-- unsupported block: image --><p>But some cache decisions cannot be made on the request. The origin&#x27;s <code>Cache-Control</code> directives (cache-control: max-age=3600) are part of the response Cloudflare receives from the origin. The status code, the ETag, the Last-Modified timestamp, Set-Cookie, and the cache-tag format the origin chooses are all things in the response that the origin passes to the cache. None of it is available when request-time rules run.</p><p>Responses from the origin are the source of truth, so if something needed to be changed on Cloudflare that wasn’t available at request time, that previously left you with three workarounds:</p><ol><li>Change the origin.</li><li>Write a Worker that re-fetches and rewrites the response.</li><li>Live with a worse hit ratio.</li></ol><p>Each of those costs engineering time, adds latency, or burns money. <strong>Cache Response Rules give you a fourth option: a ruleset where you can modify the origin&#x27;s response before it hits Cloudflare&#x27;s cache.</strong> </p><h2>Two phases, two questions</h2><p>The cleanest way to think about the difference between <a href="https://developers.cloudflare.com/cache/how-to/cache-rules/"><strong><span style="text-decoration:underline">Cache Rules</span></strong></a> and the new <a href="https://developers.cloudflare.com/cache/how-to/cache-response-rules/"><strong><span style="text-decoration:underline">Cache Response Rules</span></strong></a> is as two phases, each answering its own question. </p><ul><li><strong>Cache Rules</strong> <strong>run in the request phase, </strong>before Cloudflare talks to the origin server. They answer: <em>given this request, should Cloudflare cache the response, and under what cache key? </em>That’s three decisions that can be made at request time with cache rules: <strong>whether</strong> to cache (eligible vs. bypass), <strong>what</strong> the object is (the cache key and how to identify the stored object in the future), and <strong>how</strong> to cache (edge TTL, browser TTL, serve-stale, etc). All of these questions must be settled before the origin fetch, using only what’s in the request. </li></ul><p></p><!-- unsupported block: image --><ul><li><strong>Cache Response Rules run in the response phase,</strong> after the origin replies but before the response is written to Cloudflare&#x27;s cache. Cache Response Rules answer: <em>now that the origin has responded, should we adjust how we cache it? </em>Cache Response Rules can rewrite the <strong>how </strong>from the request by<em> </em>stripping headers that would make a response ineligible for cache,<em> </em>changing origin <code>cache-control</code> directives that tell Cloudflare whether and how to cache<em>, or </em>setting cache tags for purging content. When a cache rule and cache response rule are in conflict with each other, the cache response rule wins. </li></ul><!-- unsupported block: image --><p>The response phase can&#x27;t do everything the request phase can. It can&#x27;t change the <strong>what,</strong> as the key is already fixed. Though it can change <strong>whether </strong>and<strong> how </strong>Cloudflare caches, for example, a response rule can set <code>no-store</code> to make a cacheable object non-cacheable or strip a <code>Set-Cookie</code> to make non-cacheable content eligible for cache. However, Cache Response Rules can&#x27;t make a previously ineligible request cacheable by the response phase, because it’s too late.</p><p>So Cache Response Rules don&#x27;t replace Cache Rules. Cache Rules decide <strong>whether</strong>, <strong>what</strong>, and <strong>how </strong>we cache on the request. Cache Response Rules get the final word on the <strong>how </strong>and<strong> whether</strong>, once the origin&#x27;s response is seen by Cloudflare in a phase that didn&#x27;t exist before.</p><h2>What you can do</h2><p>Cache Response Rules support three actions today.</p><ol><li><strong>Strip headers that break caching</strong></li></ol><p>The <code>set_cache_settings</code> action removes things like <code>Set-Cookie, ETag</code>, or <code>Last-Modified</code> from the origin response before Cloudflare evaluates it for caching:</p><!-- unsupported block: code --><p>This is the fix for the <code>Set-Cookie-on-a-static-asset</code> problem. Origin frameworks frequently attach session cookies to every response (for load balancing or other purposes), including responses for assets that users don&#x27;t want to be associated with a session. Stripping <code>Set-Cookie</code> in the response phase makes those assets cacheable again without asking the origin, load balancer, or anything else upstream to change anything.</p><p>Cache Response Rules also run on responses that aren&#x27;t eligible for caching at all. So when you strip Set-Cookie from a dynamic response, for instance, the rule fires whether or not the object is ever stored. This allows you to control what the client sees even when the response is not stored in cache. You can also strip ETag and <code>Last-Modified</code>, which is useful when those headers are misconfigured at the origin and causing thrash. This comes with a tradeoff: stripping both ETag and Last-Modified enables <a href="https://blog.cloudflare.com/introducing-smart-edge-revalidation/"><span style="text-decoration:underline">Smart Edge Revalidation</span></a> for that response. If Cache Response Rules then <em>add</em> new validators, Cloudflare will not enable a Smart Edge Revalidation for browser conditional requests.</p><p>So stripping and modifying headers, even on dynamic requests, are a powerful new pattern that did not exist before on Cloudflare, but there can be additional configs to be aware of if you make these changes.   </p><ol><li><strong>Manage cache tags</strong></li></ol><p><code>set_cache_tags</code> lets you <code>add</code>, <code>remove</code>, or <code>set</code> cache tags used for <a href="https://developers.cloudflare.com/cache/how-to/purge-cache/purge-by-tags/"><span style="text-decoration:underline">purge by tag</span></a> on the response. Tags can be static:</p><p></p><!-- unsupported block: code --><p>Or computed from a response header expression:</p><!-- unsupported block: code --><p>That second form is the one that earns its keep during a CDN migration. If your previous CDN attached surrogate keys to responses using a header like <code>Surrogate-Keys</code> with a comma delimiter, you can translate them into Cloudflare&#x27;s <code>Cache-Tag</code> format directly in the response phase.</p><p>The third argument to split() is the limit: the maximum number of elements in the resulting array can be between 1 and 128. Use a value comfortably larger than the realistic tag count per response. The value 1 would return the entire header as a single tag. Once tags exist on the response, <a href="https://blog.cloudflare.com/instant-purge/"><span style="text-decoration:underline">purge-by-tag</span></a> just works (and by “works” we mean usually in <a href="https://blog.cloudflare.com/instant-purge/"><span style="text-decoration:underline">under 150 ms, globally</span></a>).</p><ol><li><strong>Modify Cache-Control directives</strong></li></ol><p><code>set_cache_control</code> is the action that does the most heavy lifting. You can set or remove individual directives:</p><ul><li>Duration directives: <code>max-age</code>, <code>s-maxage</code>, <code>stale-if-error</code>, <code>stale-while-revalidate</code></li><li>Qualified directives: <code>private</code>, <code>no-cache</code> (with optional header-name qualifiers)</li><li>Boolean directives: <code>no-store</code>, <code>no-transform</code>, <code>must-revalidate</code>, <code>proxy-revalidate</code>, <code>must-understand</code>, <code>public</code>, <code>immutable</code></li></ul><p>For each directive you can also set <code>cloudflare_only</code>: true, which is the part that often surprises people:</p><!-- unsupported block: code --><p>When <code>cloudflare_only</code> is true, the directive affects how Cloudflare caches the response, but the downstream Cache-Control value sent to the browser is left alone. This is the response-phase version of what <code>CDN-Cache-Control</code> gives you at the origin: cache the asset for 24 hours at Cloudflare, but tell the browser something else. The difference is that you&#x27;re now doing it from a Rules surface in the Cloudflare dashboard, rather than asking the origin to set a separate header.</p><h2>Examples worth stealing</h2><p>Below are some examples we thought could help show you the power of Cache Response Rules. Try them out, remix them, and share with others in the community so you can see how to achieve powerful cache customizations using Cache Rules with Cache Response Rules. More examples can be found in the <a href="https://developers.cloudflare.com/cache/how-to/cache-response-rules/create-api/#example-requests"><span style="text-decoration:underline">docs</span></a>.</p><h3><strong>Strip Set-Cookie from static asset extensions</strong></h3><!-- unsupported block: code --><p><em>Why it works</em>: The most common cause of &quot;this should be cacheable but isn&#x27;t&quot; is a session middleware on the origin that attaches a <code>Set-Cookie</code> to <em>every</em> response. Stripping it for known-static extensions converts those responses to cacheable without any origin change.</p><p><em>Caveat</em>: Only do this for asset types where the cookie is not semantically required. If your origin uses cookies to drive variant behavior on those URLs (very unusual but possible), strip selectively or scope stripping these headers by path.</p><h3><strong>Long-cache static assets in Cloudflare, shorter-cache them in the browser</strong></h3><!-- unsupported block: code --><p><em>Why it works</em>: Cloudflare holds the asset for a month and serves it from cache. Browsers see <code>max-age=86400</code> and re-validate after a day. You decouple the two cache lifetimes without touching the origin.</p><p><em>Caveat</em>: <code>immutable</code> tells browsers not to revalidate even on explicit refresh. Pair it only with versioned/hashed filenames.</p><h3><strong>Override no-cache on a known-static path</strong></h3><!-- unsupported block: code --><p><em>Why it works</em>: Framework defaults sometimes attach <code>no-cache</code> to every response. If you know /static/* is safe to cache, you can strip the directive and impose your own TTL (at Cloudflare only), without changing what the origin or any downstream cache sees.</p><p><em>Caveat</em>: Be honest about what&#x27;s actually static. If <code>/static/</code> is sometimes used to serve user-specific content, narrow the match (extension, response header signal, content type).</p><h3><strong>Translate cache tags during a CDN migration</strong></h3><!-- unsupported block: code --><p><em>Why it works</em>: A lot of CDN migration pain comes from origin tooling that emits cache-tag headers in another vendor&#x27;s format. Instead of asking the origin team to ship a release that adds Cloudflare&#x27;s <code>Cache-Tag</code>, translate the existing header in the response phase. Purge-by-tag on Cloudflare starts working immediately.</p><p><em>Caveat</em>: The third argument to split() is the limit (1–128) on the resulting array size, not anything to do with the separator. </p><h2>How to use Cache Response Rules</h2><p><strong>Dashboard</strong></p><ol><li>Go to <strong>Cache</strong> &gt; <strong>Cache Rules</strong> in the Cloudflare dashboard.</li><li>Select Create rule and then <strong>Cache Response Rule</strong>.</li><li>Choose a name and an expression. The expression builder exposes both request fields and response fields. </li><li>Choose an action: <strong>Modify cache-control directives</strong>, <strong>Modify cache tags</strong>, or <strong>Strip headers</strong>.</li><li>For directives, toggle <strong>Cloudflare only</strong> when you want the change to apply only to Cloudflare&#x27;s view of the asset.</li><li>Save as a draft to iterate, or deploy directly.</li></ol><!-- unsupported block: image --><h3><strong>API</strong></h3><p>Rules in this phase live at:</p><!-- unsupported block: code --><p>For further information about how to use Cache Response Rules including API and terraform examples, please see the <a href="https://developers.cloudflare.com/cache/how-to/cache-response-rules/create-api/"><span style="text-decoration:underline">documentation</span></a>.</p><h2>Use Cache Response Rules today</h2><p>Cache Rules saw the request. Cache Response Rules see the response. Both give you even more control to build the perfect cache on Cloudflare. They are available on all plans today — go <a href="http://dash.cloudflare.com/?to=/:account/:zone/caching/rules/response-rules/new"><span style="text-decoration:underline">try them</span></a> out!</p>]]></content:encoded>
      <guid isPermaLink="false">01KY05J6W4B4TF7X9348W51RWP</guid>
    </item>
    <item>
      <title>How the 2026 World Cup affected Internet traffic</title>
      <link>https://blog.cloudflare.com/2026-world-cup-internet-traffic/</link>
      <pubDate>Tue, 21 Jul 2026 12:59:40 GMT</pubDate>
      <description>We analyzed global HTTP traffic to explore how kickoff times, streaming habits, and hydration breaks reshaped online activity worldwide. From late-night traffic surges to halftime browsing spikes, here is how the world connected during the global tournament. </description>
      <dc:creator>Sabina Zejnilovic</dc:creator>
      <dc:creator>Francisco Contente</dc:creator>
      <dc:creator>Lai Yi Ohlsen</dc:creator>
      <category>Internet Traffic</category>
      <category>Radar</category>
      <content:encoded><![CDATA[<p>For 96 years, the World Cup has been a global phenomenon, uniting nations and communities through a shared love of sportsmanship. While its popularity is nothing new, what is novel today is how rare a truly collective global experience has become. In an era defined by microtrends and algorithmic bubbles, it is increasingly uncommon for people across most countries to engage in the exact same event.  </p><p>That is precisely the unifying power of the World Cup. Fans from all over the globe reshape their daily routines around these once-in-a-lifetime matchups and storylines — and because Cloudflare operates a global network with 330+ points of presence worldwide, we are in a unique position to see exactly how this global ritual reshaped the world’s online activity throughout June and July 2026. </p><p>Cloudflare Radar tracks HTTP traffic, DNS, security, and more to highlight global Internet trends. In this blog post we’ll use that data to explore how the World Cup impacted global traffic patterns throughout the tournament’s run. </p><h2>How did the World Cup change our behavior online? </h2><p>To understand how traffic changes throughout a match, we had to establish what it is “normally.” One way to do this is by looking at raw request volumes, or the amount of traffic we see on our network per country. But these amounts vary per country (the amount of daily traffic in the United States is always a larger number than the traffic in Portugal), which makes it difficult to establish a globally applicable baseline. Instead, we defined &quot;normal&quot; using the median traffic of the four preceding weeks: a month-long window that provided a stable, per-minute reference and smoothed out day-to-day noise.</p><p>We also wanted to know whether traffic rose or fell relative to that baseline, but a plain difference wouldn&#x27;t let us compare a high-volume country against a low-volume one. Instead, we used the ratio of current to baseline traffic, expressed as a log₂ value: the log makes increases and decreases symmetric around zero (+1 = twice normal, −1 = half). In other words, <strong>a score of zero means traffic is perfectly normal, a positive number shows a spike, and a negative number shows a drop.</strong></p><h2>Whether you’re staying up late or waking up early, kickoff time impacts traffic</h2><p>One factor shaping how traffic changes is simply what time the match kicks off locally. The largest changes in activity happen when a match is played in the overnight and early-morning hours — roughly midnight to 8am local time. These are the hours when very few people are normally online, so <strong>fans staying up (or waking early) to watch push traffic well above its usual level, more than doubling it in some cases</strong>. As the graph shows, this is where the deviation peaks on both workdays and weekends.</p><!-- unsupported block: image --><p>By contrast, matches played during normal daytime and working hours — around 9 a.m. to mid-afternoon —  don’t show such an impact: traffic stays close to its usual level, likely because the people watching would already have been online anyway. In the early evening there&#x27;s a smaller, second lift, most visible on weekdays, as a match keeps people connected at a time when usage would normally start to wind down. Weekends follow a similar shape, with the strong early-morning rise but a gentler evening bump.</p><!-- unsupported block: image --><p>The impact of kickoff time is easiest to see when comparing matches within a single country that take place at very different hours. Bosnia and Herzegovina provides a clear example. As seen in the graph shown above, when Bosnia played at 2 a.m. local time, people stayed awake to watch and traffic during the game jumped to well above its normal level, at times more than doubling. When Bosnia played in the evening, the opposite happened: traffic dipped below normal (falling to about 70% of typical value), as people put their devices aside and focused on the match itself.</p><p>When Brazil played Japan in the Round of 32 (Brazil won 2–1 on June 29, 2026), the two countries watched the very same game 12 hours apart: kickoff in Houston (GMT−5) fell during normal waking hours in Rio de Janeiro (GMT−3), while in Tokyo (GMT+9) it landed in the dead of night.</p><!-- unsupported block: image --><p>The result is two nearly parallel curves for the same 90 minutes: one higher than normal, one lower. Japan&#x27;s traffic (red) sits well above normal, around +1, roughly double its usual level, because the match aired in the small hours, when almost no one would ordinarily be online. Brazil&#x27;s traffic (green), by contrast, runs <em>below</em> normal, around −0.4, as the game fell in the middle of an ordinary active day. In this case,<strong> </strong>watching the match pulled people <em>away</em> from their usual browsing rather than adding to it. </p><h2>Which matches moved the Internet most? </h2><p>One of the most compelling aspects of the World Cup is seeing which storylines and teams capture the attention of fans across the world. We’ve discussed how regional traffic patterns change as a result of matches. But who are they watching? Which matches made the most impact on Internet traffic? </p><p>Here&#x27;s how we calculated this: for each match, we took the two-hour window after kickoff and, for every country with enough baseline traffic to give stable measurements (small, noisy markets are excluded), computed how far traffic strayed from normal. We then took the absolute value of each country&#x27;s deviation, so we&#x27;re measuring <em>how much</em> traffic changed, not in which direction (a surge and a drop both count as impact), and for each match we took the median of those absolute deviations across all countries. Because several group-stage matches were played simultaneously, making it impossible to attribute a country&#x27;s traffic swing to one game or the other, we dropped those concurrent matches to avoid ambiguity.</p><p>The result is this ranking of the matches that moved the Internet most, worldwide. And there&#x27;s a surprise: <strong>the very top spot wasn’t snagged by a final or semifinal. It was Argentina vs. Switzerland on July 11, a quarterfinal that saw Argentina win 3-1 — and that moved Internet traffic by a factor of about 1.26. </strong>That put it ahead of the France vs. Spain semifinal, which had a factor of 1.21.<strong> </strong>The rest of the top matches were a mix of quarterfinals, round-of-16 and even round-of-32 ties. </p><!-- unsupported block: image --><h3>The teams that moved the Internet: Argentina, followed by France, Spain and Norway </h3><p>To decide which team the world watched most, we looked at each team&#x27;s matches and aggregated the median worldwide impact across all countries. In other words, when a given team took the field, how much did the typical country&#x27;s traffic move away from normal? Not surprisingly, <strong>Argentina topped the list at 1.17x, meaning that when Argentina played, the typical country&#x27;s traffic swung about 17% away from its normal level, the strongest global pull of any team. </strong>This comes as no surprise, since they were the defending champions and each knockout game could have been Lionel Messi&#x27;s last dance for his national team. Love them or hate them, people were watching them.</p><p>Not far behind were nations packed with superstars such as France, Brazil, Portugal, Morocco, Spain — and Norway, fueled by the Erling Haaland phenomenon. Haiti and Iraq appear in the top as outliers due to their high deviation scores relative to their typical traffic, suggesting matches against major teams drove disproportionate engagement.</p><!-- unsupported block: image --><h2>Sharp increase in traffic to sports betting sites </h2><p>Compared to HTTP request data in the month preceding the World Cup, there was an overall increase in requests to gambling industry websites since the opening game. Additionally, whereas pre-tournament traffic followed a clear weekly pattern, after the Cup’s opening game, the trend flattened into a more constant profile, likely a consequence of the high, near-daily regularity of matches.</p><!-- unsupported block: image --><!-- unsupported block: image --><h2>Divergent Behavior: Why Traffic Patterns Varied by Country. </h2><p>Because Cloudflare is present in 120+ countries and handles traffic from Internet users worldwide, we can see distinct behavioral patterns across the globe. For example, when examining the deviation trends during the Algeria vs. Austria group stage game on June 28, we noticed something peculiar: Austria’s traffic (in red) <em>increased</em> during halftime, while Algeria&#x27;s (in green) decreased. The former follows the pattern described above of people spending more time online while not watching the game, while Algeria’s is the complete opposite — and they’re not the only ones. </p><!-- unsupported block: image --><p><em>Algeria, in green and denoted as DZ, saw a much higher uptick in Internet traffic during the match than Austria, in red.</em></p><h3>Countries clustered by behavior </h3><p>To understand patterns in behavior across countries we grouped every country&#x27;s match-day behavior by the shape of its traffic curve and let the patterns cluster together. </p><p>Grouping match-day traffic shapes this way, three distinct patterns emerge. The largest group (44 countries playing 101 matches) shows Internet usage rising during hydration breaks and halftime, the natural pauses in play, as people reach for their phones. A second, smaller group (8 countries playing 18 matches)) is its near mirror image: traffic falls at exactly those same moments, dipping during the breaks instead of climbing. The third group is a clear outlier, made up entirely of Iran&#x27;s three matches. The explanation is simple: the May baseline was measured while Iran was still coming back online after the shutdown, so its match-day traffic sits far above that depressed reference, producing a deviation unlike any other country&#x27;s. You can read more about Iran’s Internet shutdowns and partial restoration throughout 2026 <a href="https://blog.cloudflare.com/tag/internet-shutdown/"><span style="text-decoration:underline">on our blog</span></a>. </p><h3>Streaming makes some countries appear more online </h3><!-- unsupported block: image --><p>To better understand the second cluster, which included Algeria, Tunisia, Jordan, Egypt and DR Congo, we looked more closely at the traffic mix for these countries. We broke down traffic patterns by Multipurpose Internet Mail Extensions, or MIME type, and grouped it in families to easily distinguish clusters of content types. MIME types act like digital labels that tell browsers exactly what kind of file they are receiving, whether it&#x27;s an HTML page, a JPEG image, or an MP4 video stream. By tracking these labels, we can infer what kinds of content users are consuming. </p><p>Our hypothesis was that this behavior could be explained by a disproportionate amount of people watching the games via streaming in those countries. To test this, we compared traffic pattern distribution in games with teams of both clusters. In the following example, we see traffic distribution of Algeria and Austria respectively in the match between both countries.</p><!-- unsupported block: image --><p><em>In Algeria, traffic was far above normal, then dipped at halftime. Note the large increase in streaming traffic, in orange.</em></p><!-- unsupported block: image --><p><em>In Austria, where streaming services were used less, Internet traffic increased at halftime.</em></p><p>In the Algeria graph above, we can see that the bulk of the increase during the match window indeed was driven by requests to multimedia and streaming services. This supports our hypothesis that the traffic trendlines correlate with use of streaming to watch the match.</p><p>In Algeria, traffic rose sharply at kickoff, dropped during half-time, and returned to elevated levels once the second half began. Hydration breaks, by contrast, had little to no visible effect, which suggests that viewers don&#x27;t meaningfully change their Internet or social behavior for short, in-play pauses, but do so during the longer halftime interval. Other countries in this cluster show similar behavior. This might be because a viewer is unlikely to close a stream for a three-minute cooling break, but a fifteen-minute halftime is long enough to close the stream and step away. </p><h3>What do people do during halftime? </h3><p>A minority of countries, including Tunisia and Algeria, disconnect during halftime, with traffic dropping below its in-play level (the blue boxes, sitting under the 1.0 line). The majority of countries go the other way: traffic rises during the break as people pick up their phones the moment play stops, then settles again when the second half begins. Croatia and Bosnia and Herzegovina show this most strongly, with halftime traffic running well above their in-play baseline.</p><!-- unsupported block: image --><h3>And hydration breaks? </h3><p>Halftime is a long, familiar pause, but what about the much shorter hydration breaks? These last only about three minutes, taken midway through each half. Is three minutes really enough to change how people behave online? It turns out it is. Just as at halftime, the moment play briefly stops, traffic in most countries ticks up before falling again when the game resumes.</p><p>We measured this by taking, for each match, the peak number of requests in a window around the middle of each half (where the hydration break falls) and comparing it to the five minutes immediately before the break. The pattern lines up with everything we&#x27;ve seen so far: the same audiences that surge at halftime also spike during these brief pauses, while the few countries that tend to disconnect during breaks show little or no lift. Even a three-minute gap in play is enough for a large share of viewers to glance back at their phones.</p><!-- unsupported block: image --><h2>The final matches </h2><p>At the scale of Radar’s global HTTP requests, it is genuinely hard for any single event to leave a visible mark. Even so, the 2026 World Cup Final, which pitted Europe’s and American football champions against each other, had enough social impact to affect the Internet’s footprint. When looking at the volume of HTTP bytes from July 18 to July 20 we can immediately identify the final match kicking off at 21:00 UTC on July 19, as well as England vs. France for the Bronze medal at 23:00 UTC on July 18.</p><!-- unsupported block: image --><p>During the final match, Argentina and Spain&#x27;s traffic volume increased up to 20 percentage points when compared to a similar period. The bronze medal match also coincided with a traffic increase, although at a smaller scale.</p><!-- unsupported block: image --><p>HTTP request volume during the final match also appeared correlated not only with the timeline of the game, but with each individual stage as well. Looking at the graph, we can roughly pinpoint moments such as the kickoff, halftime break, hydration breaks, as well as the final whistle.</p><!-- unsupported block: image --><h2>Keep up with world events on Cloudflare Radar</h2><p>Across every time zone, match, and goal, Cloudflare Radar provides a front-row seat to how the world connects during landmark cultural moments. To explore more interactive traffic insights and track how major worldwide events shape internet activity every day, visit <a href="https://radar.cloudflare.com/"><span style="text-decoration:underline">Cloudflare Radar</span></a> or follow us on social media at <a href="https://twitter.com/CloudflareRadar"><span style="text-decoration:underline">@CloudflareRadar</span></a> (X), <a href="https://noc.social/@cloudflareradar"><span style="text-decoration:underline">https://noc.social/@cloudflareradar</span></a> (Mastodon), and <a href="http://radar.cloudflare.com"><span style="text-decoration:underline">radar.cloudflare.com</span></a> (Bluesky).</p>]]></content:encoded>
      <guid isPermaLink="false">01KY2432VQPD5FTF2RARXXYQGH</guid>
    </item>
    <item>
      <title>Cloudflare Internal DNS is now generally available</title>
      <link>https://blog.cloudflare.com/internal-dns/</link>
      <pubDate>Mon, 20 Jul 2026 20:59:31 GMT</pubDate>
      <description>Cloudflare Internal DNS brings authoritative and recursive DNS for private networks to the same global network and control plane that runs Cloudflare's Zero Trust, networking, and public DNS.</description>
      <dc:creator>Enrique Somoza</dc:creator>
      <dc:creator>Hannes Gerhart</dc:creator>
      <category>Cloudflare Gateway</category>
      <category>DNS</category>
      <category>General Availability</category>
      <category>Internal DNS</category>
      <category>Networking</category>
      <category>Product News</category>
      <category>SASE</category>
      <category>Zero Trust</category>
      <content:encoded><![CDATA[<p>Starting today, Cloudflare Internal DNS is generally available. Cloudflare Internal DNS provides authoritative and recursive DNS for private networks on the same global network and control plane customers already use for public DNS, Zero Trust, networking, and application services.</p><p>Internal DNS — sometimes also referred to as private DNS — is one of the last pieces of enterprise infrastructure still managed separately from the rest of the network. Many organizations operate one platform for public DNS, another for internal DNS, and use cloud-native DNS services inside each cloud environment with separate security policies layered on top. None of these systems share a common control plane. Split-horizon DNS adds another layer of complexity, often requiring multiple DNS environments to remain synchronized so internal and external users receive different answers for the same hostname. When those systems drift, outages follow.</p><p>With Cloudflare Internal DNS, you get a single platform to manage public and private DNS resources, enforcing <a href="https://developers.cloudflare.com/cloudflare-one/traffic-policies/dns-policies/"><span style="text-decoration:underline">DNS policies</span></a> and gaining visibility across your entire DNS stack. For Enterprise customers, this is included with <a href="https://www.cloudflare.com/products/gateway/"><span style="text-decoration:underline">Cloudflare Gateway</span></a> without any additional charge.</p><h2>Why customers are adopting Internal DNS</h2><p><strong>Consolidate DNS operations</strong>. Public and private DNS run on one platform, with one API, one audit trail, and one place to set policy. The appliance refresh cycle and the scaling bottlenecks that came with legacy DNS go away.</p><p><strong>Simplify split-horizon DNS</strong>. Internal and external resolution are defined as separate views over shared zones, managed from a single control plane. There are no parallel systems to keep in sync, so there&#x27;s no drift to chase down.</p><p><strong>Extend Zero Trust to DNS</strong>. Resolver policies decide which users and devices resolve against which view, enforced by the same Cloudflare Gateway that already governs the rest of your traffic. Private name resolution stops being the gap in an otherwise Zero Trust architecture.</p><p><strong>Modernize legacy infrastructure</strong>. Retire hardware appliances, legacy DNS servers, and cloud-locked resolvers. Cloudflare Internal DNS runs on the infrastructure behind 1.1.1.1, with no hardware to rack and no capacity to provision.</p><h2>What we built</h2><p>Cloudflare Internal DNS consists of two components: <strong>Gateway Resolver</strong> and <strong>Internal Authoritative DNS</strong>. Authoritatively managing zones is a different job from enforcing DNS security and routing policies.</p><!-- unsupported block: image --><p>The Gateway Resolver handles recursive resolution and policy evaluation. <a href="https://blog.cloudflare.com/introducing-cloudflare-for-teams/"><span style="text-decoration:underline">Launched in 2020</span></a> and powered by 1.1.1.1 for public resolution, it comes with a built-in policy engine that can filter DNS queries and redirect queries to different upstream sources — all based on <a href="https://developers.cloudflare.com/cloudflare-one/traffic-policies/expression-syntax/"><span style="text-decoration:underline">flexible expressions</span></a>, with comprehensive logging and audits feeding a single pane of glass.</p><p>Internal Authoritative DNS serves records for internal zones built on the same authoritative platform Cloudflare has operated for over a decade and that <a href="https://w3techs.com/technologies/overview/dns_server"><span style="text-decoration:underline">serves more domains</span></a> than any other provider.</p><p>There are three primary objects customers work with:</p><ul><li><strong>Internal Zones</strong> hold the authoritative records for private resources: environment-specific apps, service endpoints, databases.</li><li><strong>DNS Views</strong> group zones into the resolution context a given set of users or devices should see. This is what makes split-horizon work without parallel systems.</li><li><strong>Resolver Policies</strong> sit in Gateway and route matching queries to a specific view.</li></ul><p>Zone references let administrators reuse a shared zone across multiple views rather than copying its records into each one. A common zone like intranet.local is defined once and referenced everywhere it&#x27;s needed, which is the difference between a Don&#x27;t-Repeat-Yourself configuration and the duplicated, drift-prone setup that split-horizon usually forces.</p><h3>How a query resolves</h3><p>A DNS query from a client first hits the Gateway Resolver, where policy is evaluated. From there, one of three things happens. If a resolver policy matches and points at an internal view, the query is routed to Internal Authoritative DNS and answered from the matching view&#x27;s zones. If policy blocks the query, it is dropped at the resolver. Otherwise, the query follows the public path, with 1.1.1.1 resolving it against the public DNS hierarchy. Views can also fall back to public resolution when a name isn&#x27;t found internally, so a single resolver can serve both private and public names without the client needing to know which is which.</p><!-- unsupported block: image --><h3>How a change propagates</h3><p>Record changes follow a predictable, high-speed path from input to edge.</p><p>Every change enters through the same DNS Records API, whether it originates in the dashboard, in Terraform, or in a direct API call. That unified ingress means there is exactly one write path to reason about and audit, regardless of how the change was made. The change is persisted in Cloudflare&#x27;s core data centers for durability and validated before it propagates.</p><p>From there, changes replicate across Cloudflare&#x27;s global network and affected cached entries are invalidated as the updates arrive, so edited records take effect in seconds rather than waiting on TTL expiry.</p><h3>Getting started</h3><p>If you&#x27;re an Enterprise customer using Cloudflare Gateway, you have access to Internal DNS today. Open the Cloudflare dashboard, navigate to Networking, then <a href="https://dash.cloudflare.com/?to=/:account/internal-dns"><span style="text-decoration:underline">Internal DNS</span></a>.</p><p>Setting up Internal DNS typically takes three steps: create a zone, create a view, and define a resolver policy that determines which users and devices should resolve against that view.</p><p>Create an internal zone and your first internal record:</p><!-- unsupported block: code --><p>Then create a DNS view and link your zone to it:</p><!-- unsupported block: code --><p>Finally, <a href="https://developers.cloudflare.com/cloudflare-one/traffic-policies/resolver-policies/#create-a-resolver-policy"><span style="text-decoration:underline">create a Gateway resolver policy</span></a> in the <a href="https://dash.cloudflare.com/?to=/:account/one/traffic-policies/resolver-policies"><span style="text-decoration:underline">Zero Trust dashboard</span></a> that routes matching traffic to your view. <a href="https://developers.cloudflare.com/cloudflare-one/networks/resolvers-and-proxies/dns/locations/"><span style="text-decoration:underline">Create a Gateway location</span></a>, set your conditions, select Internal DNS View as the resolution method, and choose your view. That&#x27;s it. Queries matching your policy now resolve against your internal zones.</p><p>Terraform support is available, and because Terraform writes through the same DNS Records API as everything else, infrastructure-as-code changes follow the identical ingestion and propagation path. Full documentation and end-to-end configuration examples are available in <a href="https://developers.cloudflare.com/dns/internal-dns/get-started/#manage-with-terraform"><span style="text-decoration:underline">our developer documentation</span></a>.</p><h3>Internal DNS as part of the Connectivity Cloud</h3><p>Internal DNS works with any Cloudflare connectivity method that routes DNS traffic through the Gateway Resolver, including the Cloudflare One Client (formerly WARP), DNS over HTTPS (DoH), DNS over TLS (DoT), standard DNS on port 53, PAC file deployments, and Cloudflare WAN.</p><p>For organizations running Cloudflare WAN, every device on the connected network can resolve internal hostnames through Cloudflare without requiring the Cloudflare One Client on individual devices. The result is a consistent DNS experience across remote users, branch offices, data centers, and cloud environments using a single control plane.</p><p>More importantly, Internal DNS is not a standalone DNS service. It extends the same Connectivity Cloud platform that organizations already use to secure users with Zero Trust, connect networks with Cloudflare WAN, accelerate applications, and protect Internet-facing services.</p><p>Bringing private DNS onto the same global network as everything else is just the starting point. Tighter integration across DNS, networking, and Zero Trust policy is where this goes next — so resolving an internal hostname, reaching the service behind it, and enforcing who is allowed to access it become decisions made through a single platform, rather than multiple disconnected systems.</p><p>Ready to consolidate your DNS? Open the dashboard, head to Networking, then <a href="https://dash.cloudflare.com/?to=/:account/internal-dns"><span style="text-decoration:underline">Internal DNS</span></a>, and create your first zone today. Questions or want to compare notes with other operators? Join the conversation in the <a href="https://community.cloudflare.com"><span style="text-decoration:underline">Cloudflare Community</span></a>.</p>]]></content:encoded>
      <guid isPermaLink="false">01KXNA2XDCKTZTZQ1NC6HM40AZ</guid>
    </item>
    <item>
      <title>Cloudflare WAF protects WordPress applications from two high-severity vulnerabilities </title>
      <link>https://blog.cloudflare.com/wordpress-vulnerabilities/</link>
      <pubDate>Fri, 17 Jul 2026 21:30:43 GMT</pubDate>
      <description>Cloudflare has deployed two WAF rules in response to high-severity vulnerabilities disclosed to us by the WordPress security team. The new rules protect all Cloudflare customers using affected WordPress versions, but customers should still update immediately to a patched release</description>
      <dc:creator>Daniele Molteni</dc:creator>
      <dc:creator>Ah-young Choi</dc:creator>
      <dc:creator>Georgie Yoxall</dc:creator>
      <dc:creator>Kuber Nandwani</dc:creator>
      <dc:creator>Vikram Grover</dc:creator>
      <category>CVE</category>
      <category>Vulnerabilities</category>
      <category>WAF</category>
      <category>WordPress</category>
      <content:encoded><![CDATA[<p>Cloudflare has deployed new Web Application Firewall (WAF) protections for two critical vulnerabilities affecting WordPress. The protections address an Unauthenticated Remote Code Execution (RCE) vulnerability in WordPress&#x27;s REST API and a related SQL Injection vulnerability.</p><p>The WordPress security team disclosed the vulnerabilities to Cloudflare before public release so that we could prepare protections for customers. <strong>Cloudflare has deployed the new rules to protect all customers, including those on free and paid plans, as long as their application traffic is proxied through the Cloudflare WAF.</strong> The rules were deployed at 17:03 UTC on July 17 2026.</p><p>WAF protections reduce exposure while customers update, but they are not a substitute for patching. WordPress has released fixes in version 7.0.2, with backports to affected earlier branches: 6.9.5, 6.8.6, and 7.1 Beta 2 (<a href="https://wordpress.org/news/2026/07/wordpress-7-0-2-release/"><span style="text-decoration:underline">see release details</span></a>). Versions earlier than 6.8 are not affected. WordPress is treating this as its highest-severity, highest-priority class of issue and is forcing automatic updates to affected sites, so most sites will be updated automatically. We still recommend confirming that you are on a patched release or the backports for your branch and follow the guidance in the official WordPress security release <a href="https://wordpress.org/news/2026/07/wordpress-7-0-2-release/"><span style="text-decoration:underline">announcement</span></a>. </p><h3>What you need to know</h3><p>The vulnerabilities affect different parts of the request path:</p><ul><li><strong>CVE-2026-60137: SQL injection. </strong>A vulnerability in WordPress version 6.8 and later allows crafted input to alter a database query. Rating High.</li><li><strong>CVE-2026-63030: Unauthenticated remote code execution. </strong>A vulnerability in WordPress version 6.9 and later allows an unauthenticated attacker to execute code through the batch endpoint of the REST API when a persistent object cache is not in use. This vulnerability is related to the SQL injection described above. No login or user interaction is required to exploit this vulnerability. Rating Critical.</li></ul><p>The SQL injection vulnerability is present from version 6.8 onwards, while the RCE only affects versions from 6.9. So 6.8.6 addresses the SQLi only since the RCE isn&#x27;t present on 6.8, while 6.9.5, 7.0.2, and 7.1 Beta 2 get fixes for both.</p><p>Cloudflare created two rules to detect requests associated with these vulnerabilities:</p><!-- unsupported block: table --><p>Cloudflare customers running WordPress sites on Pro, Business, or Enterprise plans should ensure that Cloudflare Managed Rules are enabled. Customers can follow the steps in our <a href="https://developers.cloudflare.com/waf/get-started/#1-deploy-the-cloudflare-managed-ruleset"><span style="text-decoration:underline">WAF Managed Rules documentation</span></a>. Customers on free plans are automatically protected through the Free Ruleset.</p><p>The new rules are deployed with the default Managed Ruleset action of Block. Customers running WordPress sites should review any ruleset-level overrides, including those that change all rules from Block to Log, and ensure the new rules use the recommended action while they update WordPress. Cloudflare customers should also monitor Security Events for requests matching either rule. </p><h3>Defense in depth while you patch</h3><p>The SQL injection rule detects crafted parameter values before they reach WordPress. The unauthenticated RCE rule targets requests attempting to reach the remote code execution path. Together, they detect the attack at two different points.</p><p>These rules reduce risk while organizations update affected systems; they do not fix the underlying vulnerable code. Updating WordPress remains the most effective way to address the vulnerabilities.<br/>If an immediate update is not possible, verify that both Cloudflare rules are active with the recommended action and review logs for suspicious requests to the affected REST API endpoint.</p><h3>Looking forward</h3><p>Cloudflare will monitor matching traffic and test the rules against new attack variations, updating detections when needed.</p><p>We thank the WordPress security team for coordinating with Cloudflare and other infrastructure providers to help protect users before details of the vulnerabilities became public.</p><!-- unsupported block: image --><p></p>]]></content:encoded>
      <guid isPermaLink="false">01KXQS1XDD7HKB324JEWG6BZDA</guid>
    </item>
    <item>
      <title>A broken DNSSEC rollover took down .al. Now 1.1.1.1 tells you when validation is bypassed</title>
      <link>https://blog.cloudflare.com/dnssec-nta-ede-33/</link>
      <pubDate>Tue, 14 Jul 2026 13:00:00 GMT</pubDate>
      <description>When a failed DNSSEC key rollover took down the .al TLD, we deployed a Negative Trust Anchor to restore resolution. This time, though, clients didn't have to take our word for it: 1.1.1.1 returned EDE 33, a new DNS error code that signals directly in the response that DNSSEC validation was bypassed.</description>
      <dc:creator>Sebastiaan Neuteboom</dc:creator>
      <category>1.1.1.1</category>
      <category>DNS</category>
      <category>DNSSEC</category>
      <category>Incident Report</category>
      <category>Reliability</category>
      <category>Standards</category>
      <content:encoded><![CDATA[<p>On July 3, 2026, the Albanian communications authority (AKEP), the operator of the <code>.al</code> country-code top-level domain (TLD) of Albania, attempted a DNSSEC key rollover. Something went wrong, resulting in DNSSEC validation failures. Any validating DNS resolver receiving these signatures was required by the DNSSEC specification to reject them and return errors to clients. That includes <a href="https://www.cloudflare.com/learning/dns/what-is-1.1.1.1/"><span style="text-decoration:underline">1.1.1.1</span></a>, the public DNS resolver operated by Cloudflare.</p><p>The <code>.al</code> TLD is the online home of Albanian government services, banks, and media; it ranks <a href="https://radar.cloudflare.com/tlds/al?dateRange=7d"><span style="text-decoration:underline">#191 on Cloudflare Radar&#x27;s TLD ranking</span></a>. Anyone trying to visit those sites, using a validating resolver, found them unreachable during the incident. The failure had the potential to affect every <code>.al</code> domain, regardless of where it was hosted or which authoritative nameservers served it.</p><p>Just two months earlier, a similar incident struck <code>.de</code>, the TLD of Germany. As we described in <a href="https://blog.cloudflare.com/de-tld-outage-dnssec/"><span style="text-decoration:underline">our blog post on the incident</span></a>, our response was to install a Negative Trust Anchor (NTA) for <code>.de</code>, temporarily suspending DNSSEC validation in 1.1.1.1 to keep domains reachable while the registry resolved the issue. We did the same for .<code>al</code>.</p><p>NTAs restore resolution, but silently. A client receiving a response served under an NTA has no way to tell, from the response alone, that DNSSEC validation was bypassed, leaving it unable to distinguish a legitimate answer from a spoofed one. For the <code>.al</code> incident, 1.1.1.1 addressed that gap for the first time, returning a new Extended DNS Error (EDE) code alongside every affected response to signal that the answer was not DNSSEC-validated due to the presence of an NTA.</p><p>The graph below shows the SERVFAIL and NOERROR rates for <code>.al</code> queries on 1.1.1.1 throughout July 3. The SERVFAIL rate climbs as cached records expire and resolvers are forced to revalidate. It drops sharply when the NTA is applied at 17:15 UTC, restoring resolution.</p><!-- unsupported block: image --><h3>What happened to <code>.al</code></h3><p>We discussed how DNSSEC works in more detail in <a href="https://blog.cloudflare.com/de-tld-outage-dnssec/#how-dnssec-works"><span style="text-decoration:underline">our prior blog post</span></a>. A brief recap:</p><p>DNSSEC builds a chain of trust from the root zone down to individual domain names. The root zone holds a Delegation Signer (DS) record for each signed TLD, a fingerprint of that TLD&#x27;s DNSKEY. A resolver verifying <code>.al</code> checks that the DNSKEY served by <code>.al</code>&#x27;s nameservers matches the DS record in the root. If it does, the resolver trusts that DNS responses from <code>.al</code>&#x27;s nameservers are authentic. The same pattern repeats one level down: <code>.al</code> holds DS records for its signed child zones, each with a matching DNSKEY. A break anywhere in that chain, such as a DS record pointing to a key that no longer exists, causes validation to fail for everything below it.</p><p>Before the incident, the root zone held a DS record matching the DNSKEY served by the <code>.al</code> nameservers, as illustrated below.</p><!-- unsupported block: image --><p>At around 14:15 UTC, the <code>.al</code> operator published a new DNSKEY and stopped serving the old one. The DS record in the root zone still pointed to the old DNSKEY (id=26319), so any resolver attempting to validate <code>.al</code> responses found no matching key and failed.</p><!-- unsupported block: image --><p>At roughly 17:00 UTC, the <code>.al</code> operator removed the new DNSKEY without restoring the old one. The zone now had no DNSKEY records at all, while the DS record in the root still pointed to id=26319, and resolution continued to fail.</p><!-- unsupported block: image --><p>At roughly 19:15 UTC, the <code>.al</code> operator removed the DS record from the root zone. Without a DS record, resolvers no longer expected DNSSEC validation for <code>.al</code>, and resolution was restored, though the entire TLD was now unsigned.</p><!-- unsupported block: image --><p>As of publishing, <code>.al</code> remains unsigned. The DS record has not been restored to the root zone by the <code>.al</code> operators. Without a DS record, every <code>.al</code> domain is unable to use DNSSEC protections.</p><h3>Why Negative Trust Anchors are used</h3><p>Having a broken DNSSEC configuration can be painful, especially when it impacts an entire TLD at once. As we covered in our <code>.de</code> <a href="https://blog.cloudflare.com/de-tld-outage-dnssec/#negative-trust-anchors">incident blog</a>, recursive DNS operators can install a Negative Trust Anchor (NTA) as defined in <a href="https://datatracker.ietf.org/doc/html/rfc7646"><span style="text-decoration:underline">RFC 7646</span></a>, which tells a resolver to treat a zone as unsigned and bypass validation. Before installing the NTA, we attempted to reach the <code>.al</code> operator directly and posted on the <a href="https://www.dns-oarc.net/oarc/services/chat"><span style="text-decoration:underline">DNS-OARC Mattermost</span></a> to alert the community. We received no response, in part because the operator&#x27;s contact addresses were themselves under <code>.al</code>, making them unreachable during the outage.</p><p>We applied the NTA for <code>.al</code> and rolled it out to all 1.1.1.1 users by 17:15 UTC, roughly three hours after the chain broke.</p><p>The tradeoff is the same as it was for <code>.de</code>: a Negative Trust Anchor suspends DNSSEC validation, which means <code>.al</code> domains were no longer protected against DNS spoofing for the duration. We judged this acceptable for the same reason: the failure was public, confirmed, and affecting every validating resolver equally.</p><p>The Negative Trust Anchor was removed the following day, once the <code>.al</code> operator had removed the DS record from the root zone. With no DS record present, resolvers no longer expected DNSSEC for <code>.al</code> and the NTA was no longer needed.</p><h3>The problem with Negative Trust Anchors</h3><p>Installing a Negative Trust Anchor is an aggressive measure. We suspend DNSSEC validation to keep domains reachable, accepting that responses are no longer cryptographically verified for the duration. Users get answers instead of SERVFAIL, but those answers carry no DNSSEC guarantee.</p><p>What makes this harder is that, up until now, nothing in the DNS response signalled this to the client; a response served under an NTA looked identical to a fully validated one. RFC 7646 acknowledges this gap and recommends that operators publicly disclose which NTAs they have in place, but that disclosure is out-of-band. For both the <code>.de</code> and <code>.al</code> incidents we published status pages, but a status page requires the user to go looking. An application, a monitoring tool, or a user querying 1.1.1.1 had no way to tell, from the response alone, that DNSSEC validation was bypassed.</p><h3>Bringing transparency to Negative Trust Anchors</h3><p>Extended DNS Error (EDE) codes, defined in <a href="https://datatracker.ietf.org/doc/html/rfc8914"><span style="text-decoration:underline">RFC 8914</span></a>, allow resolvers to include additional context alongside any DNS response, whether that is an error or a successful answer. Babak Farrokhi at Quad9 proposed an Internet-Draft to signal the presence of a Negative Trust Anchor directly in the DNS response, using a new EDE code: <a href="https://datatracker.ietf.org/doc/draft-farrokhi-dnsop-ede-nta/"><span style="text-decoration:underline">Disclosure of Negative Trust Anchors in DNS Responses</span></a>. We joined as co-authors, and 1.1.1.1 now implements it.</p><p>During the <code>.al</code> incident, any query for a <code>.al</code> name returned both the answer and the new EDE code while the Negative Trust Anchor was installed. Here is what that looked like:</p><!-- unsupported block: code --><p>The response is a NOERROR with a valid answer: <code>google.al</code> resolves, but two EDE codes accompany it. <code>EDE 9 (DNSKEY Missing)</code> surfaces the underlying DNSSEC failure: the chain of trust was broken and validation failed. <code>EDE 33 (Negative Trust Anchor)</code> signals that 1.1.1.1 applied a Negative Trust Anchor and served the response anyway. Together they give clients and operators full visibility into what happened: the answer is real, but it was not DNSSEC-validated.</p><p>1.1.1.1 returns EDE 33 on any response generated while an NTA is active, regardless of whether the query itself would have failed DNSSEC validation. A query for a domain that does not use DNSSEC at all will still carry EDE 33 if it falls under an active NTA. This is intentional: the NTA covers the entire zone, and transparency applies equally to every response served under it. This also resolves an issue we flagged in our <code>.de</code> blog, where 1.1.1.1 incorrectly returned <code>EDE 22 (No Reachable Authority)</code> instead of surfacing the underlying DNSSEC error. During the <code>.al</code> incident, 1.1.1.1 correctly returned <code>EDE 9 (DNSKEY Missing)</code> alongside EDE 33.</p><p>The Internet-Draft is an individual submission and EDE 33 has been <a href="https://www.iana.org/assignments/dns-parameters/dns-parameters.xhtml#extended-dns-error-codes"><span style="text-decoration:underline">assigned by the Internet Assigned Numbers Authority (IANA)</span></a>. Thanks to our co-author, Babak Farrokhi at Quad9, the <a href="https://www.knot-dns.cz/docs/latest/html/man_kdig.html"><span style="text-decoration:underline">kdig</span></a> tool from the Knot project <a href="https://github.com/CZ-NIC/knot/commit/1b053bcfe17eaa4f008d589d6ec0ea53145e22e4"><span style="text-decoration:underline">now recognizes EDE 33 by name</span></a>, and <a href="https://github.com/NLnetLabs/unbound/pull/1470"><span style="text-decoration:underline">a pull request for Unbound</span></a> is under review. We hope other resolver implementations will follow. The Internet-Draft has been submitted to the <a href="https://datatracker.ietf.org/wg/dnsop/about/"><span style="text-decoration:underline">Internet Engineering Task Force (IETF) DNSOP Working Group</span></a>, and will be discussed in the DNSOP Working Group at the IETF meeting, taking place in Vienna from July 18 to July 24.</p><h3>Closing the gap</h3><p>TLD-level DNSSEC failures are rare, but when they happen they affect every domain underneath the affected TLD simultaneously, and every validating resolver equally. The <code>.al</code> incident, following closely behind <code>.de</code>, shows that Negative Trust Anchors are a necessary operational tool, but one that has, until now, been invisible to the users they affect.</p><p>EDE 33 closes a gap that RFC 7646 left open. A response served under a Negative Trust Anchor now says so directly, giving operators, monitoring tools, and users the information they need to understand what the resolver did and why.</p><p>The Internet-Draft is available at the <a href="https://datatracker.ietf.org/doc/draft-farrokhi-dnsop-ede-nta/"><span style="text-decoration:underline">IETF datatracker</span></a>. If you have thoughts on it, the <a href="https://mailarchive.ietf.org/arch/browse/dnsop/"><span style="text-decoration:underline">IETF DNSOP mailing list</span></a> is the right place to share them.</p><p>If you want to learn more about how DNSSEC works, visit our page <a href="https://www.cloudflare.com/en-gb/learning/dns/dnssec/how-dnssec-works/"><span style="text-decoration:underline">How does DNSSEC work?</span></a> And you can always follow real-time DNS trends and TLD data on <a href="https://radar.cloudflare.com/tlds/al?dateStart=2026-07-03&amp;dateEnd=2026-07-03"><span style="text-decoration:underline">Cloudflare Radar</span></a>.</p>]]></content:encoded>
      <guid isPermaLink="false">4k8LbDKkNXwcJVEFYF1ije</guid>
    </item>
    <item>
      <title>Introducing Precursor: detecting agentic behavior with continuous client-side signals</title>
      <link>https://blog.cloudflare.com/introducing-precursor/</link>
      <pubDate>Mon, 13 Jul 2026 13:00:00 GMT</pubDate>
      <description>Precursor, our new continuous behavioral validation engine for bot management, offers visibility into how humans and bots actually interact across the full user journey. By turning session-level behavior into bot detection signals, it identifies advanced automation with higher precision — while reducing friction for legitimate users.</description>
      <dc:creator>Marina Elmore</dc:creator>
      <dc:creator>Benedikt Wolters</dc:creator>
      <category>AI</category>
      <category>Bot Management</category>
      <category>Cybersecurity</category>
      <category>JavaScript</category>
      <category>Precursor</category>
      <category>Product News</category>
      <category>Turnstile</category>
      <content:encoded><![CDATA[<p>Bot mitigation is an adversarial game: attackers adapt, defenders respond, and the cycle continues. At Cloudflare, we stay ahead by combining visibility across our global network with signals from the client-side environment. At the network level, we analyze over 1 trillion requests per day to understand reputation, patterns, and anomalies across more than 20% of the web. On the client side, we’ve pushed detection deeper with <a href="https://developers.cloudflare.com/turnstile/"><span style="text-decoration:underline">Cloudflare Turnstile</span></a>, which has evolved from a <a href="https://blog.cloudflare.com/turnstile-private-captcha-alternative/"><span style="text-decoration:underline">CAPTCHA replacement</span></a> to a risk-based managed challenge that adapts the amount of friction needed to verify the user is authentic.</p><p>Today, Turnstile runs nearly 3 billion times per day on some of the most sensitive endpoints on the Internet, helping verify users at key moments like login, signup, and checkout. This improves protection on the most important areas of customer applications, but still leaves limited visibility into the rest of the application — how humans and bots actually interact across the full user journey.</p><p>This is the visibility gap we’re closing today with our launch of <strong>Precursor</strong>.</p><h3>Introducing Precursor</h3><p><a href="https://developers.cloudflare.com/cloudflare-challenges/precursor"><span style="text-decoration:underline">Precursor</span></a> is a client-side, session-based verification system, built with privacy in mind, that uses dynamically injected JavaScript to continuously collect behavioral signals as visitors interact with your application. These signals are processed and incorporated into Cloudflare’s bot protection in real time, allowing us to continuously distinguish human traffic from automated or agentic traffic.</p><p>This extends the client-side detections offered by a <a href="https://developers.cloudflare.com/cloudflare-challenges/"><span style="text-decoration:underline">Challenge</span></a> to your entire web application. Precursor is an optional complement to Turnstile — both are features of our Enterprise Bot Management. This user-journey-based detection is powerful because modern automation is increasingly capable of appearing legitimate in short bursts. Bots can execute JavaScript, use real browser environments, and pass individual CAPTCHAs without raising suspicion. What remains difficult to replicate is consistent human behavior over time.</p><p>Precursor is built to capture that layer of interaction, turning behavior itself into a reliable signal for detecting fraud and abuse. By evaluating behavior across an entire session, Precursor adds significantly more signal to each decision. This improves detection precision, making it easier to distinguish real users from automation without relying on aggressive Challenges. For legitimate users, Precursor means fewer unnecessary interruptions. For bot developers, it raises the cost of operating automation by requiring them to simulate a full session. This is significantly harder to build, more expensive to maintain, and far less reliable to operate at scale.</p><h3>To err is human </h3><p>When a bot developer tries to make a mouse movement look human, they usually add <a href="https://en.wikipedia.org/wiki/Gaussian_noise"><span style="text-decoration:underline">Gaussian noise</span></a> or uniform random delays. But human movement isn&#x27;t just &quot;noisy,&quot; it is also constrained by physics:</p><ul><li><strong>Wrist pivot:</strong> A human mouse movement is often an arc, limited by the range of the wrist and the rotation of the forearm.</li><li><strong>Cognitive load:</strong> There is a measurable delay between a human seeing a checkbox and clicking it.</li><li><strong>Hand tremor:</strong> Even the steadiest human hand oscillates at a physiological tremor frequency.</li></ul><p>Bots, by contrast, often behave in ways that give them away. They move in linear interpolations or mathematically ideal Bézier curves. They click with a precision that humans could never replicate. And even when they do manage to simulate human error, there is a rhythm to human movements that can only be seen by examining an entire session.</p><p>Mouse movement is just one example of the signals Precursor evaluates, but it illustrates the difference clearly. Below is an example of a mouse automation library interacting with a site. You can see how the mouse moves in perfectly straight lines, always returns to an origin, and reacts with the same velocity. </p><!-- unsupported block: htmlBlock --><p>Now, contrast that with a human navigating the same site: you see irregular paths, small corrections and overshoots, and variations in speed, timing, and direction. </p><!-- unsupported block: htmlBlock --><p>Individually, these interactions might look plausible. But over the course of a session, these patterns diverge in ways that are difficult to fake. Precursor is designed to capture and evaluate these behavioral signatures as they develop over a visitor’s interaction with an application.</p><h3>How Precursor works</h3><p>To evaluate behavior over time, Precursor continuously collects interaction data on the client and builds a session-level view of activity for that site.</p><p><strong>1. Injection and collection layer</strong></p><p>When Precursor is enabled on your application, Cloudflare automatically injects a lightweight script into HTML responses from your site as they pass through our network, with no additional configuration, network connections, or third-party embedding required. The injected Precursor bundle is compact, obfuscated, and assembled dynamically for each response. The bundle is designed to not interfere with any additional page logic of the hosted web application.</p><p>The script attaches lightweight event listeners to capture interaction signals such as pointer movement, keyboard activity, focus changes, and visibility. These events are serialized into a compact format and buffered in memory. At regular intervals, the buffered data is sent back to the evaluation layer for analysis.</p><p><strong>2. Evaluation layer</strong></p><p>On the edge server, incoming Precursor payloads are deserialized into behavioral inputs. A dispatcher runs a roster of evaluators on the input data. Each evaluator reads the Precursor streams it cares about and can raise signals into the shared detection registry.</p><p>Evaluators are designed to cross-reference data. For example, they confirm that pointer activity correlates with page visibility duration, or that keyboard events only fire when a text field is focused. This stream of information is then consolidated into individual signals that are used for weighting detections.</p><p><strong>3. Session integration</strong></p><p>Precursor data is session-scoped, meaning it accumulates throughout a session. Session scoping is important because it means a bot cannot reset its behavioral signature by refreshing the page or starting over with a new challenge. The system also feeds session metadata into downstream detection layers for additional shadow-mode heuristics and session analysis, predicted vs. actual completion, and session delinquency heuristics. These edge-side observations are logged for detection improvement purposes and to adjust the <a href="https://developers.cloudflare.com/bots/concepts/bot-score/"><span style="text-decoration:underline">bot score</span></a> of a session. </p><p><strong>4. Privacy by design</strong></p><p>Precursor was designed to collect signals that help to distinguish human patterns from automated and abusive patterns. The event listeners capture the minimum information needed to be a useful signal for detecting automation and abuse. For example, keyboard activity is captured as timing and rhythm, not as the actual keys pressed. In addition, behavioral signals are evaluated as aggregate patterns rather than individual actions and are consumed internally by Cloudflare&#x27;s bot detection systems; they are not exposed to customer dashboards or tied to user accounts, login identities, or persistent profiles.</p><p>Taken together, this allows Precursor to maintain a continuously evolving evaluation of behavior, maximizing precision while minimizing the friction on good users.</p><h3>Per-session analytics</h3><p>To support this new layer of detection, we are introducing session-based views in Security Analytics. These dashboards shift the perspective from individual requests to full visitor journeys. You can now answer questions like:</p><ol><li>What does a typical session look like on my site?</li><li>Where do sessions diverge from expected behavior?</li><li>Which sessions show signs of automation over time?</li></ol><!-- unsupported block: image --><p>These analytics now capture information that per-request analytics can’t —  especially the behavior that occurs between requests. Precursor feeds directly into existing systems like bot score, challenge decisions, and security rules, so you benefit from this added context immediately.</p><h3>What’s next</h3><p>Precursor is the foundation for extending bot detection across the entire application. We are continuing to expand the range and depth of behavioral signals for security, how session-level insights influence our bot management protections, and new ways to visualize and act on session data. As bots evolve, detection needs to move beyond isolated checkpoints and into the full flow of user activity.</p><h3>Get started</h3><p>Precursor is rolling out now and can be enabled directly from your Cloudflare dashboard. Precursor will be free to use until our GA release later this year. Getting started is simple: <a href="https://dash.cloudflare.com/?to=/:account/:zone/security/settings"><span style="text-decoration:underline">turn Precursor on</span></a> for your zone and choose how strictly you want to verify sessions. You can run it in a low-friction mode to observe behavior in the background, or require a fully verified session by enforcing Challenges if a session doesn’t already exist. </p><p>Once enabled, Precursor begins enhancing your existing bot defenses immediately, with no changes required to your application. If you&#x27;re already using Bot Management or Turnstile, Precursor extends those protections beyond Challenges and into the rest of the session. Enable Precursor to extend detection across the full user session, including the activity between moments you already protect.</p>]]></content:encoded>
      <guid isPermaLink="false">6ePF6Vl7bAXNNpdFDoFbqs</guid>
    </item>
    <item>
      <title>Improving Smart Tiered Cache for Public Cloud Regions</title>
      <link>https://blog.cloudflare.com/smart-tiered-cache-for-public-clouds/</link>
      <pubDate>Fri, 10 Jul 2026 12:00:00 GMT</pubDate>
      <description>Smart Tiered Cache allows for precise upper tier selection for origins hosted on AWS, GCP, Azure, and Oracle Cloud with customer-provided cloud region hints.</description>
      <dc:creator>Chenxi Zhang</dc:creator>
      <category>Cache</category>
      <category>CDN</category>
      <category>Performance</category>
      <category>Product News</category>
      <category>Smart Shield</category>
      <category>Tiered Cache</category>
      <content:encoded><![CDATA[<p>In 2021, we shipped <a href="https://blog.cloudflare.com/tiered-cache-smart-topology"><span style="text-decoration:underline">Smart Tiered Cache</span></a>. The idea: for each origin behind your site, Cloudflare picks the single best upper-tier data center to route through, based on real-time latency. Flip one switch, and we find the fastest path from our network to your origin.</p><p>That works as long as an origin IP lives in one fixed place. Public cloud origins usually don&#x27;t. They sit behind anycast or regional unicast front ends, so one origin IP can look equally close to a dozen Cloudflare data centers at once — and the latency probes have nothing to lock onto. Smart Tiered Cache handles this the safe way: when there&#x27;s no clear winner, it falls back to several upper tiers. Nothing breaks. You just lose the thing that made a single closest tier worth it, which is cache efficiency.</p><p>Smart Tiered Cache for Public Cloud Regions fixes this by letting you provide a cloud region hint. With that hint, Cloudflare can map public cloud origins to the right region and select better primary and fallback upper tiers, even when the origin IP itself looks anycast or ambiguous.</p><h3>We made our most popular tiered cache topology smarter</h3><p>Since it was launched, Smart Tiered Cache has become the most popular tiered cache topology among Cloudflare customers. It’s available to all plans, for free.</p><p>Much of our work aims to continually improve it. Over time, we’ve extended Smart Tiered Cache to handle more origin architectures, including:</p><ul><li><strong>November 2024</strong>: <a href="https://developers.cloudflare.com/changelog/post/2024-11-20-smart-tiered-cache-for-r2/"><span style="text-decoration:underline">Smart Tiered Cache for R2</span></a>: We taught Smart Tiered Cache to automatically select the closest upper tier to where the R2 bucket actually lives, reducing latency with zero configuration.</li><li><strong>January 2025</strong>: <a href="https://developers.cloudflare.com/changelog/post/2025-01-08-smart-tiered-cache-for-load-balancing/"><span style="text-decoration:underline">Smart Tiered Cache for Load Balancing</span></a>: We extended Smart Tiered Cache to select a single optimal upper tier for an entire Load Balancing pool, so all origins in the pool share the same cache, improving hit ratios.</li></ul><p>Each of these improvements has shared a common goal: <strong>understand the customer’s origin infrastructure and automatically do the best thing for that infrastructure.</strong></p><p>While we’ve been improving this system for a while, customers still had a common frustration: Smart Tiered Cache did not work when an origin is behind an anycast or regional unicast network, because this architecture prevented us from knowing where the origin is located. And this wasn’t an edge case, either. Origins hosted on public cloud providers behind anycast IPs are a growing slice of the Internet.</p><p>Today, we’re closing that gap for origins hosted on AWS, GCP, Azure, and Oracle Cloud.</p><h3>Why anycast cloud origins are different</h3><p>Smart Tiered Cache works by measuring the latency from each Cloudflare data center to the origin’s IP address. The data center with the lowest latency becomes the upper tier: the single point through which all cache misses funnel on their way to your origin. By concentrating cache misses at one data center, you get higher cache hit ratios, fewer connections to your origin, and lower latency on origin pulls. This works well when the origin has a fixed, unicast IP address that can be reliably probed.</p><!-- unsupported block: image --><p>Many cloud providers use <a href="https://www.cloudflare.com/learning/cdn/glossary/anycast-network/"><span style="text-decoration:underline">anycast</span></a> or regional unicast networking for their load balancers, front-end services, and regional ingress points. When we probe these IPs, the origin appears to be “close” to many data centers simultaneously. That is because the IP address represents the cloud provider’s front end, not a single physical origin location. Different Cloudflare data centers may reach different nearby cloud edges for the exact same IP, and the provider then carries the request across its own network to the actual backend. So Smart Tiered Cache cannot confidently pick one best upper tier.</p><p>In practice, this could result in hairpin traffic across continents, adding a whole extra round trip. Say your origin sits in Singapore, behind an anycast IP from a cloud provider. Because of how anycast works, our Chicago data center might show the lowest probe latency to that IP. Smart Tiered Cache would then select Chicago as the upper tier. The result: a request from an end user in Asia hits a nearby Cloudflare data center, gets routed cross-continent to the upper tier in Chicago, and Chicago fetches from the origin back in Singapore, crossing the ocean twice. That hairpinning adds hundreds of milliseconds of latency, and it&#x27;s one of the most consistently reported issues from customers with cloud-hosted origins.</p><!-- unsupported block: image --><p>To address this unnecessary back-and-forth, Smart Tiered Cache learned to detect anycast origins with a constraint from physics: the speed of light. We measure probe latencies from multiple checkpoint data centers around the world to the origin. If the combined latencies from two checkpoint data centers are faster than what light in fiber could physically travel between the two, the origin must be answering from multiple locations, not one. That means it&#x27;s anycast.</p><!-- unsupported block: image --><p>When Smart Tiered Cache detects an anycast origin, it plays it safe: it won&#x27;t pin that IP to a single upper tier. Instead, it falls back to a tiered cache topology with multiple upper tiers. Tiered caching still works, but spreading traffic across multiple tiers instead of one means more requests reach the origin. For some setups that’s a fine trade. But if you want one upper tier close to an origin that lives on a public cloud behind anycast IPs, there hasn&#x27;t been a good option — until now. </p><h3>Tell us the region</h3><p>From the Cloudflare dashboard, go to <strong>Caching &gt; Tiered Cache</strong> &gt; <a href="https://dash.cloudflare.com/?to=/:account/:zone/caching/tiered-cache"><span style="text-decoration:underline">Origin Configuration</span></a>. Find your origin IP, click &quot;Set Region Hint,&quot; and tell us the cloud region (for example, aws:us-east-1 or gcp:europe-west1). Smart Tiered Cache takes over from there. Note that, on the dashboard, region hints can only be set for origins whose IPs we&#x27;ve detected as anycast.</p><!-- unsupported block: image --><p>You can set hints one IP at a time, or bulk-edit cloud regions for all your origin IPs at once. Beyond the dashboard, the same configuration is available via the API and through Terraform, so you can integrate it into your existing infrastructure-as-code workflows.</p><!-- unsupported block: image --><p>We&#x27;re launching with AWS, GCP, Azure, and Oracle Cloud, with more providers coming.</p><h3>How Smart Tiered Cache for Public Cloud Regions works</h3><p>Every few hours, we fetch the latest IP range files from each supported cloud provider. These files map every cloud region to its current set of IP prefixes, so when a provider adds, removes, or reassigns a subnet, we pick it up.</p><!-- unsupported block: image --><p>We match those subnets against our upper tier database, which is built from continuous latency probing refreshed every 15 minutes. For each cloud region, each matching subnet contributes a weighted vote based on its current upper-tier assignment. The upper tier with the strongest signal becomes the region’s primary upper tier. Primary and fallback always come from different points of presence (PoPs), so losing one PoP can&#x27;t take out both.</p><p>Some regions don&#x27;t have enough probe data, for example, perhaps the new region’s cloud provider is still rolling out, or the region has no origin onboarded to Cloudflare yet — so there’s nothing to vote on. We fall back to geography: the closest of our Tier 1 PoPs. As origins come online and probe data builds up, the region quietly switches from that geographic guess to the option backed by real data.</p><h3>Try it now, and what&#x27;s next</h3><p>All this means that the work of selecting the optimal region for your cache — the constant probing, the algorithmic choice of each region&#x27;s best upper tier, the geographic fallbacks, the failover across PoPs — runs on our side. Your job is selecting the region hint.</p><p>If your anycast origin sits on a public cloud, you can turn this on now. In the dashboard, go to Caching &gt; Tiered Cache &gt; <a href="https://dash.cloudflare.com/?to=/:account/:zone/caching/tiered-cache"><span style="text-decoration:underline">Origin Configuration</span></a>. Find your origin IP, click Set Region Hint, and pick your region.</p><p>Next up, we’re expanding to more providers, and continuing to teach Smart Tiered Cache to recognize more origin setups and pick the right path on its own. To learn more about how Tiered Cache can benefit your service, check out our <a href="https://developers.cloudflare.com/cache/how-to/tiered-cache/"><span style="text-decoration:underline">Tiered Cache documentation</span></a>.</p>]]></content:encoded>
      <guid isPermaLink="false">2smERhDvpGH6FbC035djwD</guid>
    </item>
    <item>
      <title>Why we cannot wait for better post-quantum signature algorithms</title>
      <link>https://blog.cloudflare.com/ml-dsa-will-have-to-do/</link>
      <pubDate>Thu, 09 Jul 2026 14:00:00 GMT</pubDate>
      <description>NIST is advancing nine new post-quantum signature algorithms as potential candidates for future standardization. We take a closer look at all of them, and argue that while they are in the works and show great potential, we should use ML-DSA for now — the best currently available. </description>
      <dc:creator>Bas Westerbaan</dc:creator>
      <dc:creator>Christopher Patton</dc:creator>
      <category>Cryptography</category>
      <category>Post-Quantum</category>
      <category>Research</category>
      <category>Security</category>
      <content:encoded><![CDATA[<p>RSA and ECC, cryptographic algorithms that we have all relied on for decades, are <a href="https://blog.cloudflare.com/the-quantum-menace/"><span style="text-decoration:underline">vulnerable</span></a> to the attack of sufficiently advanced quantum computers. Such quantum computers do not exist yet, but they seem to be coming <a href="https://scottaaronson.blog/?p=9718"><span style="text-decoration:underline">sooner</span></a> than expected. Luckily, the solution is already available: migrate to ML-KEM encryption and ML-DSA signatures, which are designed to be resistant to quantum attack. They were standardized <a href="https://blog.cloudflare.com/nists-first-post-quantum-standards/"><span style="text-decoration:underline">in 2024</span></a> by the U.S. National Institute of Standards and Technology (NIST) after an eight-year open international competition.</p><p>The migration to post-quantum cryptography is in full swing now. At the time of writing, the majority of traffic handled by Cloudflare is already using ML-KEM encryption, and is thus secured against the threat to data posed by <a href="https://en.wikipedia.org/wiki/Harvest_now,_decrypt_later"><span style="text-decoration:underline">harvest-now-decrypt-later</span></a> attacks. But encryption is only one part of the equation: to be fully secure against quantum computers capable of breaking classical cryptography, we aim to deploy post-quantum signatures to protect authentication systems from unauthorized access. We are <a href="https://blog.cloudflare.com/post-quantum-roadmap"><span style="text-decoration:underline">targeting 2029</span></a> for Cloudflare to be fully post-quantum secure.</p><p>ML-DSA, the best all-around post-quantum signature scheme standardized today, has its downsides: it’s much larger on the wire, and many <a href="https://github.com/fancy-cryptography/fancy-cryptography"><span style="text-decoration:underline">tricks</span></a> we were able to perform with RSA and ECC simply cannot be done with ML-DSA. There are better post-quantum signature schemes on the horizon: last month, NIST <a href="https://groups.google.com/a/list.nist.gov/g/pqc-forum/c/LXoTAe5AN78/m/ZXgBCNlgDAAJ"><span style="text-decoration:underline">announced</span></a> that it is advancing nine post-quantum signature schemes to the third round of the “<a href="https://csrc.nist.gov/projects/pqc-dig-sig"><span style="text-decoration:underline">signatures on-ramp</span></a>”. And a draft standard for <a href="https://falcon-sign.info/"><span style="text-decoration:underline">FN-DSA</span></a> (née Falcon), which was picked from the previous competition, is expected imminently.</p><p>We have been very interested in advances in post-quantum signature algorithms, and wrote about the progress in <a href="https://blog.cloudflare.com/sizing-up-post-quantum-signatures/"><span style="text-decoration:underline">2021</span></a>, <a href="https://blog.cloudflare.com/nist-post-quantum-surprise/"><span style="text-decoration:underline">2022</span></a>, <a href="https://blog.cloudflare.com/another-look-at-pq-signatures/"><span style="text-decoration:underline">2024</span></a>, and <a href="https://blog.cloudflare.com/pq-2025/#signatures-on-the-horizon"><span style="text-decoration:underline">2025</span></a>. In this blog post we’ll treat you to the latest developments in great detail.</p><p>But first we have to deal with the elephant in the room: These new signature algorithms will not be ready in time for the PQ transition — not even close, as we <a href="#timelines"><span style="text-decoration:underline">will see later on</span></a>. The problem is arriving too soon for us to wait. ML-DSA is available today, and it will have to do for the first migration. As Eric Rescorla <a href="https://educatedguesswork.org/posts/pq-emergency/"><span style="text-decoration:underline">wrote</span></a> in 2024:</p><blockquote><em>You go to war with the algorithms you have, not the ones you wish you had.</em></blockquote><p>Nonetheless, the search for better post-quantum signature algorithms is crucial for several reasons, and we firmly believe it is still the best use of NIST’s limited resources.</p><p>Let’s have a look at the signature algorithms in detail. After that we’ll look at the timeline for their availability, and the reasons why we still need them.</p><h2>The signature algorithms</h2><p>In the table below, we compare the candidate signature algorithms that progressed to the third round (marked by 🤔), with classical algorithms vulnerable to quantum attack (marked by ❌), and the post-quantum algorithms that are already standardized ( ✅) or soon will be (📝). Each candidate proposes several variants. We list the most relevant variants to TLS, the protocol used to secure connections on the Internet. To explore all variants, check out <a href="https://thomwiggers.nl/"><span style="text-decoration:underline">Thom Wigger</span></a>s&#x27; <a href="https://pqshield.github.io/nist-sigs-zoo/"><span style="text-decoration:underline">signatures zoo</span></a>.</p><!-- unsupported block: htmlBlock --><p>A few more remarks on this table: Most candidates have multiple variants in every security level. We show the most relevant variants for TLS at the 128-bit security level, the gold standard for security. CPU times are taken from the <a href="https://pqshield.github.io/nist-sigs-zoo/"><span style="text-decoration:underline">signatures zoo</span></a> in June 2026, which collected them from the round two submission documents and later advances. Candidates are allowed to make changes for the third round, which will influence these numbers. Some will improve (both in compute and size), whereas others will regress to counter new attacks. Check out the zoo for the latest numbers. We marked FN-DSA and SQIsign signing with a  ⚠️️, as both are hard to implement in a fast and <a href="https://blog.cloudflare.com/kyber-isnt-broken/#side-channel-attacks"><span style="text-decoration:underline">timing side-channel secure</span></a> manner. LMS signing has a ⚠️, as secure LMS signing requires keeping state across signatures, and the listed signing time assumes a 32MB cache. The 128-24 variant of SLH-DSA is marked with a ⚠️️ as it’s meant to create fewer than 2^24 signatures.</p><h2>No &quot;all-star&quot; algorithm</h2><p>One thing that stands out immediately is that the quantum-vulnerable elliptic curves signature algorithm Ed25519 is by far the best all-around choice (ignoring its quantum vulnerability): it has the best numbers in almost every single metric, including public key size, signature size, and signing time. It’s only beaten on verification time, but it’s more than fast enough for the vast majority of applications.</p><p>This is quite different than the roster of post-quantum algorithms. Instead of a single &quot;all-star&quot; algorithm, we have roughly two categories of schemes: the &quot;specialists&quot; that approach our trusty elliptic curve signatures on some metrics, but are problematic on others, which make them great in the right deployment scenario. Then there are the “generalists”, such as ML-DSA, which don’t perform as well as elliptic curves on all metrics, but so far as downsides go, are pretty balanced.</p><h2>Specialists</h2><p>Let’s start with the specialists.</p><h3>SQIsign: small signatures / slow signing</h3><p>If you just look at the bytes on the wire, then <strong>SQIsign</strong> looks like an almost perfect drop-in replacement for elliptic curve cryptography. With signatures of 148 bytes and public keys of 65 bytes, it beats RSA-2048. Unfortunately there is no free lunch: SQIsign has three weak points. First, it’s the most complex algorithm on the docket. Secondly, its signature creation and verification is quite slow. Finally, it’s difficult to implement signature creation in a timing side-channel secure way and doing so comes with a performance penalty to boot.</p><p>That doesn’t sound great so far, but it was much worse: when we had a <a href="https://blog.cloudflare.com/another-look-at-pq-signatures/#sqisign"><span style="text-decoration:underline">look back in 2024</span></a>, there was not yet any timing side-channel secure implementation and signature verification was 20x slower. Furthermore there has been welcome progress on simplifying the scheme.</p><p>Despite these dramatic improvements, it is unlikely (side-channel secure) signing will be fast enough in the foreseeable future to be used in typical <em>online</em> cases such as the TLS handshake. However, for <em>offline</em> cases, such as CA signatures or DNSSEC, where it’s the verification time that’s more important than the signing time, SQIsign might have an application.</p><p>But the topic we should really discuss is security. SQIsign is based on <a href="https://blog.cloudflare.com/sidh-go/"><span style="text-decoration:underline">isogenies</span></a>. Rather famously, SIKE, another algorithm based on isogenies, got broken <a href="https://eprint.iacr.org/2022/975.pdf"><span style="text-decoration:underline">badly</span></a> in a late stage of the first NIST PQC competition that standardized ML-DSA. SIKE is often brought up as a cautionary example showing that post-quantum cryptography could break suddenly. This requires some nuance. First, there were already concerns about SIKE’s security, and in particular the <em>torsion points</em> that led to the break. Because of these concerns, SIKE was not selected for standardization, but deferred to an additional <a href="https://nvlpubs.nist.gov/nistpubs/ir/2022/NIST.IR.8413-upd1.pdf"><span style="text-decoration:underline">round</span></a> of evaluation before it was broken. (Indeed, this is an example of the NIST process working well.) SQIsign doesn’t use torsion points, and there is no similar concern as there was for SIKE.</p><p>One other notable security property is that the best known attacks on SQIsign are generic brute force, just like with classical attacks on well-selected elliptic curves. This is quite different from RSA, lattices, and multivariate where the attack algorithms have been slowly improving, pushing the parameters towards bigger signatures. Nonetheless, the mathematics behind isogenies is very rich, and compared to the other  algorithms, there is a lot of <em>mathematical attack surface</em>. Still, its security seems more sound than the structured multivariate algorithms we’ll discuss later.</p><p>SQIsign is an algorithm with tremendous potential. It’d be a shame to standardize it too early. To the authors, we’d like to share the following wishlist:</p><ul><li>Ideally verification time is decreased even further, even if this trades off against signing time and signature size: SQIsign signatures are already small enough, and offline signing time has some slack anyway.</li><li>The timing side-channel secure implementation should be the default, especially if signing time is decreased further, which would tempt some online signing applications.</li><li>But above all, our wish is for SQIsign to be simplified.</li></ul><h3>UOV: tiny signatures / huge public keys</h3><p><strong>UOV</strong> (unbalanced oil and vinegar) is a classic multivariate signature algorithm originally proposed in 1999. It has tiny signatures: only 96 bytes. The trade-off? A huge public key: 66kB. That wouldn’t help for a TLS server certificate, whose public key is transmitted over the wire when setting up a connection, but it would be a help for cases where the public key is predistributed.</p><p>Let’s take the WebPKI as an example. A typical browser trusts about a hundred root certificates and 30 certificate transparency logs, whose public keys would add up to about 8MB when using UOV.</p><!-- unsupported block: image --><p>Since the root certificate is transmitted to clients out of band, one idea is to use a UOV signature there. But this is not a slam dunk; because of its size, a UOV root certificate would be impractical to be cross-signed where the root is used as an intermediate. At the same time, cross signs and intermediates become less attractive anyway with any larger post-quantum signatures. This encourages more root certificates to be included directly with clients. This would again favor UOV, but to a point: if the number of root certificates grows above a thousand, we’d be dealing with more than 66MB of key material, which would make up a substantial portion of the browsers’ download size (e.g., 90MB for Firefox 151.)</p><p><strong>Multivariate security</strong></p><p>What about the security? Over the years, many variants of UOV have been proposed that use some extra mathematical structure to reduce the size of the public key. These <em>structured multivariate</em> schemes have had a spotty track record with schemes such as <a href="https://eprint.iacr.org/2022/214"><span style="text-decoration:underline">Rainbow</span></a> and <a href="https://eprint.iacr.org/2021/1677"><span style="text-decoration:underline">GeMMS</span></a> being broken quite badly. It is important to distinguish these from UOV itself, whose security track record is much better, but not perfect.</p><p>As with many cryptographic schemes, there were growing pains in the early years, as basic attacks and parametrization pitfalls were discovered. In fact, the “U” in UOV is a remnant of that: it stands for <em>unbalanced</em>, which is a fix to a parameter-setting mistake in the 1997 oil-and-vinegar scheme on which UOV is based: the original scheme had an equal number of <em>oil</em> and <em>vinegar </em>variables in the quadratic system of equations used as the public key, which turns out to allow for <a href="https://weizmann.elsevierpure.com/en/publications/cryptanalysis-of-the-oil-and-vinegar-signature-scheme-2/"><span style="text-decoration:underline">an attack</span></a>. In case you’re curious about the colorful name: the system of equations contains vinegar x vinegar and oil x vinegar, but no oil x oil terms. It’s like vinaigrette with small separate oil droplets. Back to the history: from 2005 to 2020 was a quiet period for multivariate signatures: understanding of UOV grew, but there weren’t any new attacks on typical parameters.</p><p>This changed in 2020 with the <a href="https://eprint.iacr.org/2020/1343.pdf"><span style="text-decoration:underline">discovery</span></a> of the <em>intersection attack</em> which built on the ideas of the original attack on balanced oil-and-vinegar. The intersection attack removes about 30 bits of security from a then-proposed 128-bit parameter set. A considerable blow, but not fatal: slightly adjusting parameters mitigates the attack completely, with minor increase in key and signature size.</p><p>A bigger shock was the 2025 <a href="https://eprint.iacr.org/2025/1143.pdf"><span style="text-decoration:underline">publication</span></a> of the idea to use <a href="https://en.wikipedia.org/wiki/Exterior_algebra"><em><span style="text-decoration:underline">wedges</span></em></a> to attack multivariate schemes. The initial impact on UOV was minor: only a few bits (again at the 128-bit security level.) The worry was that this idea came out of left field, and it wasn’t clear how far the approach could be taken. That concern was partly justified: the wedges idea was very fruitful and several subsequent attacks have been built on it, reducing security by about 15 bits. However, it also became <a href="https://eprint.iacr.org/2026/298"><span style="text-decoration:underline">clear</span></a> that the wedges attack and generalizations can be seen as a special cases of existing attacks such as the <em>intersection attack</em> over truncated rings — thus much more familiar than we thought. Again, these attacks can be mitigated with only minor increases in key and signature size.</p><p>What to make of all of this? Such a history of attacks is not uncommon: over the last 25 years lattices have seen larger reductions in security, although this has calmed down over the recent years. Notwithstanding, lattice-based cryptography deployed in production today uses conservative parameter sets well above 128-bits to hedge against future cryptanalysis. We’d want to do the same with UOV. Signature size only grows linearly with the security level, costing just 260 bytes even at the 256-bit security level. Unfortunately, the public key size is cubic in security level: 446kB for 256-bit. Conveniently, UOV (as most multivariate schemes) has a lot of flexibility in picking parameter sets at various intermediate security levels.</p><p>UOV is a foundational scheme with narrow but real use cases. Going forward, we’d like to see a parameter set with a bit of margin above 128 bits, say 160 bits, to hedge against future cryptanalytic improvements.</p><h3>QR-UOV: small signatures / large public keys</h3><p>Like SNOVA and MAYO which we’ll discuss later on, <strong>QR-UOV</strong> is a structured multivariate scheme: it’s a variant of UOV that adds more structure to the public key to reduce its size. The gains are modest: at best we’re looking at 12kB public keys, but signature verification is impractically slow for that particular parameter set. The more realistic parameter sets start at 24kB public keys.</p><p>With respect to security, QR-UOV is the only multivariate scheme that did not have to adjust its original (round one) parameters in response to new attacks. This is somewhat surprising as any attack on UOV can also be applied to QR-UOV. The explanation is that the attacks do apply, but the natural parameters for QR-UOV happen to make them ineffective. On the other hand, there were already several attacks known that use the specific extra structure that QR-UOV adds: indeed, for some of the parameter sets, the structure-specific attacks are the best attacks. This should be contrasted with MAYO, where there is no known attack against the extra structure MAYO adds. (We’ll get back to MAYO and SNOVA later in this post.)</p><p>Compared to last round, QR-UOV signing and verification time improved significantly, but it is still comparatively slow. All in all, QR-UOV is a hard sell: it adds exploitable structure to UOV without pushing key sizes down to general-purpose sizes.</p><h3>Hash-based signatures</h3><p><strong>Stateful hash-based signatures</strong></p><p>The very first standardized post-quantum signature algorithms are the stateful hash-based <a href="https://www.rfc-editor.org/rfc/rfc8554.html"><span style="text-decoration:underline">LMS, HSS</span></a> and <a href="https://datatracker.ietf.org/doc/html/rfc8391"><span style="text-decoration:underline">XMSS(MT)</span></a>. They have very small public keys, and for many parameter sets the signatures are much smaller than those of ML-DSA-44. To boot, their security is based on that of hashes, which are well-understood and already a cornerstone of cryptography. That makes hash-based signature algorithms a very conservative choice, and there is no need to hedge with higher security levels.</p><p>So, what’s the catch?</p><p>There are two. The big one is keeping the eponymous state. These stateful hash-based signature schemes are built out of one-time-signature keys which are collected into Merkle trees. The signer has to keep track of which one-time-signature keys have been used, which can be as simple as just a counter. If the signer mucks it up, though, and accidentally uses the same one-time-signature key twice on a different message, then anyone can <a href="https://eprint.iacr.org/2016/1042"><span style="text-decoration:underline">likely</span></a> use those two signatures to create their own signature on any message. You have to keep a lot in mind to keep the state correctly. Some considerations: you want to make sure that updates are written to storage before handing out the signature; you don’t want the old state to be restored from a backup; and you can’t export/import a private key from one place to another without agreeing on how to split or keep the state. The state is, as Adam Langley <a href="https://www.imperialviolet.org/2013/07/18/hashsig.html"><span style="text-decoration:underline">pointed out</span></a> several years ago, a huge foot-cannon.</p><p>Another downside is that the most competitive parameter sets can only create a modest number of signatures. The 1,112 byte signatures (as listed in the table above) can only be used to create about a million signatures. You can explore the trade-offs with this <a href="https://westerbaan.name/~bas/hashcalc/"><span style="text-decoration:underline">calculator</span></a>.</p><p>Together this leaves a very small niche for stateful hash-based signatures: signers have to be able to keep state; signature size has to be a real concern; and signers have to be OK with a hard limit on the number of signatures.</p><p><strong>SLH-DSA: conservative security / large and slow</strong></p><p><strong>SLH-DSA</strong> is a hash-based signature that doesn’t have the low signature limit and avoids the problem of keeping the state. The basic idea is to make the number of one-time-signature keys so large that you can pick one at random without having to worry about using the same one twice, since the chance of picking the same one twice is diminishingly small. SLH-DSA is a bit more efficient than that, by replacing the one-time-signature key as a building block with a few-time-signature key, where security degrades gracefully if keys are occasionally reused. It still comes at a cost. SLH-DSA has two variants, one that optimizes for small signature size, and one that optimizes for fast signing. The size-optimized one is not small at all at 8kB, and the signing-optimized one is even slower than SQIsign.</p><p><strong>Fewer signature variants of SLH-DSA</strong></p><p>NIST has <a href="https://csrc.nist.gov/pubs/sp/800/230/ipd"><span style="text-decoration:underline">proposed</span></a> to standardize an additional parameter set for SLH-DSA with much smaller signatures, but that can only be used to create about 16 million signatures before security reduces. At 3.8kB the signatures are still larger than those of ML-DSA-44, but the combined public key and signature size is very close. The parameter set was chosen to make signature verification fast at the cost of signing time. The signing time is very bad indeed.</p><p><strong>Use cases</strong></p><p>So why ever use SLH-DSA? The selling point is the conservative security. For a long-term trusted key that is hard to replace, it could make sense if the application can stomach the large signature and slow verification of the standardized variants or the slow signing time of the newly proposed one. There are two more caveats to add. First, it’s better to set things up so that key algorithms are not burned-in and can be replaced after the fact. And secondly, in most cases systems (such as secure connections with TLS) do not just depend on signatures, but also on key agreement. There is no hash-based key agreement mechanism, so we end up needing to trust something less conservative, like lattices, anyway.</p><h3>FN-DSA: small key and signatures / subtle signing</h3><p>Comparing the numbers, FN-DSA-512 (née Falcon) looks much better than ML-DSA-44 on almost every metric: faster verification, smaller public key, and much smaller signatures at 666 bytes. Signing is three times slower, but it’s still 25x faster than RSA-2048. To boot it’s already picked to become FIPS 206. So why don’t we consider FN-DSA to be a general-purpose algorithm?</p><p>It’s because it’s difficult to implement FN-DSA signing securely. The most well-known sharp edge of FN-DSA is that it is most naturally and efficiently implemented using hardware-accelerated <a href="https://en.wikipedia.org/wiki/Floating-point_arithmetic"><span style="text-decoration:underline">floating-point arithmetic</span></a>. This is a first for a cryptographic standard. One big challenge with it is that we have little experience implementing fast floating-point arithmetic in a side-channel safe way. What we know so far is that it’s subtle and not very robust: a safe implementation of FN-DSA signing using the Floating-Point Unit (FPU) for one processor might not be safe for another. Instead of relying on the FPU, the floating point operations can be emulated. This is easier to get right, but about 20 times slower, making it about as slow as RSA-2048. There has been some welcome <a href="https://github.com/fixed-point-fndsa/fxp-falcon-py"><span style="text-decoration:underline">progress recently</span></a> to implement FN-DSA signing safely using fixed-point arithmetic, which is much faster than the floating-point emulation. So just use that and FN-DSA is good to go? This presumes a level of awareness that might not be warranted. Anecdotally at conferences, every time we saw a presenter compare post-quantum signature algorithms including FN-DSA in benchmarks, they couldn’t answer whether floating-point emulation was used.</p><p>Another consequence of using floating points is that it’s difficult to make test vectors for signing. Just one example of this is that the outcome of <em>a+(b+c)</em> and <em>(a+b)+c</em> are only guaranteed to be close, but not the same. That means that to have useful test vectors, the FN-DSA specification would need to be very precise on the order of floating-point operations. Another example is <em>a*b+c</em>, which can be computed in two steps (multiply and then add), or at once using <a href="https://en.wikipedia.org/wiki/Multiply%E2%80%93accumulate_operation"><span style="text-decoration:underline">fused-multiply-add</span></a> (FMA). The latter is faster, but again gives a slightly different answer as rounding happens only once. Not all processors support FMA, but for those that do, compilers typically automatically use FMA for the performance boost. There are also mathematical optimizations that cause trouble. For instance, the reference implementation computes a value (norm) in a faster roundabout way using <a href="https://en.wikipedia.org/wiki/Parseval%27s_theorem"><span style="text-decoration:underline">Parseval’s theorem</span></a>. Mathematically the answer is exactly the same, but as floating-points are only an approximation, the resulting value is ever so slightly different. Similarly, the safe fixed-point arithmetic implementation gives slightly different results.</p><p>Why is this a problem? It is because it is still the humble test vector that catches most implementation bugs in practice. Other more refined methods like formal verification will certainly catch more, but test vectors are hard to beat in simplicity.</p><p>Another surprising sharp edge from not having a fixed implementation is the following. From two deterministic signatures created by slightly different implementations from the same private key, one can <a href="https://eprint.iacr.org/2024/1709"><span style="text-decoration:underline">derive parts of that private key</span></a>. FN-DSA does not use deterministic signatures, instead adding a randomizer to thwart this. There is a tension with testing: you need a deterministic interface to test signing, but you don’t want that to be used to create actual signatures.</p><p>How to deal with the wiggle room in the FN-DSA specification will undoubtedly be a point of discussion. The discrepancy between the implementations might actually have a silver lining: NIST could decide to generate the test vectors (<a href="https://csrc.nist.gov/projects/cryptographic-algorithm-validation-program"><span style="text-decoration:underline">CAVP</span></a>) from the fixed-point arithmetic implementation. That the more risky floating-point implementation wouldn’t pass the test vectors would be a feature, not a bug, as it would steer implementations towards the safer fixed-point version!</p><p>You can read about a few other interesting sharp edges in <a href="https://keymaterial.net/2026/05/13/so-you-want-to-deploy-fn-dsa/"><span style="text-decoration:underline">this blog post</span></a>. Stepping back from the specifics, the main point is that FN-DSA is a complicated scheme. It’s not a surprise that it took NIST a <a href="https://csrc.nist.gov/csrc/media/presentations/2025/fips-206-fn-dsa-(falcon)/images-media/fips_206-perlner_2.1.pdf"><span style="text-decoration:underline">couple of years</span></a> (not counting the current limbo) just to write the draft standard. It’ll take longer than usual for the final standard to come out and for cryptographic libraries to add support. FN-DSA is farther away than it seems. We’ll compare timelines later in this blog post.</p><p>If the numbers are still very tempting, there is one last thing you should be aware of: FN-DSA-512 is parametrized for 128-bit security compared to ML-DSA-44’s generous 160 bits. If lattice cryptanalysis improves, there is no middle security level: the next step-up is all the way to FN-DSA-1024 at 256 bits. FN-DSA-1024 has double the key and signature sizes and signing and verifying times of FN-DSA-512. An FN-DSA-1024 signature is still half the size of that of ML-DSA-44, but the <em>public key+signature</em> only differs about 20%.</p><p>To close the discussion of FN-DSA, it is good to emphasize that all difficulties with FN-DSA are on the signing side: the verification of an FN-DSA signature is very straightforward. </p><h2>General-purpose algorithms</h2><p>Now let’s turn to the algorithms that are meant to be general-purpose replacements for ML-DSA.</p><h3>HAWK</h3><p><strong>HAWK</strong> is a curious case. In many aspects it’s similar to FN-DSA: a structured lattice hash-then-sign scheme with similar sizes for signatures and public keys with a missing middle security level. The main benefit of HAWK over FN-DSA is that signing is very fast and doesn’t use floating-point arithmetic, although it’s not a simple algorithm either. This comes with a trade-off: HAWK is based on and introduces a new security assumption, the <em>lattice isomorphism problem </em>(LIP). In 2024, two years after the introduction of HAWK, it was discovered that this problem is <a href="https://eprint.iacr.org/2024/441"><span style="text-decoration:underline">easy to solve</span></a> in the special case of totally real number fields, which aren’t used in HAWK or any other cryptography. In 2025, this attack was <a href="https://eprint.iacr.org/2025/280.pdf"><span style="text-decoration:underline">extended</span></a> to a broader class of number fields. This hasn’t yet applied to HAWK, but it’s getting closer. A new paper <a href="https://eprint.iacr.org/2026/1318.pdf"><span style="text-decoration:underline">published</span></a> in June 2026 suggests there is a way to extend the attack to HAWK. An error has been found in the paper, although it’s yet unclear how fundamental it is to the approach. Regardless, the trajectory is concerning.</p><p>Even ignoring the potential attacks, HAWK faces some headwinds: its additional security assumption prevents it from displacing FN-DSA, but its practical benefits (especially considering the lack of middle security level) fall short of that of the structured multivariate candidates. It also doesn&#x27;t increase diversity in security assumptions, an outcome that NIST is hoping for.</p><h3>Proof-of-knowledge schemes</h3><p>FAEST, MQOM, and SDitH all share a similar overall structure. Their public keys are instances of some hard problem and their secret keys are the solutions.</p><ul><li>A <strong>FAEST</strong> public key is the AES-encryption of a known plaintext under a secret key.</li><li><strong>MQOM</strong> gets its name from the Multivariate Quadratic problem, which is closely related to (but more conservative than) the cryptographic assumptions underlying the multivariate schemes. The public key is a system of quadratic equations, and the secret key is a solution to that system of equations.</li><li><strong>SDitH</strong> is based on the hardness of the Syndrome Decoding problem for random linear codes. This problem is related to the code-based schemes submitted to the original NIST competition, but these were eliminated in the third round.</li></ul><p>In all cases, a signature is a <a href="https://en.wikipedia.org/wiki/Zero-knowledge_proof"><span style="text-decoration:underline">zero-knowledge proof</span></a> that the signer knows the solution of that hard problem, while at the same time (almost as an afterthought) acknowledging the message-to-be-signed as part of the proof.</p><p>Many signature schemes are zero-knowledge proofs like this behind the scenes, notably ML-DSA, SQIsign, and Ed25519. Why don’t we group those with <em>proof of knowledge</em> schemes too?</p><p>The difference is generalizability: the <a href="https://blog.cloudflare.com/lattice-crypto-primer/#warm-up"><span style="text-decoration:underline">zero-knowledge proof used for ML-DSA</span></a> is only able to prove something about a specific LWE problem as used in ML-DSA: the proof uses mathematical structure in the key. There are <a href="https://eprint.iacr.org/2022/1341.pdf"><span style="text-decoration:underline">ways</span></a> to create zero-knowledge proofs using lattices for any general statement, but those proof systems are very different from ML-DSA, and would create rather larger signatures on the order of 50kB.</p><p>In contrast, the proof system used in FAEST, MQOM, and SDitH can be used to prove arbitrary statements. For instance, FAEST can be modified to use the hard problem of MQOM instead. This leads to a more efficient scheme called <a href="https://eprint.iacr.org/2024/490.pdf"><span style="text-decoration:underline">KuMQuat</span></a>. (We’ll get to some performance numbers later on.) Conversely, MQOM can be adjusted to use AES as the hard problem.</p><p>This flexibility is great for two reasons. First, it doesn’t require any specific mathematical structure in the hard problem used, and thus we can pick a very conservative problem such as breaking AES. Some problems lead to a more efficient signature than others, as we see with MQ as used in MQOM. MQ is still quite a conservative assumption: it does not contain the hidden subspace used in UOV and thus the other multivariate signatures. Neither the <a href="#multivariate-security"><span style="text-decoration:underline">intersection nor wedges attacks</span></a> apply to it. In fact, the MQ-problem is NP-hard. To be secure, one still needs to pick the correct size of the problem, and although MQ has been studied for quite a while, it certainly has not seen the same scrutiny as deployed algorithms like AES.</p><p>The second and greater benefit is that we’re able to create much more than just a plain signature scheme from a general zero-knowledge proof system: we can create <a href="https://eprint.iacr.org/2026/109.pdf"><span style="text-decoration:underline">blind signatures</span></a> and even full-fledged anonymous credentials.</p><p>Here it’s good to note a limitation: the size of the proofs for all three grows linearly with the statement proven. In technical terms: they’re not <em>succinct</em> like <a href="https://aszepieniec.github.io/stark-anatomy/"><span style="text-decoration:underline">STARKs</span></a> and <a href="https://eprint.iacr.org/2022/1341.pdf"><span style="text-decoration:underline">LaBRADOR</span></a>, which outperform them handily for large statements. It’s another example where sometimes it’s better to pick the approach that’s not optimal asymptotically.</p><p>Back to advantages: apart from the hard problem chosen, and the security of hash functions, these three schemes don’t require any further security assumption. This makes FAEST as conservative as SLH-DSA.</p><p>So what’s the difference except for the chosen hard problem? These schemes started off quite differently, but have been improving and converging since the first round. The proof system in MQOM is a bit simpler than FAEST, but it also does not perform as well: <a href="https://eprint.iacr.org/2024/490.pdf"><span style="text-decoration:underline">KuMQuat</span></a> (FAEST+MQ) outperforms MQOM.</p><p>Talking about performance, let’s start with a comparison to SLH-DSA. All three schemes have variants that outperform any standardised SLH-DSA parameter set and often by a good margin. SLH-DSA does have one distinct advantage: the verification routine is simpler to implement.</p><p>Against ML-DSA-44 the comparison is more interesting. All schemes have a smooth trade-off between runtime and signature size. To illustrate, here are trade-offs <a href="https://eprint.iacr.org/2024/490.pdf"><span style="text-decoration:underline">reported</span></a> for KuMQuat (FAEST+MQ.) Verification times are close to signing times.</p><!-- unsupported block: image --><p>KuMQuat can be parametrized to have somewhat smaller signatures than ML-DSA-44 at the cost of long signing (and verification) runtime. At the other end, it can have similar signing time as ML-DSA-44, at the cost of larger signatures, although the public key+signature size is still similar.</p><p>These schemes have improved quite a bit over the years, and we expect some improvements still. Although they won’t improve upon ML-DSA as dramatically as some of the other schemes considered, their conservative security and especially their potential for broader applications like anonymous credentials make them very appealing. To showcase the flexibility of the underlying zero-knowledge proof system, we’d like each scheme in this category to present numbers on how well they’d perform with a different underlying hard problem.</p><h3>Structured multivariate: MAYO versus SNOVA</h3><p>Like QR-UOV discussed earlier, MAYO and SNOVA are variants of UOV that add extra structure to the public key to reduce its size. MAYO and SNOVA take two different approaches: SNOVA makes aggressive bets to get the best performance, whereas MAYO treads carefully with a conservative design.</p><p>SNOVA does have impressive performance. Its main parameter set has 248 byte signatures (smaller than RSA-2048!) with only a 1kB public key. It beats every other post-quantum scheme (except SQIsign) on public key+signature size and has great runtime.</p><p>MAYO’s performance is nothing to scoff at either. MAYOone has the best verification time and its 454 byte signatures are still smaller than those of FN-DSA-512, HAWK-512, and RSA-4096. Combined with its 1,420 byte public key, MAYOone does slightly fall behind FN-DSA-512 and HAWK-512. However, MAYO takes the lead again if we ask for some security margin. FN-DSA and HAWK have a missing middle security level and thus need to bump all the way to the 256-bit security, whereas MAYO’s granularity can add extra security at the cost of slightly increasing public key and signature sizes.</p><!-- unsupported block: table --><p>If that wasn’t good enough, both MAYO and SNOVA allow for a trade-off between signature and public key size. Thus, we can get even smaller signatures for public keys that are transmitted ahead of time. Pushed to the extreme, MAYO becomes UOV.</p><p>So far we have discussed performance. What about the security? MAYO adds a “whipping” structure on top of UOV: any attack on UOV will also work for MAYO, but there might be attacks specific to the whipping structure of MAYO. So far no attacks on the whipping structure, and thus on MAYO specifically, have been found. The worst that has happened is that some UOV attacks have affected some MAYO variants more than typical UOV parameter sets, due to the UOV parameter choices that are natural for MAYO.</p><p>This is in stark contrast to SNOVA. SNOVA has been hit quite hard on its specific structure several times. In response, the SNOVA team has not just tweaked parameters, but continuously changed the actual structure. Every time, they take the leap forward and propose a new SNOVA with even better performance. We noted this <a href="https://blog.cloudflare.com/pq-2025/#the-fight-between-mayo-versus-snova"><span style="text-decoration:underline">last year</span></a> and the pattern has continued, whereas MAYO’s basic design is stable.</p><p>Furthermore, the structure SNOVA uses <a href="https://eprint.iacr.org/2024/1297.pdf"><span style="text-decoration:underline">can be</span></a> <a href="https://eprint.iacr.org/2026/659"><span style="text-decoration:underline">seen</span></a> as a special form of the whipping map that MAYO uses. That means that any MAYO-specific attack would apply to SNOVA, but not the other way around.</p><p>All in all, we’ve seen a lot of progress on the understanding of multivariate security. NIST <a href="https://csrc.nist.gov/pubs/ir/8610/final"><span style="text-decoration:underline">wrote</span></a> that they expect an extra round before standardizing a multivariate scheme. That seems prudent. To us, it’s unclear whether SNOVA would be ready by then, but MAYO so far seems to have matured well.</p><h2>Timelines</h2><p>Now, let’s have a look ahead and sketch when these new signature algorithms might become usable.</p><h3>Progress for ML-DSA so far</h3><p>It’s illustrative to look at ML-DSA.</p><!-- unsupported block: table --><p>After NIST selected Dilithium to become ML-DSA, it took a year to draft a proposal for the standard, and another year for the algorithm standard to be published. The algorithm standard is not enough: protocols need to agree on how to integrate ML-DSA. For certificates that took another year. That’s not the end of it: software needs to add support for ML-DSA and its integration into protocols.</p><p>These steps are not purely sequential: work on software implementation of ML-DSA started before the final standard. Also, protocol integration standards are often “done” before they’re a final standard. For instance, the use of ML-DSA in TLS is done, but at the time of writing it’ll take a couple of months before the RFC for that is out. Notably OpenSSL jumped the gun and added support for ML-DSA before the IANA codepoints were assigned. Notably missing still is agreement on which hybrid signatures should be used in TLS (<a href="https://keymaterial.net/2026/06/18/on-hybrid-signatures/"><span style="text-decoration:underline">or at all</span></a>), for which (at the time of writing) no IANA code points have been assigned.</p><h3>When will these new signature algorithms be ready for use?</h3><p>So where does that leave us for new signature algorithms? If the <strong>FN-DSA</strong> draft is released today, and it progresses at the same rate as ML-DSA, then we’d perhaps have some early software support in early 2029, but no significant deployment. Looking at the time it took to write the FN-DSA draft standard, it is likely that the final standard, protocol integrations, and software support will progress slowly as well. We would not expect FN-DSA to be widely available before 2033.</p><p>The progress in cryptanalysis of <strong>multivariate</strong> schemes gave NIST pause: they wrote that they expect multivariate to at least take another round of about two years. On the other hand, multivariate schemes are reasonably easy to implement. That means we might see a multivariate NIST standard in 2031, and wider product availability not earlier than 2034.</p><p>NIST is more confident in the security of <strong>SQIsign</strong> than that of multivariate. Not unlike FN-DSA, SQIsign is a difficult scheme to standardise and implement. At the same time, a lot of progress is made in simplifying SQIsign. It seems likely that SQIsign will make large changes for the third round, and will thus require a fourth round of evaluation. In either case, wide availability before 2035 seems unlikely.</p><p>As discussed <a href="#hawk"><span style="text-decoration:underline">above</span></a>, <strong>HAWK</strong> occupies an awkward middle ground between FN-DSA and structured multivariate candidates. If it were standardized, which seems unlikely even before the recent progress in cryptanalysis, we wouldn’t expect product availability before 2034.</p><p>That leaves the <em>proof of knowledge</em> algorithms <strong>MQOM</strong>, <strong>SDitH</strong>, and <strong>FAEST</strong>. We’ve seen dramatic improvements to these schemes over the rounds. If that rate of change holds, it’ll require another round, but if it’s stable now, a proof of knowledge algorithm will be the first new NIST standard to see the light in 2030. If it’s out this early, it’ll likely not outperform ML-DSA dramatically. Nonetheless, it’ll still be very welcome to build anonymous credentials and other primitives beyond signatures.</p><p>So, should you wait on one of these signatures for your post-quantum migration? Given recent advances in quantum hardware and software, we don’t believe we can afford to wait. At Cloudflare, we’re aiming to be <a href="https://blog.cloudflare.com/post-quantum-roadmap"><span style="text-decoration:underline">fully migrated by 2029</span></a>. None of these signatures will be out in time. Deadlines of most regulators vary between 2030 and 2035. These did not account for recent progress, and we expect them to be adjusted. We saw just this with the <a href="https://blog.cloudflare.com/post-quantum-eo-2026/"><span style="text-decoration:underline">June 2026 US executive order</span></a> setting a 2031 deadline. Even if deadlines weren’t changed, we wouldn’t advise waiting.</p><p>Why? Deploying post-quantum signatures in 2034 to beat a 2035 deadline is not enough. In a system of any reasonable size, you can’t upgrade everything all at once. You’ll need a transition period where both post-quantum and traditional signatures are supported. And supporting both allows for a downgrade attack. The most straightforward way to prevent such downgrades is to disable classical cryptography. That will take time, and is frankly not even an option in many sufficiently distributed systems like the WebPKI. We will cover how to deal with downgrades in a future blog post. In the meantime <a href="https://www.chromium.org/Home/chromium-security/post-quantum-auth-roadmap/"><span style="text-decoration:underline">here</span></a> is some reading if you’re curious. In any case, dealing with downgrades will take time.</p><p>It seems clear these new post-quantum signature algorithms will not be ready to use in time for the first migration. So why bother?</p><h3>Why we still need them</h3><p>We’ve had <a href="https://ee.stanford.edu/~hellman/publications/24.pdf"><span style="text-decoration:underline">50 years</span></a> to weave public key cryptography all through our digital society. We have a few short years left to make it all quantum secure. For most of these upgrades the procedure is clear: drop in post-quantum cryptography. Easier said than done: it’s a monumental task. But then there are cases that are fundamentally harder. There&#x27;s no all-star signature in a post-quantum world, and there are cases where the size of ML-DSA is a problem. With enough resources and stakeholder agreement, systems can be re-engineered to work well with these larger signatures. Indeed, thanks to ongoing re-engineering, the post-quantum WebPKI is shaping up to <a href="https://blog.cloudflare.com/bootstrap-mtc"><span style="text-decoration:underline">perform better</span></a> than the quantum-vulnerable one of today. It is unrealistic to expect that this will happen for every system before it’s too late. Some will have to accept a performance cost. Others will need to deal with the security gap in other ways, such as restricting access, tunneling, more monitoring, or a myriad of other measures that are costly on their own. Once smaller post-quantum signatures arrive, these compensating controls can be removed, and full efficiency and security restored.</p><p>An indirect, but no less important benefit of the ongoing NIST competition is its help in furthering post-quantum cryptography beyond basic primitives: it isn’t just key agreement and signatures that are quantum vulnerable. There is a long tail of <a href="https://github.com/fancy-cryptography/fancy-cryptography"><span style="text-decoration:underline"><em>fancy </em>cryptographic primitives</span></a> out there used in production, such as anonymous credentials, PAKEs, and threshold signatures to name a few. For most, post-quantum variants are not readily available or are understudied. For some, the same goal can be achieved without fancy cryptography, but with a regrettable regression in subtle privacy goals. NIST cannot run a competition to define a post-quantum standard for each of these specific primitives, but luckily the signatures competition has been a huge help here.</p><p>The most clear example is FAEST. Although designed as a signature scheme, its underlying machinery (VOLEitH) can be repurposed in combination with a multivariate scheme like MAYO, to create an efficient <a href="https://www.usenix.org/system/files/conference/usenixsecurity26/sec26_prepub_baum.pdf"><span style="text-decoration:underline">post-quantum anonymous credential</span></a>. Without the signatures competition, VOLEitH wouldn’t be as developed and vetted as it is today.</p><p>Many of the candidate schemes briefly point out their usefulness apart from signatures. We hope to see more of the indirect applications of these schemes highlighted.</p><p>Despite great signatures and more advanced cryptography on the horizon, we should not forget the task at hand: staying secure in the immediate future.</p>]]></content:encoded>
      <guid isPermaLink="false">5wMIF1Oa7DES2YNRpqiVB0</guid>
    </item>
  </channel>
</rss>